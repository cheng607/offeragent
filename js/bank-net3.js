/**
 * OfferAgent · 扩充题库（计算机网络 · 第三批：TCP 细节 / TLS / 网络诊断与优化）
 * 挂载到 global.App.net3Bank，由 bank.js 加载时合并。
 */
(function (global) {
  'use strict';

  var NET3 = [
    {
      id: 'net-101', category: 'network', tags: ['TCP', '状态', 'TIME_WAIT'], difficulty: 3,
      question: 'TCP 的 TIME_WAIT 和 CLOSE_WAIT 状态分别是什么？大量 TIME_WAIT 如何解决？',
      answer: `一句话结论：TIME_WAIT 是主动关闭方在发送最后一个 ACK 后等待 2MSL 的状态；CLOSE_WAIT 是被动关闭方收到 FIN 后、还未调用 close 的状态。

【TIME_WAIT】
1. 出现在主动关闭方（先发 FIN 的一方）；
2. 持续 2MSL（约 1~4 分钟），作用：确保最后一个 ACK 能到达、让旧连接的数据包在网络中消失；
3. 大量 TIME_WAIT 会占用端口和连接资源。

【CLOSE_WAIT】
1. 出现在被动关闭方（收到 FIN 的一方）；
2. 若应用没调用 close，会一直停留在 CLOSE_WAIT，通常意味着「程序 bug：忘了关闭连接」；
3. 大量 CLOSE_WAIT 是应用层问题，不是系统配置问题。

【大量 TIME_WAIT 的解决】
1. 开启 tcp_tw_reuse（复用 TIME_WAIT 连接，仅客户端可用）；
2. 调整 tcp_max_tw_buckets（限制数量）；
3. 让服务器做「被动关闭」方（客户端主动关闭）；
4. 长连接复用，减少连接建立/关闭。

【追问】为什么 TIME_WAIT 要等 2MSL？→ 一是保证最后的 ACK 丢失后能重传 FIN，二是让旧报文在网络中消亡，避免污染新连接。

【避坑】CLOSE_WAIT 堆积要查应用代码（连接没释放），不要只调内核参数；TIME_WAIT 是正常状态，别盲目关闭。`,
      source: 'TCP/IP 详解 / 大厂八股'
    },
    {
      id: 'net-102', category: 'network', tags: ['TCP', '滑动窗口', '流量控制'], difficulty: 3,
      question: 'TCP 的滑动窗口和流量控制机制是怎样的？什么是零窗口？',
      answer: `一句话结论：TCP 用滑动窗口实现流量控制，接收方通过窗口大小（rwnd）告诉发送方还能收多少，避免发送过快淹没接收方。

【滑动窗口机制】
1. 发送方维护一个窗口，窗口内是「已发送未确认 + 可发送」的数据；
2. 窗口大小 = min(拥塞窗口 cwnd, 接收窗口 rwnd)；
3. 收到 ACK 后窗口右移（滑动），可继续发送新数据；
4. 实现批量发送 + 批量确认，提高吞吐。

【流量控制】
1. 接收方在 ACK 中携带 rwnd（接收缓冲区剩余空间）；
2. 发送方据此限制发送量，防止接收方缓冲区溢出。

【零窗口】
1. 接收方缓冲区满时 rwnd=0，发送方停止发送；
2. 发送方会周期性发送「窗口探测」报文，等接收方窗口打开后恢复；
3. 若窗口一直为 0，可能形成死锁，需探测机制打破。

【追问】滑动窗口和拥塞控制有何区别？→ 滑动窗口是「接收方驱动的流量控制」，拥塞控制是「发送方驱动的网络拥塞控制」，两者共同决定发送速率。

【避坑】窗口大小单位是字节（TCP），别和「报文段个数」混淆；rwnd 缩小时要收缩窗口右侧边界。`,
      source: 'TCP/IP 详解 / 大厂八股'
    },
    {
      id: 'net-103', category: 'network', tags: ['TCP', 'Nagle', '延迟确认'], difficulty: 3,
      question: 'Nagle 算法和延迟确认（Delayed ACK）是什么？为什么它们会冲突？',
      answer: `一句话结论：Nagle 是发送端「攒小包」减少小报文，延迟确认是接收端「攒 ACK」减少确认报文；两者叠加可能导致明显延迟。

【Nagle 算法（发送端）】
1. 规则：当已发送的数据未确认时，若下一个数据不足 MSS，则先不发送，攒够 MSS 或等 ACK 再发；
2. 目的：减少网络上的小报文数量，提高带宽利用率；
3. 问题：对延迟敏感的小包（如交互式应用）会变慢。

【延迟确认（接收端）】
1. 收到数据后不立即回 ACK，等一段时间（约 40ms）希望有数据一起捎带（piggyback）ACK；
2. 目的：减少 ACK 报文数量。

【冲突场景】
1. 发送方等 ACK（Nagle 不发小包），接收方等更多数据（延迟回 ACK）；
2. 双方互相等待，形成最多 40ms+ 的延迟；
3. 典型影响：小请求 + 小响应的交互场景变卡。

【解决】
1. 关闭 Nagle：设置 TCP_NODELAY（对交互场景常用）；
2. 关闭延迟确认：设置 TCP_QUICKACK；
3. 应用层避免「write-write-read」模式。

【避坑】延迟敏感的服务（游戏、RPC）建议开 TCP_NODELAY；批量传输可保留 Nagle。`,
      source: 'TCP/IP 详解 / 大厂八股'
    },
    {
      id: 'net-104', category: 'network', tags: ['TCP', '四次挥手', '状态'], difficulty: 2,
      question: '为什么 TCP 需要四次挥手？可以三次吗？为什么有 TIME_WAIT？',
      answer: `一句话结论：因为 TCP 是全双工，双方都要独立关闭自己的发送方向，所以需要四次（或更多）挥手；TIME_WAIT 保证最后 ACK 可靠到达。

【四次挥手过程】
1. 主动方发 FIN，进入 FIN_WAIT_1；
2. 被动方回 ACK，进入 CLOSE_WAIT（此时被动方可能还有数据要发）；
3. 被动方发 FIN，进入 LAST_ACK；
4. 主动方回 ACK，进入 TIME_WAIT，等 2MSL 后 CLOSE。

【为什么不能三次】
· 主动方发 FIN 后，被动方可能还有数据没发完，需要先 ACK 表示「收到关闭请求」，等数据发完再发自己的 FIN；
· 若把 ACK 和 FIN 合并（变成三次），就假设被动方立刻没有数据要发，这不总是成立。

【TIME_WAIT 的作用】
1. 可靠终止：若最后的 ACK 丢失，被动方会重发 FIN，主动方能再次 ACK；
2. 让旧报文消亡：防止旧连接的迟到报文污染新连接。

【追问】关闭时能不能主动方先 ACK 再发 FIN 的顺序合并？→ 可以，若被动方刚好没有数据要发，ACK+FIN 可合并，实际上还是四次报文但可能更紧凑。

【避坑】四次挥手的「四次」是指正常交互；被动方的 ACK 和 FIN 若合并，实际只有三次报文，但逻辑上仍是两个方向各关一次。`,
      source: 'TCP/IP 详解 / 大厂八股'
    },
    {
      id: 'net-105', category: 'network', tags: ['UDP', '可靠传输', 'QUIC'], difficulty: 3,
      question: 'UDP 是不可靠的，如何基于它实现可靠传输？（QUIC/KCP 思路）',
      answer: `一句话结论：在 UDP 之上，由应用层协议自行实现「确认、重传、拥塞控制、有序交付」等机制，即可获得可靠传输（如 QUIC、KCP）。

【为何在 UDP 上做可靠】
1. TCP 实现在内核，协议栈改动困难、握手/队头阻塞等开销大；
2. UDP 灵活，可在用户态快速迭代实现新特性。

【需实现的机制】
1. 序列号：标识每个数据包，保证有序；
2. 确认 ACK：接收方确认收到的包；
3. 重传：超时或收到重复 ACK 时重传（快速重传）；
4. 拥塞控制：类似 TCP 的窗口算法（KCP 用更激进的策略）；
5. 流量控制：接收窗口。

【QUIC 的优势】
1. 0-RTT 握手（缓存密钥后首次连接就发数据）；
2. 无队头阻塞（每个流独立，一个流丢包不影响其他流）；
3. 连接迁移（用连接 ID 而非四元组，切换网络不断连）；
4. 基于 UDP，天然支持多路复用。

【KCP 特点】
· 牺牲带宽换低延迟，比 TCP 快 30%~40%，适合游戏、实时音视频。

【避坑】在 UDP 上重造轮子成本高，是否值得要看场景（低延迟实时 vs 高吞吐稳定）。`,
      source: 'QUIC / KCP 协议'
    },
    {
      id: 'net-106', category: 'network', tags: ['HTTP/3', 'QUIC', '协议'], difficulty: 3,
      question: 'HTTP/3 和 QUIC 是什么关系？它解决了 HTTP/2 的哪些问题？',
      answer: `一句话结论：HTTP/3 是运行在 QUIC 之上的新一代 HTTP；QUIC 是基于 UDP 的传输协议，解决了 HTTP/2 的 TCP 队头阻塞和握手慢等问题。

【层级关系】
HTTP/3（语义层）→ QUIC（传输层，内置 TLS 1.3）→ UDP → IP。

【解决的问题】
1. 队头阻塞：HTTP/2 基于单条 TCP，TCP 丢包会阻塞所有流；QUIC 每个流独立，丢包只影响该流；
2. 握手慢：QUIC 0-RTT/1-RTT，比 TCP+TLS 快；
3. 连接迁移：用连接 ID 标识连接，切换 WiFi/4G 不断连；
4. 内置加密：QUIC 集成 TLS 1.3，加密所有报文。

【核心特性】
1. 0-RTT：缓存服务端密钥后，首次就能发数据；
2. 多路复用无队头阻塞；
3. 前向纠错与更精细的丢包恢复。

【现状】HTTP/3 已在主流浏览器和 CDN 广泛支持，底层可能回退到 TCP。

【追问】HTTP/2 的队头阻塞具体指什么？→ 单 TCP 连接上一个包丢失，后续所有流都要等重传，即使它们无关。

【避坑】HTTP/3 不等于「把 HTTP/2 换成 UDP」，QUIC 实现了大量 TCP 的可靠性机制，是更彻底的协议重构。`,
      source: 'RFC 9114 / QUIC'
    },
    {
      id: 'net-107', category: 'network', tags: ['TLS', 'HTTPS', '握手'], difficulty: 3,
      question: 'TLS 握手过程是怎样的？TLS 1.2 和 1.3 有什么区别？',
      answer: `一句话结论：TLS 握手用于协商加密参数、认证身份、生成会话密钥；1.3 简化了流程，减为 1-RTT，并移除不安全的算法。

【TLS 1.2 握手（2-RTT）】
1. ClientHello：客户端发支持的加密套件、随机数；
2. ServerHello：服务端选加密套件、发证书、随机数；
3. 客户端验证证书，生成预主密钥并用公钥加密发给服务端；
4. 双方用随机数 + 预主密钥算出会话密钥；
5. 切换对称加密通信。

【TLS 1.3 改进（1-RTT）】
1. 握手合并：ClientHello 直接带上密钥交换参数，ServerHello 直接返回密钥材料；
2. 只保留安全的 AEAD 加密套件（如 AES-GCM、ChaCha20）；
3. 支持 0-RTT 会话恢复（PSK 复用）；
4. 移除 RSA 密钥交换（易受攻击），只用前向安全的 ECDHE。

【关键概念】
· 非对称加密交换密钥，对称加密传输数据；
· 证书链验证服务端身份。

【追问】为什么握手要用「非对称 + 对称」混合？→ 非对称安全但慢，用于交换密钥；对称快，用于后续大量数据加密。

【避坑】TLS 1.3 的 0-RTT 有「重放攻击」风险，幂等请求才安全。`,
      source: 'RFC 8446 / 大厂八股'
    },
    {
      id: 'net-108', category: 'network', tags: ['HTTP', '队头阻塞', '性能'], difficulty: 2,
      question: '什么是队头阻塞（Head-of-Line Blocking）？HTTP/1.1 和 HTTP/2 有何不同？',
      answer: `一句话结论：队头阻塞指前面的请求/包阻塞了后面的处理；HTTP/1.1 是「连接级」队头阻塞，HTTP/2 解决了应用层但仍有「TCP 层」队头阻塞。

【HTTP/1.1 的队头阻塞】
1. 一个连接上多个请求必须排队，前一个响应没完成，后面的请求无法发送/处理；
2. 浏览器用「多开连接」（同域 6 个）缓解，但治标不治本。

【HTTP/2 的多路复用】
1. 一个连接上多个「流」并发传输，互不阻塞；
2. 每个流有独立编号，帧可交错发送；
3. 解决应用层队头阻塞。

【HTTP/2 仍有的队头阻塞】
1. 所有流共用一条 TCP，TCP 层一个包丢失，后续所有流都要等重传；
2. 这是「传输层」队头阻塞，只能靠 QUIC/HTTP/3 解决。

【总结】HTTP/1.1（连接级阻塞）→ HTTP/2（应用层解决，传输层仍阻塞）→ HTTP/3（彻底解决）。

【追问】HTTP/2 的多路复用如何实现？→ 用「帧」和「流」，每个流有 ID，帧头标注所属流，交错传输后按流重组。

【避坑】HTTP/2 单连接丢包问题在弱网下明显，这是它不如 HTTP/3 的关键点。`,
      source: 'HTTP/2 / HTTP/3 规范'
    },
    {
      id: 'net-109', category: 'network', tags: ['HTTP', '全流程', '面试'], difficulty: 2,
      question: '从输入 URL 到页面展示，网络层经历了哪些步骤？',
      answer: `一句话结论：先 DNS 解析域名 → 建立 TCP 连接（三次握手）→ 若是 HTTPS 再 TLS 握手 → 发送 HTTP 请求 → 服务器响应 → 浏览器渲染。

【网络层完整流程】
1. DNS 解析：域名 → IP，先查浏览器/系统缓存，再递归查本地 DNS、根域名、顶级域名、权威 DNS；
2. TCP 连接：与目标 IP 三次握手建立连接；
3. TLS 握手：HTTPS 下协商密钥、验证证书；
4. 发送请求：构造 HTTP 请求（方法、路径、头、体）发给服务器；
5. 服务器处理：可能经负载均衡、应用服务器、数据库，返回响应；
6. 传输响应：HTTP 响应（状态码、头、body）返回；
7. 断开或复用连接：keep-alive 复用或四次挥手断开；
8. 浏览器渲染：解析 HTML/CSS/JS，构建 DOM/CSSOM，渲染上屏。

【追问】DNS 查询用 UDP 还是 TCP？→ 常规用 UDP（53 端口），响应大或区域传送用 TCP。

【避坑】HTTP 默认端口 80、HTTPS 443；DNS 解析可能有多级缓存，改域名后要等 TTL 生效。`,
      source: '大厂必考八股'
    },
    {
      id: 'net-110', category: 'network', tags: ['HTTP', 'keep-alive', '长连接'], difficulty: 2,
      question: 'HTTP 的 keep-alive（长连接）是什么？如何工作？',
      answer: `一句话结论：keep-alive 让多个 HTTP 请求复用同一个 TCP 连接，避免每次请求都重新握手，减少延迟和资源开销。

【原理】
1. HTTP/1.0 默认短连接，每个请求都要新建/关闭 TCP；
2. HTTP/1.1 默认开启 keep-alive，连接可复用；
3. 响应头 Connection: keep-alive，配合 Content-Length 或 chunked 分隔响应边界。

【工作方式】
1. 一个 TCP 连接上顺序发送多个请求（HTTP/1.1 串行，HTTP/2 可并发）；
2. 用 Content-Length 或 Transfer-Encoding: chunked 确定响应结束；
3. 空闲超时后关闭连接。

【优点】减少 TCP 三次握手和四次挥手开销，降低延迟；
【缺点】连接占用服务器资源，需合理设置超时和最大请求数。

【相关参数】keep-alive timeout、max requests（Nginx 的 keepalive_timeout）。

【追问】HTTP/1.1 长连接下如何区分响应边界？→ 靠 Content-Length 或 chunked 编码，否则无法判断响应结束。

【避坑】长连接不是「永远不断」，服务器会因超时或资源限制主动关闭；客户端要处理连接被关闭后重连。`,
      source: 'HTTP/1.1 规范 / 大厂八股'
    },
    {
      id: 'net-111', category: 'network', tags: ['TLS', '证书', '双向认证'], difficulty: 3,
      question: 'HTTPS 的证书是如何工作的？什么是双向认证（mTLS）？',
      answer: `一句话结论：证书用于证明服务器（或客户端）身份，由 CA 签名；客户端用 CA 公钥验证证书有效性；双向认证是服务器也要求客户端出示证书。

【单向认证（常规 HTTPS）】
1. 服务器持有证书（含公钥 + CA 签名）；
2. 客户端拿到证书后用内置的 CA 根证书验证签名链；
3. 验证通过后，用证书公钥交换密钥，建立加密通道。

【证书链验证】
· 服务器证书 → 中间 CA → 根 CA，逐级验证签名；
· 根证书预置在操作系统/浏览器信任库中。

【双向认证 mTLS】
1. 服务器在握手中也要求客户端提供证书（CertificateRequest）；
2. 客户端出示自己的证书，服务器验证客户端身份；
3. 适合：微服务间安全通信、金融/企业内部、IoT 设备认证。

【关键概念】
· 证书包含：持有者信息、公钥、有效期、签发者、签名；
· 验证：检查签名、有效期、域名匹配、是否被吊销（OCSP/CRL）。

【避坑】自签名证书需要手动加入信任，否则会报「不受信任」；mTLS 要管理客户端证书的签发与吊销。`,
      source: 'X.509 / TLS 规范'
    },
    {
      id: 'net-112', category: 'network', tags: ['NAT', '内网穿透', '网络'], difficulty: 2,
      question: 'NAT 的原理是什么？有哪些类型？内网穿透如何实现？',
      answer: `一句话结论：NAT 把内网私有地址转换为公网地址，实现多个内网设备共享一个公网 IP 上网；内网穿透则是让外网能访问内网服务。

【NAT 原理】
1. 内网主机发数据包时，NAT 设备把源地址（私网 IP:端口）改成公网 IP:端口，并记录映射；
2. 响应回来时，按映射表把目的地址改回私网地址；
3. 解决 IPv4 地址不足，同时隐藏内网结构。

【NAT 类型】
1. 全锥形（Full Cone）：最宽松；
2. 受限锥形（Restricted Cone）；
3. 端口受限锥形（Port Restricted Cone）；
4. 对称型（Symmetric）：最严格，映射随目的地址变化。

【内网穿透】
1. 反向代理/端口映射：在公网服务器映射内网端口；
2. 反向隧道：内网主动连公网服务器建立隧道（如 frp、ngrok）；
3. 打洞（P2P）：NAT 穿透，利用 STUN/TURN 让两端直连（如 WebRTC、P2P 传输）。

【追问】为什么对称型 NAT 最难穿透？→ 它的映射随目标地址变，无法预知端口，需要 TURN 中继。

【避坑】NAT 会带来「连接超时、端口复用」等问题，长连接要发心跳保活。`,
      source: 'NAT / WebRTC 原理'
    },
    {
      id: 'net-113', category: 'network', tags: ['负载均衡', 'LVS', 'Nginx'], difficulty: 2,
      question: '四层负载均衡（LVS）和七层负载均衡（Nginx）有什么区别？',
      answer: `一句话结论：四层工作在传输层（按 IP+端口转发，如 LVS），七层工作在应用层（按 URL、Header 转发，如 Nginx）。

【四层负载均衡（LVS）】
1. 工作在网络第 4 层（传输层）；
2. 只看 IP 和端口，不看内容；
3. 转发效率高、吞吐大，但灵活性低；
4. 模式：DR、NAT、TUN。

【七层负载均衡（Nginx/HAProxy）】
1. 工作在第 7 层（应用层）；
2. 能解析 HTTP 协议，按 URL、Header、Cookie 路由；
3. 功能丰富：缓存、SSL 卸载、限流、重写，但性能略低于四层；
4. 适合 Web 应用、API 网关场景。

【典型架构】
· 外层 LVS（四层）做流量分发 → 内层 Nginx（七层）做精细路由 → 应用服务器。

【追问】为什么常「四层 + 七层」组合？→ 四层抗高并发、七层做业务路由与缓存，各取所长。

【避坑】四层无法做「基于 URL 的灰度/会话保持」，这些必须七层；七层性能瓶颈可用多实例 + LVS 前置解决。`,
      source: 'LVS / Nginx 架构'
    },
    {
      id: 'net-114', category: 'network', tags: ['安全', 'DDoS', '防护'], difficulty: 2,
      question: '什么是 DDoS 攻击？常见类型有哪些？如何防护？',
      answer: `一句话结论：DDoS 是攻击者用大量受控机器同时向目标发请求，耗尽带宽或资源使其瘫痪；防护靠流量清洗、限流、CDN 和高防等。

【常见类型】
1. SYN Flood：发大量 SYN 不完成握手，占满半连接队列；
2. UDP Flood：发大量 UDP 包占满带宽；
3. HTTP Flood（CC 攻击）：模拟大量 HTTP 请求耗尽应用资源；
4. DNS 反射放大：伪造源地址请求 DNS，放大流量打目标。

【防护手段】
1. 流量清洗/黑洞路由：运营商或高防服务识别并清洗攻击流量；
2. CDN 分散：把流量分散到边缘节点；
3. SYN Cookie：SYN Flood 时用 cookie 机制避免半连接耗尽；
4. 限流：应用层限流、验证码、IP 黑名单；
5. 高防 IP / 云防护（如阿里云高防、Cloudflare）。

【追问】SYN Cookie 的原理？→ 服务器不保存半连接状态，把连接信息编码到 SYN-ACK 的序列号里，收到 ACK 再重建。

【避坑】DDoS 是「消耗战」，单机很难彻底防御，需借助运营商/CDN/云防护的分层能力。`,
      source: '网络安全 / 大厂八股'
    },
    {
      id: 'net-115', category: 'network', tags: ['IPv6', '地址', '网络'], difficulty: 2,
      question: 'IPv4 和 IPv6 有什么区别？为什么要迁移到 IPv6？',
      answer: `一句话结论：IPv6 用 128 位地址解决 IPv4 地址枯竭问题，并带来简化报头、内置安全、自动配置等改进。

【核心区别】
1. 地址长度：IPv4 32 位（约 43 亿），IPv6 128 位（海量）；
2. 表示：IPv4 点分十进制（192.168.1.1），IPv6 冒号十六进制（2001:db8::1）；
3. 报头：IPv6 报头更简单，扩展头可选；
4. 配置：IPv6 支持无状态自动配置（SLAAC）；
5. 安全：IPv6 原生集成 IPsec（虽非强制）。

【为什么迁移】
1. IPv4 地址已分配殆尽；
2. NAT 只是权宜之计，破坏端到端模型；
3. 物联网、海量设备需要更多地址。

【过渡技术】
· 双栈（同时支持 v4/v6）、隧道（6to4）、翻译（NAT64）。

【追问】IPv6 有 NAT 吗？→ 地址充足通常不需要 NAT，但也有 NAT66 用于隔离场景。

【避坑】IPv6 部署要考虑应用、网络设备、DNS 的全面兼容，双栈是主流过渡方案。`,
      source: 'RFC 8200 / 大厂八股'
    },
    {
      id: 'net-116', category: 'network', tags: ['MTU', 'IP分片', 'MSS'], difficulty: 3,
      question: 'MTU、MSS 分别是什么？为什么会有 IP 分片？如何避免？',
      answer: `一句话结论：MTU 是链路层能传输的最大帧大小（以太网默认 1500 字节），MSS 是 TCP 报文段的最大数据大小（MTU - 20 IP 头 - 20 TCP 头）。

【概念】
1. MTU（最大传输单元）：数据链路层的最大载荷，超出需分片；
2. MSS（最大分段大小）：TCP 层协商的数据段大小，避免 IP 分片；
3. 关系：MSS = MTU - IP 头 - TCP 头 = 1500 - 20 - 20 = 1460（以太网）。

【IP 分片问题】
1. 若 IP 包超过路径上的 MTU，路由器会分片，接收端重组；
2. 分片导致：增加开销、任一分片丢失需整包重传、部分设备丢分片包；
3. 分片还可能被防火墙丢弃（尤其是 UDP）。

【如何避免】
1. TCP 通过 MSS 协商，让数据段不超过路径 MTU；
2. 路径 MTU 发现（PMTUD）：探测路径最小 MTU；
3. 应用层控制 UDP 包大小。

【追问】为什么分片对 UDP 影响更大？→ UDP 无 MSS 协商，大包会被 IP 分片，丢失一个分片整个数据报作废。

【避坑】VPN/隧道会降低有效 MTU（多一层封装），常导致分片或「能 ping 通但大包不通」的诡异问题。`,
      source: 'TCP/IP 详解 / 网络诊断'
    },
    {
      id: 'net-117', category: 'network', tags: ['TCP', '拥塞控制', 'BBR'], difficulty: 3,
      question: 'TCP 拥塞控制 BBR 算法是什么？它和传统的基于丢包的算法有何不同？',
      answer: `一句话结论：BBR 不再以「丢包」作为拥塞信号，而是实时测量「带宽」和「往返时延」来调整发送速率，能更好地利用高带宽高延迟网络。

【传统算法（基于丢包）】
1. 慢启动、拥塞避免、快重传、快恢复（Reno/Cubic）；
2. 问题：把丢包当拥塞信号，在有一定丢包率（如无线）的网络上会过早降速，带宽利用率低。

【BBR（瓶颈带宽 + 往返时延）】
1. 持续探测瓶颈带宽 BtlBw 和最小 RTT；
2. 用「带宽 × RTT」估算 BDP（带宽时延积），按 BDP 控制发送速率；
3. 不因偶发丢包就激进降速，充分利用带宽。

【优势】
· 高带宽、高丢包（如跨国、无线）网络下吞吐显著提升；
· 队列延迟更低。

【应用】Google 在 YouTube 等场景大规模部署，Linux 4.9+ 内核支持。

【追问】BBR 的缺点？→ 在公平性上早期版本偏激进，可能挤占其他流量；已多轮改进（BBRv2）。

【避坑】BBR 不是银弹，低速/低丢包网络下和 Cubic 差异不大；理解它要先理解「基于丢包 vs 基于时延」的本质。`,
      source: 'Google BBR / Linux 内核'
    },
    {
      id: 'net-118', category: 'network', tags: ['代理', '正向代理', '反向代理'], difficulty: 2,
      question: '正向代理和反向代理有什么区别？各自的应用场景？',
      answer: `一句话结论：正向代理代表「客户端」访问外部资源，反向代理代表「服务器」接收外部请求，二者方向相反。

【正向代理】
1. 客户端通过代理访问目标服务器，代理对服务器隐藏真实客户端；
2. 典型应用：翻墙访问、内网访问外网、匿名访问、访问控制；
3. 例：客户端配置代理，访问外网。

【反向代理】
1. 客户端访问代理，代理将请求转发给后端服务器，代理对客户端隐藏真实服务器；
2. 典型应用：负载均衡、SSL 卸载、缓存、安全防护、统一入口；
3. 例：Nginx 前置转发到多个应用服务器。

【核心区别】
1. 代理的对象不同：正向代理「客户端」，反向代理「服务器」；
2. 部署位置不同：正向在客户端侧，反向在服务器侧；
3. 隐藏对象不同：正向隐藏客户端，反向隐藏服务器。

【追问】透明代理是什么？→ 客户端无感知，网络层自动把流量导向代理，常用于企业网络审计。

【避坑】Nginx 既可作为反向代理（负载均衡），也可配置为正向代理（转发客户端请求），角色由配置决定。`,
      source: '代理服务器原理 / 大厂八股'
    },
    {
      id: 'net-119', category: 'network', tags: ['HTTP', '状态码', '网关'], difficulty: 2,
      question: 'HTTP 状态码 502、504、499 分别代表什么？如何排查？',
      answer: `一句话结论：502 是网关从上游收到无效响应，504 是网关等待上游超时，499 是客户端主动断开连接（Nginx 自定义）。

【502 Bad Gateway】
1. 网关/代理从上游服务器收到无效响应；
2. 常见原因：上游服务崩溃、进程挂了、上游没监听端口；
3. 排查：看上游服务是否存活、进程是否重启、网关 upstream 配置。

【504 Gateway Timeout】
1. 网关等待上游响应超时；
2. 常见原因：上游处理太慢、死锁、数据库慢查询、超时时间设置太短；
3. 排查：看上游接口耗时、网关 proxy_read_timeout 配置。

【499 Client Closed Request】
1. Nginx 自定义：客户端在服务器响应前主动断开连接；
2. 常见原因：用户取消请求、前端超时主动断开、慢接口导致客户端放弃；
3. 排查：看接口耗时、前端超时设置。

【追问】502 和 504 的本质区别？→ 502 是「上游给了错误响应」，504 是「上游一直没响应直到超时」。

【避坑】排查网关错误要「网关层 + 上游服务层」一起看，先确认上游是否健康，再看超时/连接池配置。`,
      source: 'Nginx / 大厂八股'
    },
    {
      id: 'net-120', category: 'network', tags: ['诊断', 'tcpdump', '排查'], difficulty: 3,
      question: '网络抖动、丢包、连接超时如何排查？tcpdump/wireshark 怎么用？',
      answer: `一句话结论：按「分层定位」思路排查，用 ping/traceroute 看连通性，用 tcpdump/wireshark 抓包分析具体报文，用 ss/netstat 看连接状态。

【排查思路（自底向上）】
1. 物理/链路：网卡、网线、交换机状态；
2. 网络层：ping 测连通和丢包率、traceroute 看路径延迟；
3. 传输层：ss -s 看连接状态、检查 TIME_WAIT/CLOSE_WAIT 堆积；
4. 应用层：看应用日志、接口耗时。

【tcpdump 常用】
1. tcpdump -i eth0 host 10.0.0.1：抓某主机的包；
2. tcpdump -i eth0 port 80：抓某端口；
3. tcpdump -i eth0 -w file.pcap：保存抓包文件，用 wireshark 分析；
4. 常用参数：-n（不解析域名）、-s（抓包长度）、-c（数量）。

【wireshark 分析】
1. 打开 pcap，按流（Follow TCP Stream）看完整交互；
2. 过滤 tcp.retransmission 看重传、tcp.analysis.flags 看异常；
3. 看三次握手、RST、重传判断问题。

【常见结论】
· 大量重传 → 网络丢包/拥塞；
· 收到 RST → 端口未监听或被重置；
· 长时间无 ACK → 对端或中间设备问题。

【避坑】抓包要在问题复现时抓，且尽量在两端都抓对比；加密流量（HTTPS）看不到内容，但能看到握手和重传等传输层现象。`,
      source: 'tcpdump / wireshark 实践'
    }
  ];

  global.App = global.App || {};
  global.App.net3Bank = NET3;
})(window);

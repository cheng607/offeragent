/**
 * OfferAgent · 扩充题库（AI 分类 · 第三批：大模型 / LLM）
 * 挂载到 global.App.aiBank3，由 bank.js 加载时合并。
 */
(function (global) {
  'use strict';

  var AI3 = [
    {
      id: 'ai-057', category: 'ai', tags: ['大模型', 'LLM'], difficulty: 2,
      question: '什么是大语言模型（LLM）？它为什么能「通用」？',
      answer: `一句话结论：大语言模型是参数量巨大（十亿~万亿级）的自回归语言模型，通过在海量文本上预训练学到通用知识与能力，再用提示/微调适配各类任务。

【核心概念】
· 基础架构：大多基于 Transformer Decoder，自回归地「预测下一个 token」。
· 规模效应：参数量、数据量、算力同步放大后，涌现出小模型没有的能力（推理、指令遵循、上下文学习）。

【为什么能通用】
1. 语言本身是通用的知识载体，学「预测下一个词」被迫学会语法、常识、推理；
2. 涌现能力：规模越过阈值后，出现零样本/少样本泛化、思维链等能力；
3. 统一接口：一切任务都可表示为「文本进、文本出」。

【面试追问】涌现（Emergence）是什么？→ 小模型没有、模型大到一定程度突然出现的能力，如多步推理、指令遵循。

【避坑】LLM 的「知识」是概率性的，不是数据库，会出错（幻觉）；它的「通用」来自统计规律而非真正理解。`,
      source: '大模型八股文 / 面试高频'
    },
    {
      id: 'ai-058', category: 'ai', tags: ['大模型', 'LLM'], difficulty: 3,
      question: 'GPT 和 BERT 有什么区别？',
      answer: `一句话结论：BERT 是「双向 Transformer 编码器」，用掩码语言模型，擅长理解类任务；GPT 是「单向自回归解码器」，预测下一个 token，擅长生成类任务。

【架构对比】
· BERT：只用 Encoder，双向注意力（能看到前后文）；
· GPT：只用 Decoder，带掩码的自注意力（只看左边/上文）。

【预训练目标】
· BERT：MLM（随机 mask 一些词让模型预测）+ NSP；
· GPT：CLM（自回归预测下一个 token）。

【适用任务】
· BERT：分类、NER、抽取式问答、语义匹配（理解为主）；
· GPT：文本生成、对话、续写、代码生成（生成为主）。

【面试追问】为什么现在大模型几乎都走 GPT 路线（decoder-only）？→ 自回归生成统一了所有任务形式，扩展性更好、训练更高效，且涌现出的通用能力更强。

【避坑】BERT 不能直接做生成，GPT 早期不适合做双向理解（现通过 RLHF 等补齐了指令理解能力）。`,
      source: '大模型八股文 / 面试高频'
    },
    {
      id: 'ai-059', category: 'ai', tags: ['大模型', '注意力'], difficulty: 3,
      question: '自注意力机制中 Q、K、V 分别代表什么？计算流程是怎样的？',
      answer: `一句话结论：Q（Query）是「我要找什么」，K（Key）是「我有什么标签」，V（Value）是「实际内容」；用 Q 与 K 的相似度算注意力权重，再对 V 加权求和。

【计算流程】
1. 由输入 X 经三个可学习矩阵得到 Q、K、V：
   Q = X·W_Q，K = X·W_K，V = X·W_V；
2. 算相似度分数：S = Q·K^T / √d_k（除以 √d_k 防止点积过大导致 softmax 饱和）；
3. 归一化：A = softmax(S)，得到注意力权重；
4. 加权求和：Output = A·V。

【直观理解】
像数据库检索：Q 是查询，K 是键，V 是值，用 Q 去匹配 K 得到「关注度」，再取对应的 V。

【面试追问】为什么要除以 √d_k？→ d_k 越大点积越大，softmax 会进入饱和区、梯度变小，除以 √d_k 做缩放保持梯度稳定。

【避坑】Q/K/V 都是「同一输入经过不同线性变换」得到（自注意力），这是与交叉注意力的区别。`,
      source: 'Attention Is All You Need / 大模型八股'
    },
    {
      id: 'ai-060', category: 'ai', tags: ['大模型', '注意力'], difficulty: 3,
      question: '为什么要用多头注意力（Multi-Head Attention）？',
      answer: `一句话结论：多头注意力把 Q/K/V 拆成多个「头」，每个头在不同的子空间里学习不同的关注模式，再拼接，让模型能同时捕捉多种关系。

【原理】
1. 把 d_model 拆成 h 个头，每个头维度 d_k = d_model / h；
2. 每个头独立做自注意力；
3. 把所有头的输出拼接，再过一次线性变换。

【为什么要多头】
· 单头只能学一种注意力模式；
· 多头让不同头关注不同位置/关系（如一个头关注「主谓」，另一个关注「指代」）；
· 增强表达能力的多样性。

【面试追问】多头的计算量和单头一样吗？→ 总参数量不变（总维度相同），只是拆成多份并行计算，所以几乎不增加开销。

【避坑】头的数量不是越多越好，需与模型维度匹配；理解「不同子空间捕捉不同关系」是多头的本质。`,
      source: 'Attention Is All You Need / 大模型八股'
    },
    {
      id: 'ai-061', category: 'ai', tags: ['大模型', '预训练'], difficulty: 3,
      question: 'MLM 和 CLM 两种预训练目标有什么区别？',
      answer: `一句话结论：MLM（掩码语言模型）随机 mask 部分 token 让模型用上下文双向预测，适合理解；CLM（因果/自回归语言模型）只允许看上文、预测下一个 token，适合生成。

【MLM】
· 随机 mask 15% 的 token，让模型预测被 mask 的词；
· 双向：能看到前后文；
· 代表：BERT。

【CLM】
· 按顺序预测下一个 token（P(x_t | x_<t)）；
· 单向（因果掩码）：只能看左边；
· 代表：GPT 系列。

【对比】
· MLM 的 15% mask 导致预训练效率低（每步只学 15% 位置），且预训练与下游有 gap（下游没有 [MASK]）；
· CLM 每个 token 都参与预测，样本利用率 100%，且生成天然就是「预测下一个」，预训练与推理一致。

【面试追问】为什么现在主流是 CLM？→ 生成式任务统一、训练效率高、扩展性强，配合 RLHF 能补齐指令理解。

【避坑】CLM 的「因果掩码」是它只能单向的根本原因；理解两种 mask 矩阵是手写 Transformer 的关键。`,
      source: '大模型八股文 / 面试高频'
    },
    {
      id: 'ai-062', category: 'ai', tags: ['大模型', '对齐'], difficulty: 3,
      question: '什么是 RLHF（基于人类反馈的强化学习）？它的流程是什么？',
      answer: `一句话结论：RLHF 用人类偏好训练一个奖励模型，再用强化学习（PPO）优化语言模型，使其输出更符合人类期望（有用、无害、诚实）。

【三步流程】
1. SFT（监督微调）：用人工标注的高质量「指令-回答」对微调基座模型，得到 SFT 模型；
2. 奖励模型（RM）：让人对同一 prompt 的多个回答排序，训练一个打分模型（reward model）模拟人类偏好；
3. PPO 强化学习：以 RM 为奖励信号，用 PPO 算法优化 SFT 模型，同时加 KL 惩罚防止偏离太远。

【为什么需要】
基座模型「预测下一个词」并不等于「说有用的话」；RLHF 把「人类偏好」注入模型，对齐人类价值观。

【面试追问】PPO 的 KL 惩罚作用？→ 防止模型为了刷高奖励而偏离原始语言能力（防止「奖励黑客」/胡言乱语）。

【避坑】RLHF 流程复杂、训练不稳定，现在被更简单的 DPO（直接偏好优化）部分替代，后者不用单独训 RM。`,
      source: 'InstructGPT / 大模型八股'
    },
    {
      id: 'ai-063', category: 'ai', tags: ['大模型', '微调'], difficulty: 2,
      question: '什么是 SFT（监督微调）？它和预训练、RLHF 是什么关系？',
      answer: `一句话结论：SFT 用高质量「指令-回答」数据对预训练模型做监督学习，让它学会「遵循指令」；它是 RLHF 三步（SFT→RM→PPO）的第一步。

【核心概念】
· 预训练模型会「续写」但不会「听话」；
· SFT 用 (prompt, 理想回答) 数据对，做标准语言模型训练（算交叉熵）；
· 训练后模型能理解并遵循指令、按格式回答。

【在 RLHF 中的地位】
SFT 是「对齐」的起点：先把模型教成「会回答」，再用 RLHF 进一步优化「回答得好不好」。

【数据要求】
· 高质量、多样、覆盖广泛任务；
· 数据质量 >> 数量，少量优质数据常优于大量噪音数据。

【面试追问】SFT 会改变模型的「知识」吗？→ 主要是改变「行为/风格」，新增知识有限；想注入新知识靠 RAG 或继续预训练更有效。

【避坑】SFT 数据若太窄会导致「灾难性遗忘」或过拟合某种风格；学习率要小。`,
      source: '大模型微调实践 / 面试高频'
    },
    {
      id: 'ai-064', category: 'ai', tags: ['大模型', '微调'], difficulty: 3,
      question: '什么是 LoRA？它为什么能高效微调大模型？',
      answer: `一句话结论：LoRA 冻结原模型权重，只训练注入的低秩矩阵（ΔW = BA），用极少参数量实现与大模型微调接近的效果，大幅降低显存和成本。

【原理】
· 原权重 W 冻结，不更新；
· 引入低秩分解 ΔW = B·A（A、B 是两个小矩阵，秩 r 很小）；
· 前向：h = W·x + ΔW·x = W·x + B·A·x；
· 训练只更新 A、B，参数量从 d×d 降到 2·d·r。

【为什么高效】
1. 可训练参数极少（常 <1%）；
2. 显存省（不用存原权重的梯度/优化器状态）；
3. 训练快、可合并回原权重（W + BA）或保持分离热切换。

【面试追问】为什么低秩就够？→ 微调时的权重更新 ΔW 通常落在低秩子空间，信息增量有限，低秩近似足够表达。

【避坑】r 太小会欠拟合、太大失去高效意义（常用 8~64）；QLoRA 进一步把基座量化到 4bit，显存再降。`,
      source: 'LoRA 论文 / 大模型八股'
    },
    {
      id: 'ai-065', category: 'ai', tags: ['大模型', '推理'], difficulty: 3,
      question: '模型量化（Quantization）是什么？INT8/INT4 量化如何减少显存？',
      answer: `一句话结论：量化把模型权重/激活从 FP32/FP16 转成低比特（INT8/INT4）表示，用精度换显存和速度，是部署大模型的关键技术。

【原理】
· FP32 每个权重 4 字节，INT8 只有 1 字节、INT4 只有 0.5 字节；
· 量化：把浮点数映射到低比特整数（配 scale 和 zero-point）；
· 反量化：推理时把整数还原成近似浮点。

【常见方法】
1. 训练后量化 PTQ：直接对训练好的模型量化，简单快速（如 GPTQ、AWQ）；
2. 量化感知训练 QAT：训练时就模拟量化误差，精度更高但需重训。

【显存收益】INT8 约减半、INT4 约减到 1/4，让 70B 模型能跑在消费级显卡上。

【面试追问】量化为什么基本不掉精度？→ 大模型权重冗余度高，低比特足以保留大部分信息；配合校准集优化 scale 可进一步保精度。

【避坑】INT4 下部分任务精度损失明显，需实测；量化 + LoRA（QLoRA）是低成本微调大模型的流行组合。`,
      source: '大模型部署八股 / 面试高频'
    },
    {
      id: 'ai-066', category: 'ai', tags: ['大模型', '推理'], difficulty: 3,
      question: '什么是 KV Cache？它为什么能加速大模型推理？',
      answer: `一句话结论：KV Cache 把已生成 token 的 K、V 向量缓存下来，后续步只算新 token 的 Q、K、V 并复用历史 KV，避免重复计算，把解码从 O(n²) 降到增量计算。

【原理】
· 自回归生成是逐个 token 生成；
· 没有缓存时，每生成一个 token 都要重新算所有历史 token 的 K、V，计算量随序列平方增长；
· 有了 KV Cache，历史 token 的 K、V 存起来，新步只需算当前 token，再和历史 KV 拼起来做注意力。

【为什么重要】
· 解码阶段是生成的主要耗时，KV Cache 让每步只算增量；
· 代价是显存随序列长度线性增长。

【面试追问】KV Cache 的显存瓶颈？→ 长序列时 KV Cache 可能比权重还大，催生了 PagedAttention（vLLM）、GQA/MQA（减少 KV 头数）等优化。

【避坑】batch 越大、序列越长，KV Cache 显存越可观；MQA/GQA 是「减少 KV 缓存」的主流方案。`,
      source: '大模型推理八股 / 面试高频'
    },
    {
      id: 'ai-067', category: 'ai', tags: ['大模型', '推理'], difficulty: 3,
      question: '大模型推理有哪些加速方法？',
      answer: `一句话结论：从「算法、系统、量化、并行」四方面加速——KV Cache、投机采样、PagedAttention、量化、算子优化、批处理、分布式推理等。

【主要方法】
1. KV Cache：复用历史 K/V，避免重复计算。
2. 量化：INT8/INT4 降低显存与计算。
3. PagedAttention（vLLM）：把 KV Cache 分页管理，减少显存碎片、提升吞吐。
4. 投机采样（Speculative Decoding）：小模型「草稿」生成多个 token，大模型并行验证，加速解码。
5. 算子/内核优化：FlashAttention（减少显存读写）、融合算子。
6. 批处理：Continuous Batching 提高 GPU 利用率。
7. 蒸馏/剪枝：缩小模型本身。

【面试追问】FlashAttention 加速了什么？→ 不把注意力矩阵完整写回显存，用分块 + 在线 softmax 计算，降低显存 IO 瓶颈。

【避坑】「加速」要分清是「延迟优先」还是「吞吐优先」，不同场景选不同技术组合。`,
      source: '大模型推理八股 / 面试高频'
    },
    {
      id: 'ai-068', category: 'ai', tags: ['大模型', 'RAG'], difficulty: 3,
      question: '什么是 RAG（检索增强生成）？它的流程和优点是什么？',
      answer: `一句话结论：RAG 在生成前先从外部知识库检索相关内容，拼进提示词再让大模型生成，从而获得实时、准确、可溯源的知识，缓解幻觉。

【流程】
1. 索引：把文档切块 → 用 Embedding 模型向量化 → 存入向量数据库；
2. 检索：用户 query 向量化 → 在向量库中检索最相似的 top-k 块；
3. 增强：把检索到的内容拼进 prompt（作为上下文）；
4. 生成：大模型基于「问题 + 检索内容」生成回答。

【优点】
1. 知识实时更新，无需重训模型；
2. 减少幻觉、答案可溯源；
3. 可接入私有/领域知识；
4. 成本低（不用微调）。

【面试追问】RAG 和微调怎么选？→ 需要实时/私有/可溯源知识用 RAG；需要改变模型风格/能力用微调；两者可结合。

【避坑】检索质量决定 RAG 上限——切块策略、Embedding 质量、召回排序都是关键；「检索到无关内容」反而会误导生成。`,
      source: '大模型应用八股 / 面试高频'
    },
    {
      id: 'ai-069', category: 'ai', tags: ['大模型', '向量数据库'], difficulty: 2,
      question: '向量数据库是什么？为什么 RAG 需要它？',
      answer: `一句话结论：向量数据库专门存储和检索高维向量，用近似最近邻（ANN）算法做「语义相似度搜索」，是 RAG 的知识底座。

【核心概念】
· 向量：文本/图片经 Embedding 模型转成的稠密向量，语义相近的向量距离近；
· 检索：给定 query 向量，找最相似的 top-k 个向量；
· 相似度：余弦相似度、内积、欧氏距离。

【为什么需要专门数据库】
1. 高维向量的精确最近邻搜索很慢（维度灾难），需要 ANN 索引加速；
2. 支持海量向量的增删改查、元数据过滤、混合检索；
3. 传统数据库无法高效做「语义搜索」。

【常见产品】Milvus、Pinecone、Weaviate、Qdrant、FAISS（库）、pgvector。

【面试追问】ANN 有哪些算法？→ HNSW（图索引，最常用）、IVF（倒排）、PQ（乘积量化）等，都是「精度换速度」。

【避坑】向量库不是万能的，精确匹配（如关键词、ID）仍要用传统索引；「混合检索」（向量 + 关键词）常效果更好。`,
      source: '大模型应用八股 / 面试高频'
    },
    {
      id: 'ai-070', category: 'ai', tags: ['大模型', 'Embedding'], difficulty: 2,
      question: '什么是 Embedding（词/文本向量化）？它在 LLM 中扮演什么角色？',
      answer: `一句话结论：Embedding 把离散的 token/文本映射成连续稠密向量，让语义相近的对象在向量空间中距离相近，是 LLM 理解与检索的基础。

【核心概念】
· 输入 Embedding：把 token 映射为初始向量，再送入 Transformer；
· 语义向量：整句/整段编码成的向量，用于检索、聚类、相似度计算。

【在 LLM 中的角色】
1. 输入层：token → 向量（+ 位置编码）才能被网络处理；
2. 检索（RAG）：query 与文档都向量化，做语义匹配；
3. 分类/聚类：向量作为下游任务的特征。

【面试追问】好的 Embedding 应该满足什么？→ 语义相似的文本向量距离近（相似性）、能区分不同语义（区分度）、对噪声鲁棒。

【避坑】Embedding 模型和生成模型是两回事；RAG 中检索向量与生成模型要用「配套/兼容」的 Embedding 才效果好。`,
      source: '大模型应用八股 / 面试高频'
    },
    {
      id: 'ai-071', category: 'ai', tags: ['大模型', '提示工程'], difficulty: 2,
      question: '什么是 Prompt Engineering？有哪些常用技巧？',
      answer: `一句话结论：Prompt Engineering 是精心设计输入提示词，引导大模型输出更准确、更符合预期的结果，常用技巧有角色设定、少样本、思维链、结构化输出等。

【常用技巧】
1. 角色设定：让模型扮演专家（「你是一名资深架构师…」）；
2. 少样本（Few-shot）：给几个「输入-输出」示例，让模型模仿；
3. 思维链（CoT）：加「让我们一步步思考」，引导推理；
4. 明确约束：指定输出格式、长度、语言；
5. 结构化输出：要求 JSON/Markdown 格式；
6. 拆解任务：复杂任务分步引导。

【面试追问】零样本、少样本、上下文学习的区别？→ 零样本不给示例，少样本给少量示例，都是在「不更新参数」下靠提示词激发能力。

【避坑】提示词要具体、避免歧义；「太长/太模糊」都会让效果打折，需结合具体模型反复迭代。`,
      source: '大模型应用八股 / 面试高频'
    },
    {
      id: 'ai-072', category: 'ai', tags: ['大模型', 'Function Calling'], difficulty: 3,
      question: '什么是 Function Calling（函数调用 / 工具调用）？它的工作流程是什么？',
      answer: `一句话结论：Function Calling 让大模型在需要时输出「结构化调用请求」去调用外部函数/API，从而突破「只能生成文本」的限制，能查数据、执行操作。

【工作流程】
1. 定义工具：给模型描述可用函数的名称、参数 schema、用途；
2. 模型决策：模型判断用户意图，若需要就输出一个「函数调用」（含函数名 + JSON 参数），而不是直接回答；
3. 执行：应用层解析调用、真正执行函数拿到结果；
4. 回填：把函数结果再喂给模型，模型综合后生成最终回答。

【为什么重要】
· 让 LLM 能获取实时数据（查天气、查数据库）；
· 是 Agent 的基础——模型负责「决策」，工具负责「执行」。

【面试追问】Function Calling 和 RAG 的区别？→ RAG 是「检索文本拼进上下文」，Function Calling 是「模型主动发起调用」，更通用，可调用任意 API。

【避坑】模型输出的参数可能不合 schema，需要校验与重试；工具描述要清晰，否则模型会乱调用。`,
      source: '大模型应用八股 / 面试高频'
    },
    {
      id: 'ai-073', category: 'ai', tags: ['大模型', 'Agent'], difficulty: 3,
      question: '什么是 AI Agent？它与普通大模型对话有什么区别？',
      answer: `一句话结论：Agent 是「大模型 + 工具 + 规划 + 记忆」的自主系统，能分解任务、调用工具、循环执行直到完成目标；普通对话是「一问一答」的被动响应。

【核心组成】
1. 大脑（LLM）：负责理解、推理、决策；
2. 规划（Planning）：任务分解、制定步骤（ReAct、CoT）；
3. 工具（Tools）：搜索、代码执行、API、浏览器等；
4. 记忆（Memory）：短期（上下文）+ 长期（向量库/数据库）。

【与普通对话的区别】
· 普通对话：用户问 → 模型答，单轮被动；
· Agent：给定目标 → 自主规划 → 调工具 → 观察结果 → 迭代循环，直到完成。

【典型模式】ReAct（推理 Reasoning + 行动 Acting 交替）、多 Agent 协作（分工、辩论）。

【面试追问】Agent 的难点？→ 长程任务的状态管理、工具调用可靠性、错误恢复、循环失控（需要约束和终止条件）。

【避坑】「Agent」不是万能，简单任务用普通对话更稳；复杂任务才需要 Agent，且要加好工具和评测。`,
      source: '大模型应用八股 / 面试高频'
    },
    {
      id: 'ai-074', category: 'ai', tags: ['大模型', '推理'], difficulty: 2,
      question: '什么是思维链（Chain-of-Thought, CoT）？它为什么能提升推理能力？',
      answer: `一句话结论：CoT 是让模型「把中间推理步骤写出来」再给答案，把复杂问题拆成逐步推理，显著提升数学、逻辑等任务的表现。

【核心思想】
· 不直接给答案，而是引导模型输出「推理过程 → 最终答案」；
· 触发方式：提示词加「Let's think step by step」或用少样本示例。

【为什么有效】
1. 自回归模型一次生成一个 token，逐步推理相当于「用更多计算展开思考」；
2. 中间步骤提供「脚手架」，降低一次性跳到答案的难度；
3. 让模型「边说边想」，减少跳步错误。

【面试追问】CoT 和「零样本 CoT」区别？→ 经典 CoT 需要少样本示例，零样本 CoT 只需一句话「一步步想」就能激发。

【避坑】CoT 对简单任务可能没帮助甚至引入错误；其本质是「用计算换推理」，是很多「深度思考」模型的核心。`,
      source: 'Chain-of-Thought 论文 / 大模型八股'
    },
    {
      id: 'ai-075', category: 'ai', tags: ['大模型', '幻觉'], difficulty: 2,
      question: '什么是大模型幻觉（Hallucination）？如何缓解？',
      answer: `一句话结论：幻觉是模型生成的内容「看似合理但事实错误或无依据」，源于概率生成而非真实知识；靠 RAG、约束、后校验、降低温度等方式缓解。

【产生原因】
1. 模型本质是「预测下一个 token」，不是查数据库；
2. 训练数据缺失/过时，模型「编造」补全；
3. 过高的采样随机性（温度过高）；
4. 缺乏外部知识锚定。

【缓解方法】
1. RAG：接入外部知识库，用检索到的真实内容约束生成；
2. 提示约束：要求「不知道就说不知道」「给出依据」；
3. 降低温度、控制采样；
4. 后校验：用工具/规则/另一个模型检查事实；
5. 微调：用高质量对齐数据减少幻觉倾向。

【面试追问】幻觉能完全消除吗？→ 目前不能，只能「降低概率」；这是大模型落地的核心风险点。

【避坑】把 LLM 用于「精确事实查询」（如医疗、法律、金融）必须加 RAG 或人工审核，不能裸用。`,
      source: '大模型应用八股 / 面试高频'
    },
    {
      id: 'ai-076', category: 'ai', tags: ['大模型', '长文本'], difficulty: 3,
      question: '大模型如何处理长文本？有哪些长上下文技术？',
      answer: `一句话结论：长文本的挑战在于自注意力 O(n²) 复杂度与位置编码的外推；靠稀疏注意力、位置编码改进（RoPE 外推）、KV Cache 优化、上下文压缩等技术处理。

【核心挑战】
1. 自注意力复杂度随长度平方增长；
2. 训练长度有限，推理超长时位置编码外推能力不足；
3. KV Cache 显存随长度线性增长。

【处理技术】
1. 位置编码外推：RoPE + 缩放（如 NTK-aware、YaRN），让训练长度外的位置也能工作；
2. 稀疏/线性注意力：把 O(n²) 降到 O(n log n) 或 O(n)；
3. 滑动窗口 + 全局注意力（如 Longformer）；
4. 上下文压缩/摘要：先压缩历史再送入；
5. KV Cache 优化：GQA、量化 KV。

【面试追问】什么是「长度外推」？→ 模型在训练长度（如 4k）之外推理仍能保持性能的能力。

【避坑】「支持 128k 上下文」不代表「128k 内都记得准」，长文本中间信息常被「遗忘」（Lost in the Middle 现象）。`,
      source: '大模型八股文 / 面试高频'
    },
    {
      id: 'ai-077', category: 'ai', tags: ['大模型', '采样'], difficulty: 2,
      question: 'temperature、top-k、top-p 分别控制什么？如何配合使用？',
      answer: `一句话结论：temperature 控制概率分布的「尖锐度」，top-k 只保留概率最高的 k 个 token，top-p（核采样）保留累计概率达 p 的最小集合，三者共同控制生成的随机性与质量。

【三者详解】
· temperature T：softmax(z/T)，T 低 → 分布集中（更确定、更保守），T 高 → 分布平滑（更多样、更随机）；
· top-k：只从概率最高的 k 个 token 里采样，截断长尾；
· top-p（nucleus sampling）：按概率从高到低累加，截到累计概率 ≥ p 的动态集合，集合大小随分布自适应。

【如何配合】
· 追求稳定/准确（代码、事实问答）：T 低（0~0.5）+ 适度 top-p；
· 追求创意/多样（写作、头脑风暴）：T 高（0.7~1.0）；
· 常设 top-p=0.9~0.95、top-k=50 左右，避免长尾脏 token。

【面试追问】top-k 的缺点？→ 固定 k 对「分布尖/平」不自适应：尖分布会漏掉合理的词，平分布会包含很多垃圾 token；top-p 更自适应。

【避坑】贪心解码（T=0 或 argmax）最确定但易重复；三者是「截断 + 缩放」的组合拳，要按任务调。`,
      source: '大模型应用八股 / 面试高频'
    },
    {
      id: 'ai-078', category: 'ai', tags: ['大模型', '评测'], difficulty: 2,
      question: '如何评测大模型？有哪些评测维度和方法？',
      answer: `一句话结论：大模型评测分「知识、推理、生成质量、安全性、指令遵循」等维度，方法有人工评估、自动指标、基准测试集、以及用「模型评模型」。

【评测维度】
1. 知识：MMLU（多任务知识）、C-Eval（中文）；
2. 推理：GSM8K（数学）、BBH、ARC；
3. 代码：HumanEval、MBPP；
4. 指令遵循/对话：MT-Bench、AlpacaEval；
5. 安全：有害性、偏见、幻觉率。

【评测方法】
1. 自动指标：BLEU/ROUGE（生成与参考的重合度，局限大）；
2. 基准集：跑固定题集算准确率；
3. LLM-as-a-Judge：用强模型打分（如 GPT-4 评分）；
4. 人工评估：最准但成本高。

【面试追问】BLEU 的局限？→ 只看 n-gram 重合，无法衡量语义/多样性，生成任务中误导性强。

【避坑】单一指标不可信，要「多维度 + 多基准」综合看；评测集可能被训练数据污染，需注意时效与泄漏。`,
      source: '大模型评测八股 / 面试高频'
    },
    {
      id: 'ai-079', category: 'ai', tags: ['大模型', '多模态'], difficulty: 2,
      question: '什么是多模态大模型？它如何同时理解文本和图像？',
      answer: `一句话结论：多模态大模型能同时处理文本、图像、音频等多种模态，通常用「视觉编码器把图像转成 token 序列，再和文本一起送入 LLM」实现跨模态理解。

【常见架构】
1. 视觉编码器（如 CLIP 的 ViT）把图像切成 patch，编码成视觉 token；
2. 用投影层把视觉 token 对齐到 LLM 的文本嵌入空间；
3. 视觉 token 和文本 token 拼接后一起送入 LLM 生成回答。

【代表】GPT-4V、LLaVA、Qwen-VL、Gemini、CLIP（对比学习对齐图文）。

【关键点】
· 对齐：让视觉表示「进入」语言模型的语义空间（对齐层/预训练对齐）；
· 多模态任务：图像问答、图文生成、OCR、视频理解。

【面试追问】CLIP 是怎么训练的？→ 对比学习：让配对的图文向量靠近、不配对的远离，得到对齐的图文表征。

【避坑】多模态 ≠ 简单拼接，关键是「跨模态对齐」；不同模态的 token 化和对齐是核心难点。`,
      source: '多模态大模型八股 / 面试高频'
    },
    {
      id: 'ai-080', category: 'ai', tags: ['大模型', '并行'], difficulty: 3,
      question: '数据并行、张量并行、流水线并行有什么区别？',
      answer: `一句话结论：数据并行把数据切到多卡、每卡持有完整模型；张量并行把单层的矩阵运算切到多卡；流水线并行把不同层放到不同卡；大模型训练常三者结合（3D 并行）。

【三种并行】
1. 数据并行（DP）：每张卡一份完整模型，切分 batch 训练，梯度 all-reduce 同步。简单，但显存放不下大模型时失效。
2. 张量并行（TP）：把一层的大矩阵按列/行切分到多卡，通信频繁（卡间带宽要求高，适合单机内 NVLink）。
3. 流水线并行（PP）：把模型按层切分，不同层在不同卡，像流水线传递激活。显存省，但有空泡（bubble）浪费。

【3D 并行】
大模型训练 = DP × TP × PP 组合：DP 跨节点、TP 节点内、PP 按层，配合 ZeRO（优化器状态分片）进一步省显存。

【面试追问】ZeRO 做了什么？→ 把优化器状态/梯度/参数分片到多卡，解决「数据并行下每卡都要存完整优化器状态」的显存瓶颈。

【避坑】并行不是越多越好，通信开销会抵消收益，需按模型规模和硬件拓扑选组合。`,
      source: '大模型训练八股 / 面试高频'
    },
    {
      id: 'ai-081', category: 'ai', tags: ['大模型', '蒸馏'], difficulty: 2,
      question: '什么是知识蒸馏（Knowledge Distillation）？',
      answer: `一句话结论：知识蒸馏让「小模型（学生）」学习「大模型（教师）」的输出分布（软标签），把大模型的知识迁移到小模型，实现模型压缩。

【核心思想】
· 教师模型（大）对样本输出「软标签」（概率分布，含类别间关系）；
· 学生模型（小）用软标签 + 硬标签联合训练；
· 损失：KL 散度（学生分布 vs 教师软标签）+ 交叉熵（vs 真实标签）。

【为什么要软标签】
软标签（如「猫 0.7、虎 0.25、狗 0.05」）比硬标签（「猫 1.0」）多了「暗知识」——类别之间的相似关系，学生能学到更多。

【用途】
· 模型压缩（大模型 → 小模型部署）；
· 提升小模型精度；
· 大模型时代：用 GPT-4 等强模型蒸馏出小模型。

【面试追问】温度在蒸馏中的作用？→ 高温度让软标签更「软」，凸显类别间差异，利于传递暗知识。

【避坑】教师模型要足够强；温度、损失权重是超参，需调。`,
      source: '模型压缩八股 / 面试高频'
    },
    {
      id: 'ai-082', category: 'ai', tags: ['大模型', '对齐'], difficulty: 3,
      question: 'DPO（直接偏好优化）和 RLHF 有什么区别？',
      answer: `一句话结论：RLHF 需要先训奖励模型再用 PPO 强化学习（三步、复杂不稳定）；DPO 直接把「偏好对」变成损失函数，跳过奖励模型，一步训练、更简单稳定。

【RLHF】
· 三步：SFT → 训奖励模型（RM）→ PPO 优化；
· 用 RM 打分 + 强化学习，需维护多个模型、训练复杂。

【DPO】
· 用「同一 prompt 的偏好回答 vs 非偏好回答」数据对；
· 直接最大化「偏好回答相对于非偏好回答的概率差」，一步完成对齐；
· 不需要单独的 RM 和 PPO。

【对比】
· 效果：两者接近，DPO 更稳定、实现简单；
· 成本：DPO 更省（少训一个模型、少一阶段）；
· 局限：DPO 对偏好数据质量敏感，离线数据可能过拟合。

【面试追问】DPO 为什么能替代 PPO？→ 从数学上，最优策略与奖励模型有闭式解，DPO 直接优化该目标，绕开了显式的 RM。

【避坑】「DPO 一定优于 RLHF」不成立，不同场景要实测；偏好数据质量是两者共同的关键。`,
      source: 'DPO 论文 / 大模型八股'
    },
    {
      id: 'ai-083', category: 'ai', tags: ['大模型', 'MoE'], difficulty: 3,
      question: '什么是 MoE（混合专家模型）？它如何做到「大参数量、小计算量」？',
      answer: `一句话结论：MoE 把 FFN 层替换成多个「专家」子网络，靠门控网络每次只激活少数专家，从而在参数量巨大时保持较低的计算量。

【核心思想】
· 每个 MoE 层有 N 个专家（通常是 FFN）；
· 门控（Router）根据 token 选择 top-k 个专家（如 top-2）；
· 只有被选中的专家参与计算，其余「闲置」。

【为什么省计算】
· 参数量 = N 个专家的总和（巨大），但每个 token 只激活 k 个（少量）；
· 计算量与「激活的参数量」成正比，而非「总参数量」。

【代表】Mixtral 8x7B、GPT-4（传闻）、DeepSeek 系列、GShard。

【面试追问】MoE 的挑战？→ 负载均衡（某些专家过载）、训练不稳定、显存大（所有专家都要加载）、推理时专家路由开销。

【避坑】「8x7B」不是 56B 计算量，而是 8 个专家、每次激活 2 个，实际计算接近 13B；理解「总参数 vs 激活参数」是面试关键。`,
      source: 'MoE 八股 / 面试高频'
    },
    {
      id: 'ai-084', category: 'ai', tags: ['大模型', '安全'], difficulty: 2,
      question: '大模型有哪些安全风险？如何做对齐与安全防护？',
      answer: `一句话结论：大模型风险包括有害内容、幻觉误导、隐私泄露、越狱攻击、提示注入等；靠对齐训练 + 输入输出过滤 + 权限隔离等多层防护。

【主要风险】
1. 有害/违规内容生成；
2. 幻觉：生成看似真实的错误信息；
3. 隐私：训练数据或用户输入泄露；
4. 越狱（Jailbreak）：绕过安全限制；
5. 提示注入（Prompt Injection）：外部内容劫持模型行为。

【防护措施】
1. 对齐训练：RLHF/DPO 让模型符合人类价值观；
2. 安全过滤：输入/输出内容审核（关键词、分类器）；
3. 权限隔离：工具调用的最小权限原则；
4. 提示注入防御：区分「指令」与「数据」、加固系统提示；
5. 隐私保护：数据脱敏、差分隐私、本地化部署。

【面试追问】提示注入和越狱的区别？→ 越狱是诱导模型突破安全限制，提示注入是让模型把「外部恶意内容」当指令执行。

【避坑】安全是「多层纵深防御」，没有单一银弹；LLM 应用上线前要做红队测试。`,
      source: '大模型安全八股 / 面试高频'
    }
  ];

  global.App = global.App || {};
  global.App.aiBank3 = AI3;
})(window);

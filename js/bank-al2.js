/**
 * OfferAgent · 扩充题库（算法 · 第二批：al-021 ~ al-100）
 * 挂载到 global.App.al2Bank，由 bank.js 加载时合并。
 * 答案采用「一句话结论 + 完整解答」范式，对标 LeetCode 高频。
 */
(function (global) {
  'use strict';

  var AL2 = [
    {
      id: 'al-021', category: 'algorithm', tags: ['排序', '归并'], difficulty: 2,
      question: '归并排序的实现思路和复杂度？如何用它求逆序对？',
      answer: `一句话结论：归并排序是「分治 + 合并有序数组」，O(nlogn) 且稳定；合并时统计「左半比右半大」的对数即为逆序对。

【思路】递归拆分到单个元素，再两两合并成有序数组。合并时用双指针，依次取较小的放入临时数组。

【复杂度】时间 O(nlogn)，空间 O(n)（临时数组）。

【求逆序对】在合并左右两个有序子数组时，若左指针元素 > 右指针元素，则左半剩余的所有元素都与该右元素构成逆序对，累加 mid-i+1。

【代码要点】核心是 merge 函数中 l[i] > r[j] 时 count += mid - i + 1。

【面试追问】归并 vs 快排？→ 归并稳定、需额外空间；快排原地、不稳定、最坏 O(n²)。`,
      source: 'LeetCode 剑指 Offer 51'
    },
    {
      id: 'al-022', category: 'algorithm', tags: ['排序', '快排'], difficulty: 2,
      question: '快速排序的实现思路？如何优化避免最坏情况？',
      answer: `一句话结论：快排是「选基准 → 分区 → 递归」，平均 O(nlogn)；通过随机选基准/三数取中避免有序数组退化成 O(n²)。

【思路】选 pivot，把小于它的放左边、大于的放右边，再递归排序左右。

【分区】双指针：i 指向比 pivot 小的区域末尾，遍历遇到小的就 swap 到 i++，最后 pivot 归位。

【优化】
1. 随机选 pivot 或三数取中，避免最坏情况；
2. 小数组改用插入排序（常数更小）；
3. 三路快排处理大量重复元素。

【复杂度】平均 O(nlogn)，最坏 O(n²)，空间 O(logn)（递归栈）。

【面试追问】为什么快排不稳定？→ 分区时相等元素的相对顺序可能被交换打乱。`,
      source: 'LeetCode / 剑指 Offer'
    },
    {
      id: 'al-023', category: 'algorithm', tags: ['排序', '堆'], difficulty: 2,
      question: '堆排序的实现思路和复杂度？',
      answer: `一句话结论：堆排序用「建大顶堆 + 反复取堆顶交换到末尾」，O(nlogn) 原地、不稳定。

【思路】
1. 建堆：从最后一个非叶子节点开始向下调整（heapify），O(n)；
2. 排序：把堆顶（最大）与末尾交换，堆大小减一，再调整堆顶，重复直到有序。

【向下调整】比较父节点与左右子节点，与较大者交换，递归/迭代下沉。

【复杂度】建堆 O(n)，排序 O(nlogn)，总 O(nlogn)，空间 O(1)。

【面试追问】为什么建堆是 O(n) 不是 O(nlogn)？→ 从下往上调整，越靠下节点越多但调整层数越少，级数求和为 O(n)。堆排序不稳定为什么？→ 交换会打乱相等元素顺序。`,
      source: 'LeetCode / 排序'
    },
    {
      id: 'al-024', category: 'algorithm', tags: ['排序'], difficulty: 2,
      question: '计数排序、桶排序、基数排序的区别和适用场景？',
      answer: `一句话结论：三者都是「非比较排序」，利用数据分布特性做到 O(n)，但适用受限；计数按值范围、桶按区间、基数按位。

【计数排序】统计每个值出现次数，累加确定位置；要求值范围小（如成绩 0~100），O(n+k)。

【桶排序】把数据分到多个桶，桶内排序后拼接；要求数据分布均匀，O(n)。

【基数排序】按个位、十位…逐位排序（配合稳定计数排序）；适合整数/定长字符串，O(d(n+k))。

【共同点】不依赖比较，突破比较排序 O(nlogn) 下界，但都需要数据有范围/结构。

【面试追问】为什么比较排序下界是 O(nlogn)？→ n 个元素 n! 种排列，比较树高度至少 log(n!) ≈ nlogn。`,
      source: '算法八股文'
    },
    {
      id: 'al-025', category: 'algorithm', tags: ['二分查找'], difficulty: 2,
      question: '二分查找如何找目标值的左右边界？',
      answer: `一句话结论：找左边界用「收缩 right」的模板，找右边界用「收缩 left」的模板，关键是循环条件与边界更新的配合。

【找左边界】while(left<right)，mid 偏左，nums[mid]<target 时 left=mid+1，否则 right=mid；结束后 left 即第一个 >= target 的位置。

【找右边界】mid 偏右，nums[mid]<=target 时 left=mid，否则 right=mid-1；结束 left 即最后一个 <= target 的位置。

【通用记忆】
· 左边界：找第一个 >= target；
· 右边界：找最后一个 <= target。

【避坑】注意 mid 取 floor 还是 ceil 要与区间收缩匹配，否则死循环；边界要处理「找不到」的情况。`,
      source: 'LeetCode 34 在排序数组中查找元素区间'
    },
    {
      id: 'al-026', category: 'algorithm', tags: ['二分查找'], difficulty: 2,
      question: '如何在旋转排序数组中搜索目标值 / 找最小值？',
      answer: `一句话结论：旋转数组「一段有序、一段无序」，二分时判断 mid 落在哪段，再决定往哪边搜，O(logn)。

【搜索目标】判断 nums[mid] 与 nums[left]：若左半有序且 target 在区间内则搜左，否则搜右。

【找最小值】若 nums[mid] > nums[right]，说明最小值在右半（left=mid+1）；否则在左半（right=mid），循环到 left==right。

【关键】旋转数组性质：必有一半是有序的；利用有序半段快速排除。

【避坑】有重复元素时（如 [3,3,1,3]）需在相等时 right-- 退化为 O(n)。`,
      source: 'LeetCode 33 / 153'
    },
    {
      id: 'al-027', category: 'algorithm', tags: ['二分查找', '矩阵'], difficulty: 2,
      question: '搜索二维矩阵（每行每列都递增）如何高效查找？',
      answer: `一句话结论：从「右上角」或「左下角」出发，利用行列都递增的性质，每次排除一行或一列，O(m+n)。

【思路】从右上角开始：若当前值 > target，说明该列都大于 target，列左移；若 < target，说明该行都小于 target，行下移；相等则找到。

【为什么不用二分】每行二分是 O(mlogn)；右上角法是 O(m+n)；若矩阵按行整体有序（每行首元素 > 上一行末尾）则可整体二分 O(log(mn))。

【避坑】区分两种矩阵：①每行每列递增 → 右上角法；②严格展平有序 → 整体二分。`,
      source: 'LeetCode 240 搜索二维矩阵 II'
    },
    {
      id: 'al-028', category: 'algorithm', tags: ['链表'], difficulty: 1,
      question: '如何合并两个有序链表？合并 K 个有序链表呢？',
      answer: `一句话结论：合并两个有序链表用双指针逐个比较拼接；合并 K 个用「两两归并」或「优先队列（最小堆）」。

【合并两个】比较两个链表头，取小的接到结果，指针后移，直到一个为空，接上剩余。

【合并 K 个】
1. 两两归并：分治，每次合并一半，O(n logK)；
2. 最小堆：把 K 个头节点入堆，每次弹出最小接上，再入其下一个，O(n logK)。

【代码要点】用 dummy 头节点简化边界。

【面试追问】K 个链表总节点 n，复杂度？→ 两两归并 O(nlogK)，堆 O(nlogK)，空间堆 O(K)。`,
      source: 'LeetCode 21 / 23'
    },
    {
      id: 'al-029', category: 'algorithm', tags: ['链表', '双指针'], difficulty: 2,
      question: '如何删除链表的倒数第 N 个节点？（只遍历一次）',
      answer: `一句话结论：用快慢双指针，快指针先走 N 步，再一起走，快指针到末尾时慢指针正好在待删节点的前驱。

【思路】
1. dummy 节点指向 head；
2. fast 先走 N+1 步；
3. slow、fast 一起走，fast 到 null 时 slow 指向待删前驱；
4. slow.next = slow.next.next 删除。

【为什么用 dummy】处理删除头节点等边界情况。

【复杂度】O(n) 时间，O(1) 空间。

【面试追问】如何找链表中点？→ 快慢指针，fast 走两步 slow 走一步。如何判断回文链表？→ 找中点 + 反转后半段 + 比较。`,
      source: 'LeetCode 19'
    },
    {
      id: 'al-030', category: 'algorithm', tags: ['链表', '双指针'], difficulty: 1,
      question: '如何找链表的中间节点？',
      answer: `一句话结论：快慢指针，fast 每次走两步、slow 走一步，fast 到末尾时 slow 即中点。

【思路】slow=head，fast=head；while(fast && fast.next){ slow=slow.next; fast=fast.next.next; }。

【偶数个节点】两种中点：若 fast 初始 = head，返回的是「第二个中点」；若 fast = head.next，返回「第一个中点」。

【应用】归并排序链表、判断回文链表、重排链表。

【复杂度】O(n) 时间，O(1) 空间。`,
      source: 'LeetCode 876'
    },
    {
      id: 'al-031', category: 'algorithm', tags: ['链表', '双指针'], difficulty: 1,
      question: '如何判断两个链表是否相交？交点在哪？',
      answer: `一句话结论：两链表相交则「尾节点相同」；找交点用双指针各自走完自己再走对方，相遇点即交点。

【判断相交】分别走到各自末尾，比较尾节点引用是否相同。

【找交点】pa 从 A 头、pb 从 B 头出发，各自走到末尾后切换到另一链表头继续，若相交会在交点相遇（走过的总路程相等），不相交则同时到 null。

【原理】pa 走 A+B，pb 走 B+A，长度相同，若相交必在交点首次相遇。

【复杂度】O(m+n) 时间，O(1) 空间。

【面试追问】为什么不用哈希表？→ 哈希 O(m+n) 但 O(m) 空间，双指针更优。`,
      source: 'LeetCode 160'
    },
    {
      id: 'al-032', category: 'algorithm', tags: ['链表', '双指针'], difficulty: 2,
      question: '如何判断一个链表是不是回文链表？',
      answer: `一句话结论：找中点 → 反转后半段 → 前后比较，O(n) 时间 O(1) 空间。

【思路】
1. 快慢指针找中点；
2. 反转后半段链表；
3. 从两头向中间比较值；
4. （可选）恢复链表。

【代码要点】反转链表用三个指针 prev/cur/next 迭代。

【复杂度】O(n) 时间，O(1) 空间。

【面试追问】能否用栈？→ 可以，遍历压栈再比较，但 O(n) 空间；快慢指针+反转更优。`,
      source: 'LeetCode 234'
    },
    {
      id: 'al-033', category: 'algorithm', tags: ['链表', '哈希'], difficulty: 2,
      question: '如何复制带随机指针的链表？',
      answer: `一句话结论：用哈希表记录「原节点 → 新节点」映射，两次遍历完成复制；进阶可用「原节点后接新节点」做到 O(1) 空间。

【哈希法】
1. 第一次遍历：为每个原节点创建新节点，存 map；
2. 第二次遍历：连接 next 和 random（map.get(原.random)）。

【O(1) 空间法】把新节点插在原节点后面，再拆分两条链表：
1. 每个原节点后插入副本；
2. 副本.random = 原.random.next；
3. 拆分恢复。

【复杂度】哈希法 O(n) 时间 O(n) 空间；穿插法 O(n) 时间 O(1) 空间。

【避坑】random 可能为 null，要判空。`,
      source: 'LeetCode 138'
    },
    {
      id: 'al-034', category: 'algorithm', tags: ['二叉树', '递归'], difficulty: 1,
      question: '如何求二叉树的最大深度和最小深度？',
      answer: `一句话结论：最大深度 = max(左右子树深度)+1；最小深度注意「单侧为空」时不能直接取 min。

【最大深度】递归：1 + max(maxDepth(left), maxDepth(right))；也可层序遍历数层数。

【最小深度】根到最近叶子节点的距离：若一侧为空，则最小深度 = 另一侧深度+1；两侧都非空才取 min。

【代码要点】minDepth 的坑在于 [1,null,2] 这类斜树，最小深度是 2 不是 1。

【复杂度】O(n) 时间，O(h) 空间（递归栈）。

【面试追问】用 BFS 求最小深度？→ 层序遍历，遇到第一个叶子节点返回当前层数，天然早停。`,
      source: 'LeetCode 104 / 111'
    },
    {
      id: 'al-035', category: 'algorithm', tags: ['二叉树', 'BFS'], difficulty: 2,
      question: '二叉树的层序遍历如何实现？之字形（锯齿形）遍历呢？',
      answer: `一句话结论：层序遍历用队列 BFS 逐层处理；之字形在每层根据层号决定正序还是逆序。

【层序遍历】队列存节点，每次先记录当前层大小 size，弹出 size 个节点即为一层，同时把子节点入队。

【之字形】用双端队列或按层号 flag：偶数层正序（队尾取、队头插子），奇数层逆序（队头取、队尾插子）。

【代码要点】核心是「按层分组」，用 size 控制一层。

【复杂度】O(n) 时间，O(n) 空间。

【面试追问】层序 vs 前中后序？→ 层序是 BFS（横向），前中后序是 DFS（纵向）。`,
      source: 'LeetCode 102 / 103'
    },
    {
      id: 'al-036', category: 'algorithm', tags: ['二叉树', '栈'], difficulty: 2,
      question: '二叉树前序、中序、后序遍历的迭代写法（不用递归）？',
      answer: `一句话结论：前序和中序用栈模拟「一路向左」；后序可由「前序的镜像」反转得到，或用「双栈/标记法」。

【前序】栈先压右再压左（或一路向左并压右孩子），弹出即访问。

【中序】一路把左孩子压栈，弹出一个访问，再转向右子树。

【后序】前序是「根左右」，改成「根右左」再反转结果即「左右根」；或用标记法（节点第二次弹出才访问）。

【通用模板】用「颜色标记法」：节点入栈标记 visited=false，弹出时若未访问则按序压入自己和子节点。

【复杂度】O(n) 时间，O(h) 空间。`,
      source: 'LeetCode 144 / 94 / 145'
    },
    {
      id: 'al-037', category: 'algorithm', tags: ['二叉搜索树'], difficulty: 2,
      question: '如何验证一棵树是二叉搜索树（BST）？',
      answer: `一句话结论：验证 BST 关键是「中序遍历严格递增」或「递归传上下界」，不能只判断左<根<右。

【方法一：中序遍历】BST 中序是递增序列，遍历时记录前一个值，比较是否严格递增。

【方法二：递归上下界】每个节点值必须在 (min, max) 内，向左更新 max=root.val，向右更新 min=root.val。

【常见错误】只判断 root.left < root < root.right，忽略了「左子树所有节点都要小于根」（如 [5,1,4,null,null,3,6] 中 3 < 5 但 3 在右子树，错误）。

【复杂度】O(n) 时间，O(h) 空间。`,
      source: 'LeetCode 98'
    },
    {
      id: 'al-038', category: 'algorithm', tags: ['二叉搜索树'], difficulty: 2,
      question: '如何找二叉搜索树中第 K 小的元素？',
      answer: `一句话结论：利用 BST 中序遍历有序的性质，中序遍历到第 K 个节点即为答案，O(k)。

【思路】中序遍历（左根右），用一个计数器，遍历到第 K 个元素返回。

【进阶】若频繁查询，可给节点维护「左子树节点数」，类似有序数组二分，每次 O(h)。

【复杂度】中序 O(k) 时间 O(h) 空间；维护计数后 O(h)。

【代码要点】递归中序，k-- 到 0 时记录结果。

【面试追问】第 K 大呢？→ 中序遍历「右根左」，或总数 - K + 1 小。`,
      source: 'LeetCode 230'
    },
    {
      id: 'al-039', category: 'algorithm', tags: ['二叉树', '递归'], difficulty: 2,
      question: '如何求二叉树的最近公共祖先（LCA）？',
      answer: `一句话结论：递归自底向上，若当前节点是 p/q 或左右子树分别包含 p、q，则当前节点即 LCA。

【思路】
1. 递归遍历，遇到 null 或 p、q 就返回；
2. 若左右子树都返回非空，说明 p、q 分居两侧，当前节点即 LCA；
3. 若只有一侧非空，则 p、q 都在那一侧，返回该侧结果。

【复杂度】O(n) 时间，O(h) 空间。

【变体】BST 的 LCA：利用大小关系，若 p、q 都小于根则去左，都大于则去右，分叉即答案，O(h)。

【面试追问】多个节点（>2）的 LCA？→ 递归返回「包含几个目标」，第一个包含全部目标的节点即 LCA。`,
      source: 'LeetCode 236'
    },
    {
      id: 'al-040', category: 'algorithm', tags: ['二叉树', '递归'], difficulty: 1,
      question: '如何判断一棵二叉树是否平衡（AVL）？',
      answer: `一句话结论：自底向上递归，返回子树高度，同时判断左右高度差是否 > 1，不平衡则提前返回 -1。

【思路】递归函数返回「高度」，若左右子树高度差 > 1 返回 -1 表示不平衡；任一子树返回 -1 则整棵不平衡。

【代码要点】return Math.max(l, r) + 1，但若 l 或 r 为 -1 直接返回 -1。

【复杂度】O(n) 时间，O(h) 空间（自底向上避免重复计算高度）。

【面试追问】为什么自顶向下算高度是 O(n²)？→ 每个节点都重复遍历子树算高度。`,
      source: 'LeetCode 110'
    },
    {
      id: 'al-041', category: 'algorithm', tags: ['二叉树', '递归'], difficulty: 1,
      question: '如何翻转（镜像）一棵二叉树？',
      answer: `一句话结论：递归交换每个节点的左右子树，自底向上或自顶向下皆可。

【思路】递归：交换当前节点的左右孩子，再递归翻转左右子树。

【代码要点】temp = root.left; root.left = invert(root.right); root.right = invert(temp)。

【复杂度】O(n) 时间，O(h) 空间。

【应用】镜像问题、对称问题的基础。

【面试追问】迭代实现？→ 层序遍历/栈，交换每个节点的左右孩子。`,
      source: 'LeetCode 226'
    },
    {
      id: 'al-042', category: 'algorithm', tags: ['二叉树', '递归'], difficulty: 1,
      question: '如何判断一棵二叉树是否对称（镜像对称）？',
      answer: `一句话结论：递归比较「左子树的左」与「右子树的右」、「左子树的右」与「右子树的左」，值都相等则对称。

【思路】定义函数判断两节点是否镜像：都为 null 返回 true；一个 null 返回 false；值不等 false；否则递归比较 (l.left, r.right) 和 (l.right, r.left)。

【复杂度】O(n) 时间，O(h) 空间。

【面试追问】与「翻转后相等」的关系？→ 对称 ⇔ 树等于其镜像。迭代可用队列成对入队比较。`,
      source: 'LeetCode 101'
    },
    {
      id: 'al-043', category: 'algorithm', tags: ['二叉树', '递归'], difficulty: 1,
      question: '路径总和问题：判断是否存在根到叶子路径和等于 target？',
      answer: `一句话结论：递归自顶向下累减 target，到叶子节点判断剩余值是否为 0。

【思路】递归传入「剩余值 = target - 当前值」，到叶子节点（无左右孩子）判断剩余值是否等于 0。

【代码要点】只在叶子节点判断，不能在中途节点判断（路径必须到叶子）。

【变体】
· 求所有路径：回溯收集路径列表；
· 路径总和 III：任意起点（前缀和 + 哈希）。

【复杂度】O(n) 时间，O(h) 空间。`,
      source: 'LeetCode 112 / 113'
    },
    {
      id: 'al-044', category: 'algorithm', tags: ['图', 'DFS/BFS'], difficulty: 2,
      question: '如何求岛屿数量（网格中连通的 1）？',
      answer: `一句话结论：遍历网格，遇到 1 就 DFS/BFS 把整块相连的 1 标记为已访问（或置 0），计数加一。

【思路】每个未访问的 1 是岛屿起点，向上下左右四个方向扩散，把连通的 1 全部标记，避免重复计数。

【实现】DFS（递归）或 BFS（队列）；也可用并查集。

【复杂度】O(m×n) 时间，O(m×n) 空间（最坏递归栈/队列）。

【代码要点】边界判断 + visited（或原地置 0 标记）。

【面试追问】最大岛屿面积？→ 统计每个连通块大小取最大。岛屿周长？→ 数每个 1 的边界。`,
      source: 'LeetCode 200'
    },
    {
      id: 'al-045', category: 'algorithm', tags: ['BFS', '多源'], difficulty: 2,
      question: '腐烂的橘子：如何求所有橘子腐烂需要的最短时间？（多源 BFS）',
      answer: `一句话结论：多源 BFS——把所有初始腐烂的橘子同时入队，逐层扩散，层数即所需分钟数。

【思路】
1. 把所有腐烂橘子入队（多源），记录新鲜橘子数；
2. 每轮 BFS 让腐烂橘子向四周扩散，新鲜橘子变腐烂并减计数；
3. 直到队列空；若新鲜橘子数 > 0 返回 -1（有橘子永远不腐烂）。

【为什么多源 BFS】等价于加一个虚拟源点连接所有初始腐烂橘子，求最短路。

【复杂度】O(m×n) 时间，O(m×n) 空间。

【面试追问】单源 vs 多源 BFS？→ 多源初始队列一次性塞入所有起点，BFS 天然求最短距离。`,
      source: 'LeetCode 994'
    },
    {
      id: 'al-046', category: 'algorithm', tags: ['图', '拓扑排序'], difficulty: 2,
      question: '课程表问题如何用拓扑排序判断是否有环？',
      answer: `一句话结论：构建依赖图，用「入度 + BFS/DFS」做拓扑排序，若访问节点数 == 课程数则无环。

【BFS 法（Kahn）】
1. 统计每个节点入度，构建邻接表；
2. 入度为 0 的入队；
3. 出队时访问，把其邻居入度减一，为 0 则入队；
4. 若访问数 < 总节点数，说明有环。

【DFS 法】三色标记（未访问/访问中/已访问），遇到「访问中」的节点说明有环。

【复杂度】O(V+E) 时间，O(V+E) 空间。

【面试追问】拓扑排序的典型应用？→ 依赖解析、编译顺序、任务调度。`,
      source: 'LeetCode 207 / 210'
    },
    {
      id: 'al-047', category: 'algorithm', tags: ['图', 'DFS/BFS'], difficulty: 2,
      question: '如何克隆一个图（深拷贝）？',
      answer: `一句话结论：用哈希表记录「原节点 → 克隆节点」，DFS/BFS 遍历图，避免重复克隆与死循环。

【DFS 法】
1. map 存已克隆节点；
2. 遍历邻居，若已克隆则复用，否则递归克隆邻居。

【BFS 法】队列遍历，同样用 map 去重。

【复杂度】O(V+E) 时间，O(V) 空间。

【代码要点】处理无向图，克隆节点后先放入 map 再遍历邻居，防止环导致无限递归。

【面试追问】为什么必须用 map？→ 图可能有环和共享节点，map 保证每个节点只克隆一次。`,
      source: 'LeetCode 133'
    },
    {
      id: 'al-048', category: 'algorithm', tags: ['BFS', '图'], difficulty: 2,
      question: '单词接龙：如何求从 beginWord 到 endWord 的最短转换序列长度？',
      answer: `一句话结论：把每个单词当节点、差一个字母的单词连边，BFS 求最短路径；用「通配符映射」建图避免 O(n²) 枚举。

【建图优化】对每个单词，把每个位置换成通配符（如 hot → *ot, h*t, ho*），用 map 按通配符分组，组内单词互为相邻。

【BFS】从 beginWord 层序遍历，访问过的单词标记，直到遇到 endWord 返回层数。

【复杂度】O(n×L²) 建图，BFS O(n×L)。

【面试追问】为什么用 BFS 不用 DFS？→ BFS 首次到达即最短路径。`,
      source: 'LeetCode 127'
    },
    {
      id: 'al-049', category: 'algorithm', tags: ['图', '最小生成树'], difficulty: 2,
      question: '最小生成树（MST）的 Prim 和 Kruskal 算法有什么区别？',
      answer: `一句话结论：Prim 从点出发，每次选「连接已选集合的最小边」；Kruskal 从边出发，按权重升序选「不构成环的边」，二者都贪心。

【Prim】维护已加入集合，每次选连接集合内外的最小权重边，把新点加入；用优先队列优化 O(ElogV)，适合稠密图。

【Kruskal】所有边按权重排序，用并查集判断是否成环，不成环则加入；O(ElogE)，适合稀疏图。

【共同点】贪心策略，结果总权重相同（MST 唯一时）。

【面试追问】如何判断成环？→ 并查集 find 判断两端是否已在同一集合。

【应用】网络布线、道路建设最小成本。`,
      source: 'LeetCode / 图论'
    },
    {
      id: 'al-050', category: 'algorithm', tags: ['图', '最短路径'], difficulty: 2,
      question: 'Dijkstra、Floyd、Bellman-Ford 三种最短路径算法有什么区别？',
      answer: `一句话结论：Dijkstra 求单源、非负权；Bellman-Ford 求单源、可处理负权边；Floyd 求全源、动态规划。

【Dijkstra】贪心 + 优先队列，每次选距离最小且未确定的点松弛，O((V+E)logV)，不能有负权。

【Bellman-Ford】对所有边松弛 V-1 轮，可检测负环，O(VE)。

【Floyd】三重循环动态规划，求任意两点最短路径，O(V³)，可处理负权但无负环。

【选型】单源非负 → Dijkstra；有负权 → Bellman-Ford/SPFA；全源 → Floyd。

【面试追问】Dijkstra 为什么不能负权？→ 贪心前提「已确定的最短不会再被更新」被负权破坏。`,
      source: '算法八股文'
    },
    {
      id: 'al-051', category: 'algorithm', tags: ['并查集', '图'], difficulty: 2,
      question: '并查集（Union-Find）的原理？路径压缩和按秩合并是什么？',
      answer: `一句话结论：并查集维护「元素属于哪个集合」，支持近乎 O(1) 的合并与查询；路径压缩 + 按秩合并优化到近似常数。

【操作】
· find(x)：找 x 所在集合代表（根）；
· union(x,y)：合并两集合。

【路径压缩】find 时把沿途节点直接指向根，摊薄后续查找。

【按秩合并】合并时把小树挂到大树下，控制树高。

【复杂度】两者结合后近似 O(α(n))（反阿克曼函数，接近常数）。

【应用】连通性判断、最小生成树判环、朋友圈/岛屿、动态连通。

【面试追问】如何统计集合数量？→ 维护 count，每次有效合并减一。`,
      source: 'LeetCode 547 朋友圈 / 图论'
    },
    {
      id: 'al-052', category: 'algorithm', tags: ['动态规划'], difficulty: 2,
      question: '最长上升子序列（LIS）如何求解？',
      answer: `一句话结论：DP 定义 dp[i] = 以 i 结尾的最长上升子序列长度，O(n²)；贪心 + 二分可优化到 O(nlogn)。

【DP 法】dp[i] = max(dp[j]+1)，j < i 且 nums[j] < nums[i]，答案取 max(dp)。

【贪心 + 二分】维护 tails 数组，tails[i] 表示长度 i+1 的子序列的最小末尾值；遍历时二分找到可替换位置，维护 tails 单调递增。

【复杂度】DP O(n²)；贪心+二分 O(nlogn)。

【面试追问】如何输出具体序列？→ DP 记录前驱回溯；LIS 与 LCS 关系？→ 排序后求 LCS 可解 LIS（元素可重复时）。`,
      source: 'LeetCode 300'
    },
    {
      id: 'al-053', category: 'algorithm', tags: ['动态规划'], difficulty: 2,
      question: '最长公共子序列（LCS）如何求解？',
      answer: `一句话结论：dp[i][j] 表示 text1 前 i 个与 text2 前 j 个的 LCS 长度，字符相等则 dp[i][j]=dp[i-1][j-1]+1，否则取 max。

【状态转移】
· 相等：dp[i][j] = dp[i-1][j-1] + 1；
· 不等：dp[i][j] = max(dp[i-1][j], dp[i][j-1])。

【复杂度】O(mn) 时间，O(mn) 空间（可滚动数组优化到 O(n)）。

【区别子串】子序列可不连续，子串必须连续（子串用 dp[i][j] 记录以 i、j 结尾的最长公共后缀）。

【面试追问】如何输出 LCS 内容？→ 从 dp[m][n] 反推，相等则记录字符斜向上，否则向大的方向回溯。`,
      source: 'LeetCode 1143'
    },
    {
      id: 'al-054', category: 'algorithm', tags: ['动态规划', '双指针'], difficulty: 2,
      question: '最长回文子串如何求解？',
      answer: `一句话结论：中心扩展法——枚举每个中心（单字符或双字符）向两边扩展，O(n²)；或 DP 维护 dp[i][j] 表示区间是否回文。

【中心扩展】每个位置作为中心，向两边扩展直到不相等，记录最长回文。共 2n-1 个中心。

【DP 法】dp[i][j] = (s[i]==s[j]) && (j-i<2 || dp[i+1][j-1])，从短区间往长区间递推。

【复杂度】中心扩展 O(n²) 时间 O(1) 空间；DP O(n²) 时间 O(n²) 空间。

【进阶】Manacher 算法 O(n)，面试了解即可。

【面试追问】回文子串的数量？→ 中心扩展计数。`,
      source: 'LeetCode 5'
    },
    {
      id: 'al-055', category: 'algorithm', tags: ['动态规划'], difficulty: 3,
      question: '编辑距离（两个字符串的最少操作次数）如何求解？',
      answer: `一句话结论：dp[i][j] 表示 word1 前 i 个转成 word2 前 j 个的最少操作；相等则继承对角，不等则取「增、删、改」最小 +1。

【状态转移】
· 相等：dp[i][j] = dp[i-1][j-1]；
· 不等：dp[i][j] = 1 + min(dp[i-1][j](删), dp[i][j-1](增), dp[i-1][j-1](改))。

【初始化】dp[i][0]=i（删除），dp[0][j]=j（插入）。

【复杂度】O(mn) 时间，O(mn) 空间。

【应用】拼写纠错、DNA 相似度、模糊匹配。

【面试追问】三种操作分别对应哪个转移？→ 增=dp[i][j-1]，删=dp[i-1][j]，改=dp[i-1][j-1]。`,
      source: 'LeetCode 72'
    },
    {
      id: 'al-056', category: 'algorithm', tags: ['动态规划', '背包'], difficulty: 2,
      question: '01 背包和完全背包问题有什么区别？如何求解？',
      answer: `一句话结论：01 背包每个物品最多选一次，完全背包可无限选；都用 dp[j] 表示容量 j 的最大价值，区别在遍历顺序。

【01 背包】dp[j] = max(dp[j], dp[j-w[i]]+v[i])，**倒序**遍历容量（保证每个物品只用一次）。

【完全背包】同样转移，但**正序**遍历容量（允许重复使用）。

【为什么】01 背包倒序避免「当前物品被多次使用」；完全背包正序恰好允许多次。

【复杂度】O(n×W) 时间，O(W) 空间。

【面试追问】如何求装满的方案数？→ dp[j] += dp[j-w]，初始化 dp[0]=1。`,
      source: 'LeetCode 322 零钱兑换 / 背包'
    },
    {
      id: 'al-057', category: 'algorithm', tags: ['动态规划'], difficulty: 2,
      question: '打家劫舍系列问题如何求解？',
      answer: `一句话结论：核心是「不能偷相邻」，dp[i] = max(dp[i-1], dp[i-2]+nums[i])；环形和树形是变体。

【基础版】dp[i] 表示前 i 家的最大金额：要么不偷第 i 家（dp[i-1]），要么偷第 i 家（dp[i-2]+nums[i]）。可滚动优化到 O(1) 空间。

【环形版（首尾相连）】拆成「偷 0~n-2」和「偷 1~n-1」两种情况取最大。

【树形版】每个节点返回 [偷, 不偷] 两个状态：偷当前则子节点不能偷，不偷则子节点可取 max。

【复杂度】O(n) 时间。

【面试追问】状态定义为什么是「前 i 家」而不是「以 i 结尾」？→ 因为可跳过相邻，用前缀最值更自然。`,
      source: 'LeetCode 198 / 213 / 337'
    },
    {
      id: 'al-058', category: 'algorithm', tags: ['动态规划'], difficulty: 3,
      question: '买卖股票的最佳时机系列如何求解？',
      answer: `一句话结论：用「状态机 DP」——每天有「持有/未持有」两个状态（含交易次数维度），逐天转移求最大利润。

【一次交易】遍历维护历史最低价，max(price - minPrice)。

【多次交易】累加所有上涨区间（贪心），或 dp 持有/不持有状态转移。

【最多 K 次】dp[i][k][0/1]：0 不持有、1 持有，k 次交易；转移 buy 则 k+1。

【含冷冻期/手续费】在状态转移中加入冷冻期约束或卖出扣手续费。

【复杂度】O(n) 或 O(nk)。

【面试追问】状态转移方程核心？→ 持有 = max(持有, 不持有-价格)，不持有 = max(不持有, 持有+价格)。`,
      source: 'LeetCode 121 / 122 / 123 / 188'
    },
    {
      id: 'al-059', category: 'algorithm', tags: ['动态规划'], difficulty: 1,
      question: '爬楼梯问题如何求解？与斐波那契有什么关系？',
      answer: `一句话结论：爬楼梯 f(n) = f(n-1) + f(n-2)，本质是斐波那契数列，用滚动变量 O(1) 空间。

【思路】到第 n 阶，最后一步可能从 n-1 迈 1 步或 n-2 迈 2 步，故 f(n)=f(n-1)+f(n-2)。

【优化】只用两个变量滚动，避免 O(n) 空间；进阶可用矩阵快速幂 O(logn)。

【变体】每次 1/2/3 步 → f(n)=f(n-1)+f(n-2)+f(n-3)；有障碍 → 跳过错位。

【面试追问】为什么用 DP 不用递归？→ 朴素递归指数级（重复计算），记忆化/迭代才是线性。`,
      source: 'LeetCode 70'
    },
    {
      id: 'al-060', category: 'algorithm', tags: ['动态规划', '背包'], difficulty: 2,
      question: '零钱兑换：最少硬币数凑成 amount，如何求解？',
      answer: `一句话结论：完全背包求最小值——dp[j] = min(dp[j], dp[j-coin]+1)，dp[0]=0，其余初始为无穷大。

【状态】dp[j] 表示凑成金额 j 的最少硬币数。

【转移】遍历每种硬币，正序更新：dp[j] = min(dp[j], dp[j-coin]+1)。

【结果】dp[amount] 为无穷大则返回 -1。

【复杂度】O(n×amount) 时间，O(amount) 空间。

【变体】组合数（凑成的方案数）：dp[j] += dp[j-coin]，dp[0]=1，硬币外层循环。

【面试追问】最少硬币数 vs 组合数遍历顺序有何不同？→ 求最少数正序即可；求组合数要注意「组合」与「排列」的区别。`,
      source: 'LeetCode 322'
    },
    {
      id: 'al-061', category: 'algorithm', tags: ['贪心', '数组'], difficulty: 2,
      question: '跳跃游戏：判断能否跳到终点？最少跳几步？',
      answer: `一句话结论：维护「当前能到达的最远位置」，贪心向右扩展；最少步数则记录「当前步数覆盖的边界」。

【能否到达】遍历时更新 farthest = max(farthest, i+nums[i])，若 i > farthest 说明无法前进返回 false。

【最少步数】用 end 记录当前步数能到达的边界，超过 end 则步数+1 并更新 end 为当前 farthest。

【复杂度】O(n) 时间，O(1) 空间。

【代码要点】能否到达：farthest 始终 >= i；最少步数：i 到达 end 时 jump++。

【面试追问】为什么是贪心？→ 每一步都选能扩展最远的，局部最优导致全局最优。`,
      source: 'LeetCode 55 / 45'
    },
    {
      id: 'al-062', category: 'algorithm', tags: ['排序', '区间'], difficulty: 2,
      question: '合并区间：如何合并所有重叠的区间？',
      answer: `一句话结论：按区间起点排序，遍历时若当前区间与结果末尾重叠则合并（取右端最大值），否则加入结果。

【思路】
1. 按 start 升序排序；
2. 遍历，若 intervals[i].start <= 结果末尾.end，则合并（end = max(end, intervals[i].end)）；
3. 否则作为新区间加入。

【复杂度】O(nlogn) 时间（排序），O(n) 空间。

【代码要点】重叠判断用「当前起点 <= 上一个终点」。

【面试追问】插入新区间并合并？→ 先插入再走合并区间流程，或找重叠段合并。会议室问题？→ 用最小堆维护结束时间。`,
      source: 'LeetCode 56'
    },
    {
      id: 'al-063', category: 'algorithm', tags: ['滑动窗口', '双指针'], difficulty: 2,
      question: '无重复字符的最长子串如何求解？',
      answer: `一句话结论：滑动窗口 + 哈希表，右指针扩展、左指针收缩保证窗口内无重复，维护最大长度。

【思路】right 逐个加入，用哈希表记录字符出现位置；遇到重复字符，把 left 移到「重复字符上次位置 + 1」，保证窗口内无重复。

【复杂度】O(n) 时间，O(字符集) 空间。

【代码要点】left = max(left, map.get(c)+1)，因为重复字符可能在 left 之前。

【面试追问】为什么是滑动窗口？→ 窗口有「单调性」：右扩、左缩，可线性维护。`,
      source: 'LeetCode 3'
    },
    {
      id: 'al-064', category: 'algorithm', tags: ['滑动窗口'], difficulty: 3,
      question: '最小覆盖子串：找包含目标串所有字符的最小子串，如何求解？',
      answer: `一句话结论：滑动窗口 + 计数器，右扩直到覆盖所有目标字符，再左缩求最小，维护满足条件的最短窗口。

【思路】
1. 用 need 记录目标字符需求，window 记录当前窗口；
2. 右扩：加入字符，更新 window，若满足某字符需求则 valid++；
3. 当 valid == 目标字符种类数，尝试左缩求最小，移动 left 更新。

【复杂度】O(n) 时间，O(字符集) 空间。

【代码要点】valid 计数「已满足的种类数」，避免每次全量比较。

【面试追问】如何判断「覆盖」？→ 用 need 计数器，window[c] >= need[c] 则该字符满足。`,
      source: 'LeetCode 76'
    },
    {
      id: 'al-065', category: 'algorithm', tags: ['哈希', '字符串'], difficulty: 2,
      question: '字母异位词分组：如何把字母组成相同的字符串分到一组？',
      answer: `一句话结论：把每个字符串排序后作为 key，或用「字符计数」编码作为 key，哈希分组。

【方法一：排序 key】每个字符串排序，排序结果相同的归一组，O(n·klogk)。

【方法二：计数 key】统计 26 个字母出现次数，编码成「a1b2c0...」作为 key，O(n·k)。

【复杂度】方法二 O(nk) 时间（n 个字符串，k 平均长度）。

【代码要点】用 map 存 key → list，最后输出 values。

【面试追问】判断两个字符串是否异位词？→ 排序比较 或 计数比较。`,
      source: 'LeetCode 49'
    },
    {
      id: 'al-066', category: 'algorithm', tags: ['栈', '字符串'], difficulty: 2,
      question: '字符串解码（如 "3[a2[c]]" → "accaccacc"）如何求解？',
      answer: `一句话结论：用栈处理嵌套，遇到数字/字符/左方括号入栈，遇到右方括号弹出到左方括号之间的字符串，按倍数展开再入栈。

【思路】
1. 遍历：数字解析成倍数，左方括号和字符直接入栈；
2. 遇到右方括号：弹出字符直到左方括号，得到要重复的子串；
3. 再弹出左方括号前的数字倍数，把子串重复后压回栈。

【复杂度】O(n) 时间，O(n) 空间（n 为输出长度）。

【代码要点】数字可能多位，需累计；用 StringBuilder 拼接。

【面试追问】用递归？→ 定义「解析到 index」的递归函数，遇到左方括号递归解析。`,
      source: 'LeetCode 394'
    },
    {
      id: 'al-067', category: 'algorithm', tags: ['栈', '字符串'], difficulty: 1,
      question: '有效的括号：如何判断括号串是否匹配？',
      answer: `一句话结论：用栈，遇到左括号入栈，遇到右括号判断栈顶是否匹配；最后栈空则有效。

【思路】遍历字符串，左括号入栈，右括号与栈顶匹配则弹出，不匹配或栈空则无效；结束栈必须为空。

【复杂度】O(n) 时间，O(n) 空间。

【代码要点】用 map 存右括号到左括号的映射，简化判断。

【变体】最长有效括号 → DP 或栈记录下标；生成有效括号 → 回溯。

【面试追问】只有一种括号？→ 用计数即可；多种括号必须用栈保证嵌套顺序。`,
      source: 'LeetCode 20'
    },
    {
      id: 'al-068', category: 'algorithm', tags: ['栈', '设计'], difficulty: 2,
      question: '最小栈：如何设计一个能在 O(1) 获取最小值的栈？',
      answer: `一句话结论：用「辅助栈」同步记录每个状态的最小值，push 时把当前最小值也压入辅助栈。

【思路】
1. 数据栈存元素；
2. 辅助栈存「当前栈的最小值」，push 时 min = min(新值, 辅助栈顶)，同时入栈；
3. getMin 直接返回辅助栈顶。

【优化】辅助栈只在「新值 <= 当前最小」时压入，pop 时相等才弹出，省空间。

【复杂度】所有操作 O(1)。

【面试追问】如何用栈实现队列？→ 两个栈，入栈 + 出栈，出栈时把入栈倒腾到出栈。`,
      source: 'LeetCode 155'
    },
    {
      id: 'al-069', category: 'algorithm', tags: ['单调栈'], difficulty: 2,
      question: '每日温度：求每个元素后面第一个比它大的元素的距离？',
      answer: `一句话结论：单调递减栈——栈存下标，遇到更大的温度就弹出比它小的栈顶并计算距离。

【思路】遍历数组，维护「单调递减」的栈（存下标）；当前温度 > 栈顶温度时，栈顶的答案就是「当前下标 - 栈顶下标」，弹出；最后栈里剩余的无更大元素，答案为 0。

【复杂度】O(n) 时间（每个元素入栈出栈一次），O(n) 空间。

【代码要点】比较用温度值，存的是下标用于算距离。

【面试追问】单调栈的适用场景？→ 找「下一个更大/更小元素」类问题，把 O(n²) 降到 O(n)。`,
      source: 'LeetCode 739'
    },
    {
      id: 'al-070', category: 'algorithm', tags: ['单调栈'], difficulty: 2,
      question: '下一个更大元素 I/II：如何求每个元素下一个更大的元素？',
      answer: `一句话结论：单调栈求「每个元素右边第一个更大的」，循环数组则遍历两遍（取模）。

【基础版】从右往左维护单调递减栈，弹出比当前小的，栈顶即下一个更大；或用从左往右的「弹栈填答案」法。

【循环数组（II）】数组首尾相连，遍历 2n 次（下标取模），同样单调栈处理。

【复杂度】O(n) 时间，O(n) 空间。

【代码要点】「从右往左」栈顶存候选，弹出小的保证栈单调。

【面试追问】下一个更小元素？→ 改成单调递增栈。`,
      source: 'LeetCode 496 / 503'
    },
    {
      id: 'al-071', category: 'algorithm', tags: ['双指针', '单调栈'], difficulty: 3,
      question: '接雨水：如何计算柱子间能接多少雨水？',
      answer: `一句话结论：双指针法——每个位置能接的雨水 = min(左最高, 右最高) - 自身高度，左右指针向中间收敛。

【双指针法】维护 leftMax、rightMax，哪边矮移动哪边指针，累加 max(0, min(leftMax,rightMax)-height[i])。

【单调栈法】维护递减栈，遇到更高的柱子形成凹槽，计算积水。

【复杂度】双指针 O(n) 时间 O(1) 空间。

【代码要点】核心理解：某列的积水只由「它左右两侧最高的柱子中较矮的那个」决定。

【面试追问】二维接雨水？→ 用最小堆 + BFS（积水从边界向内扩展）。`,
      source: 'LeetCode 42'
    },
    {
      id: 'al-072', category: 'algorithm', tags: ['双指针', '贪心'], difficulty: 2,
      question: '盛最多水的容器：如何求两条线围成的最大面积？',
      answer: `一句话结论：双指针从两端向中间收缩，每次移动较矮的一边，维护最大面积。

【思路】面积 = min(height[l], height[r]) × (r-l)。移动较矮的边才可能让面积变大（移动高的边只会让宽度变小、高度不增）。

【复杂度】O(n) 时间，O(1) 空间。

【代码要点】每次计算当前面积更新 max，然后移动较矮指针。

【面试追问】为什么移动较矮的边？→ 移动高边，min 不会变大且宽度减小，面积必然不增，直接排除。`,
      source: 'LeetCode 11'
    },
    {
      id: 'al-073', category: 'algorithm', tags: ['双指针', '数组'], difficulty: 2,
      question: '四数之和如何求解？如何避免重复？',
      answer: `一句话结论：排序后固定前两个数，用双指针在剩余区间找后两个数，O(n³)；用「跳过相同元素」去重。

【思路】
1. 排序；
2. 两层循环固定 i、j，双指针 l、r 在 (j+1, n-1) 找 target - nums[i] - nums[j]；
3. 根据和与目标比较移动 l/r。

【去重】i、j 层循环跳过与上一个相同的值；找到后 l、r 也要跳过相同值。

【复杂度】O(n³) 时间，O(1) 空间。

【面试追问】N 数之和通解？→ 递归 + 双指针，超过 2 个数就递归降维。`,
      source: 'LeetCode 18'
    },
    {
      id: 'al-074', category: 'algorithm', tags: ['数组', '模拟'], difficulty: 2,
      question: '螺旋矩阵：如何按顺时针螺旋顺序遍历矩阵？',
      answer: `一句话结论：维护上下左右四个边界，按「右→下→左→上」循环遍历，每次走完一条边收缩对应边界。

【思路】top/bottom/left/right 四边界，循环：先从左到右走 top 行，top++；再从上到下走 right 列，right--；以此类推，直到边界交错。

【代码要点】注意走完一行/列后要判断边界是否仍有效（避免重复遍历）。

【复杂度】O(m×n) 时间，O(1) 空间。

【面试追问】螺旋生成矩阵（II）？→ 同样思路，往对应位置填数字。`,
      source: 'LeetCode 54 / 59'
    },
    {
      id: 'al-075', category: 'algorithm', tags: ['数组', '矩阵'], difficulty: 2,
      question: '旋转图像：如何原地把矩阵顺时针旋转 90 度？',
      answer: `一句话结论：先沿主对角线转置，再左右翻转每一行，即可原地顺时针旋转 90 度。

【思路】
1. 转置：matrix[i][j] 与 matrix[j][i] 交换（对角线对称）；
2. 每行反转：matrix[i][j] 与 matrix[i][n-1-j] 交换。

【复杂度】O(n²) 时间，O(1) 空间。

【代码要点】转置只需遍历上三角（i < j）。

【面试追问】逆时针旋转？→ 先转置再上下翻转；旋转 180？→ 左右翻转 + 上下翻转。`,
      source: 'LeetCode 48'
    },
    {
      id: 'al-076', category: 'algorithm', tags: ['数组', '矩阵'], difficulty: 2,
      question: '矩阵置零：若某元素为 0，则其所在行列都置 0，如何原地实现？',
      answer: `一句话结论：用矩阵的第一行第一列作为标记，记录哪些行列要置零，避免额外空间；注意第一行列本身需单独标记。

【思路】
1. 遍历，若 matrix[i][j]==0，标记 matrix[i][0]=0 和 matrix[0][j]=0；
2. 用额外变量标记第一行/第一列本身是否含 0；
3. 根据标记置零（从 i=1、j=1 开始），最后处理第一行列。

【复杂度】O(m×n) 时间，O(1) 空间。

【避坑】第一行列既当标记又可能是结果，需先单独记录，顺序不能错。`,
      source: 'LeetCode 73'
    },
    {
      id: 'al-077', category: 'algorithm', tags: ['数组', '原地哈希'], difficulty: 3,
      question: '缺失的第一个正数：如何用 O(n) 时间 O(1) 空间找？',
      answer: `一句话结论：原地哈希——把每个正数放到「值-1」的下标位置，再遍历找第一个下标与值不符的位置。

【思路】
1. 遍历，把在 [1, n] 范围内的数 x 交换到下标 x-1 处；
2. 再次遍历，第一个 nums[i] != i+1 的位置，答案就是 i+1；
3. 都正确则答案 n+1。

【复杂度】O(n) 时间，O(1) 空间（交换原地）。

【代码要点】while 循环持续交换直到当前位置正确或值超范围。

【面试追问】为什么能 O(1) 空间？→ 复用原数组做哈希标记。`,
      source: 'LeetCode 41'
    },
    {
      id: 'al-078', category: 'algorithm', tags: ['双指针', '二分'], difficulty: 2,
      question: '寻找重复数：数组中只有一个重复数（可出现多次），如何 O(1) 空间找？',
      answer: `一句话结论：把数组看成「下标 → 值」的链表，重复数即环的入口，用快慢指针（Floyd 判环）求解。

【思路】
1. slow = nums[slow]，fast = nums[nums[fast]]，找到相遇点；
2. 相遇后，一个指针回起点，二者同步走，再次相遇点即重复数（环入口）。

【为什么】值在 [1,n] 范围内可映射为「i 指向 nums[i]」，重复数导致成环。

【复杂度】O(n) 时间，O(1) 空间。

【面试追问】为什么不能改数组/用额外空间？→ 题目限制，Floyd 是满足 O(1) 空间的经典解法。`,
      source: 'LeetCode 287'
    },
    {
      id: 'al-079', category: 'algorithm', tags: ['数组', '投票'], difficulty: 1,
      question: '多数元素（出现次数超过一半）：如何 O(n) 时间 O(1) 空间求？',
      answer: `一句话结论：摩尔投票法——候选人 + 计数，遇到相同加一、不同减一，计数归零换候选人，最后候选人即多数。

【思路】遍历，维护 candidate 和 count；count==0 时设当前为 candidate；相同 count++，不同 count--。

【原理】多数元素数量 > n/2，抵消后必然剩下来。

【复杂度】O(n) 时间，O(1) 空间。

【面试追问】超过 1/3 的元素？→ 用两个候选人（Boyer-Moore 扩展）。为什么结果是多数？→ 多数元素能抵消所有其他元素仍有剩余。`,
      source: 'LeetCode 169'
    },
    {
      id: 'al-080', category: 'algorithm', tags: ['位运算'], difficulty: 2,
      question: '只出现一次的数字 II：其他元素都出现三次，如何找只出现一次的数？',
      answer: `一句话结论：按位统计所有数在每一位上 1 的个数，对 3 取模，剩下的位组成只出现一次的数。

【思路】
1. 对 32 位的每一位，统计所有数字该位为 1 的次数；
2. 该次数 % 3，结果即目标数在该位的值；
3. 组合所有位得到答案。

【复杂度】O(32n) = O(n) 时间，O(1) 空间。

【进阶】数字电路状态机：用 ones/twos 两个变量模拟「出现 0/1/2 次」的状态转移。

【面试追问】其他元素出现两次（I）？→ 异或所有数（x^x=0）。`,
      source: 'LeetCode 137'
    },
    {
      id: 'al-081', category: 'algorithm', tags: ['位运算'], difficulty: 1,
      question: '位 1 的个数：如何统计一个整数二进制中 1 的个数？',
      answer: `一句话结论：n & (n-1) 每次消除最低位的 1，循环计数即可。

【思路】while(n!=0){ n &= (n-1); count++; }。

【原理】n-1 会把最低位的 1 变 0、其后 0 变 1，与 n 相与恰好消掉最低位 1。

【复杂度】O(1 的个数)。

【面试追问】判断 2 的幂？→ n>0 且 n&(n-1)==0。交换两个数不用临时变量？→ a^=b; b^=a; a^=b。`,
      source: 'LeetCode 191'
    },
    {
      id: 'al-082', category: 'algorithm', tags: ['位运算'], difficulty: 1,
      question: '汉明距离：两个整数二进制对应位不同的个数，如何求？',
      answer: `一句话结论：先异或，再统计异或结果中 1 的个数，即汉明距离。

【思路】diff = x ^ y（不同位为 1），然后 while(diff){ diff &= diff-1; count++ }。

【复杂度】O(1 的个数)。

【应用】错误检测、相似度度量、基因序列比对。

【面试追问】异或的性质？→ a^a=0，a^0=a，交换律结合律；常用找不同、交换、去重。`,
      source: 'LeetCode 461'
    },
    {
      id: 'al-083', category: 'algorithm', tags: ['字符串', '模拟'], difficulty: 1,
      question: '二进制求和：两个二进制字符串相加，如何实现？',
      answer: `一句话结论：从末尾逐位相加，维护进位 carry，结果倒序拼接。

【思路】双指针从末尾开始，取每位的数字（含进位）相加，sum%2 是当前位，sum/2 是进位。

【复杂度】O(max(m,n)) 时间。

【代码要点】用 carry 表示进位，逐位加，最后若 carry 不为 0 补一位。

【面试追问】字符串相加（十进制大数）？→ 同样逐位相加思路，处理任意进制。`,
      source: 'LeetCode 67'
    },
    {
      id: 'al-084', category: 'algorithm', tags: ['字符串', '大数'], difficulty: 2,
      question: '字符串相加：两个大整数（可能超 long）如何相加？',
      answer: `一句话结论：模拟竖式加法，从低位到高位逐位相加并处理进位，避免数值溢出。

【思路】双指针从两字符串末尾向前，逐位转数字相加 + 进位，结果用 StringBuilder 倒序拼接后反转。

【复杂度】O(max(m,n)) 时间。

【代码要点】carry = sum / 10，当前位 = sum % 10。

【面试追问】大数相乘？→ 逐位相乘 + 累加，用数组存每位结果再统一进位。`,
      source: 'LeetCode 415'
    },
    {
      id: 'al-085', category: 'algorithm', tags: ['字符串', '模拟'], difficulty: 2,
      question: '字符串转换整数（atoi）：如何实现并处理各种边界？',
      answer: `一句话结论：按「跳过空格 → 处理符号 → 逐位累加 → 溢出判断」四步实现，关键是溢出与非法字符处理。

【思路】
1. 跳过前导空格；
2. 处理正负号；
3. 逐位解析数字，累加 result = result*10 + digit；
4. 累加前判断是否溢出（result > (MAX-digit)/10）。

【复杂度】O(n) 时间，O(1) 空间。

【避坑】溢出返回 INT_MAX/INT_MIN；遇到非数字立即停止。

【面试追问】如何判断溢出？→ 在乘 10 前用 (INT_MAX - digit)/10 比较。`,
      source: 'LeetCode 8'
    },
    {
      id: 'al-086', category: 'algorithm', tags: ['动态规划', '字符串'], difficulty: 3,
      question: '正则表达式匹配（. 和 *）：如何判断字符串是否匹配？',
      answer: `一句话结论：dp[i][j] 表示 s 前 i 个与 p 前 j 个是否匹配；遇到 * 分「匹配 0 次」和「匹配多次」两种情况。

【状态转移】
· p[j-1] 为普通字符或小数点「.」：dp[i][j] = dp[i-1][j-1]（若匹配）；
· p[j-1] 为星号「*」：
  1. 匹配 0 次：dp[i][j] = dp[i][j-2]；
  2. 匹配多次：若 s[i-1] 与 p[j-2] 匹配，dp[i][j] = dp[i-1][j]。

【复杂度】O(mn) 时间，O(mn) 空间。

【代码要点】星号「*」一定跟在某个字符后，看 p[j-2]。

【面试追问】与通配符匹配（? 和 *）区别？→ 通配符 * 匹配任意序列，转移不同（dp[i][j-1] 或 dp[i-1][j]）。`,
      source: 'LeetCode 10'
    },
    {
      id: 'al-087', category: 'algorithm', tags: ['动态规划', '字符串'], difficulty: 3,
      question: '通配符匹配（? 和 *）：如何判断字符串是否匹配？',
      answer: `一句话结论：dp[i][j] 表示 s 前 i 与 p 前 j 匹配；? 匹配单字符，* 匹配任意长度（含空）。

【状态转移】
· p[j-1]=='?' 或与 s[i-1] 相等：dp[i][j] = dp[i-1][j-1]；
· p[j-1]=='*'：dp[i][j] = dp[i-1][j]（匹配多个）|| dp[i][j-1]（匹配空）。

【复杂度】O(mn) 时间，O(mn) 空间（可滚动优化）。

【与正则区别】通配符 * 独立、可匹配任意串；正则 * 修饰前一字符、表示重复次数。

【面试追问】贪心解法？→ 记录上一个 * 位置，失配时回退到 * 多匹配一个。`,
      source: 'LeetCode 44'
    },
    {
      id: 'al-088', category: 'algorithm', tags: ['回溯', '字符串'], difficulty: 2,
      question: '分割回文串：如何求所有把字符串分割成回文子串的方案？',
      answer: `一句话结论：回溯 + 回文判断——枚举每个分割点，若前缀是回文则递归处理剩余部分。

【思路】
1. 从 start 出发，枚举结束位置 end；
2. 若 s[start..end] 是回文，加入路径，递归处理 end+1；
3. 回溯移除，继续尝试其他分割。

【优化】预先 DP 求出所有区间是否回文，避免重复判断。

【复杂度】最坏 O(n·2ⁿ)（每个位置都可能分割）。

【面试追问】最少分割次数？→ DP：dp[i] = min(dp[j]+1)（若 s[j..i] 回文）。`,
      source: 'LeetCode 131'
    },
    {
      id: 'al-089', category: 'algorithm', tags: ['回溯', '位运算'], difficulty: 2,
      question: '子集问题：如何求一个数组的所有子集（含重复元素）？',
      answer: `一句话结论：回溯枚举每个元素「选/不选」；或位运算枚举 0~2ⁿ-1 的掩码。

【回溯法】递归，每层决定是否加入当前元素，到末尾收集结果。

【位运算法】n 个元素有 2ⁿ 个子集，用 mask 的每一位表示是否选该元素。

【含重复元素（II）】先排序，回溯时同一层跳过相同元素避免重复子集。

【复杂度】O(n·2ⁿ) 时间。

【面试追问】为什么重复元素要先排序？→ 让相同元素相邻，便于同层去重（i>start && nums[i]==nums[i-1] 跳过）。`,
      source: 'LeetCode 78 / 90'
    },
    {
      id: 'al-090', category: 'algorithm', tags: ['回溯'], difficulty: 2,
      question: '组合总和：如何找出所有和为 target 的组合（可重复选）？',
      answer: `一句话结论：回溯 + 剪枝，元素可重复选则递归时 start 不变；排序后可提前剪枝。

【思路】
1. 排序（便于剪枝）；
2. 回溯：从 start 开始选，加入当前元素，递归时 start 不变（可重复选）；
3. 剩余和 < 当前元素时 break 剪枝。

【不可重复选（去重）】递归时 start=i+1，并同层跳过相同元素。

【复杂度】指数级，剪枝后大幅减少。

【面试追问】可重复选与不可重复选的区别？→ 可重复：递归 start=i；不可重复：start=i+1。`,
      source: 'LeetCode 39 / 40'
    },
    {
      id: 'al-091', category: 'algorithm', tags: ['回溯'], difficulty: 2,
      question: '电话号码的字母组合：如何求数字串对应的所有字母组合？',
      answer: `一句话结论：回溯——每个数字对应一组字母，逐位选一个字母递归组合。

【思路】
1. 建立数字到字母的映射（2→abc...）；
2. 从第 0 位开始，枚举该数字的每个字母，递归处理下一位；
3. 到末尾收集结果。

【复杂度】O(3ᵐ×4ⁿ)（m 个三位字母数字、n 个四位字母数字）。

【代码要点】用 StringBuilder 拼接，回溯时删除最后一位。

【面试追问】这是排列还是组合？→ 组合（每个位置独立选择，顺序固定）。`,
      source: 'LeetCode 17'
    },
    {
      id: 'al-092', category: 'algorithm', tags: ['回溯'], difficulty: 2,
      question: '括号生成：如何生成所有合法的 n 对括号组合？',
      answer: `一句话结论：回溯，维护左右括号剩余数，保证「左括号数 ≥ 右括号数」的约束下递归。

【思路】
1. 用 left、right 记录还能放多少左/右括号；
2. 只要 left>0 就可放左括号；
3. 只有 right>left 时才可放右括号（保证合法）；
4. left==0 且 right==0 时收集结果。

【复杂度】卡特兰数 C(2n,n)/(n+1) 种结果。

【面试追问】为什么 right>left 才放右括号？→ 保证任意前缀左括号数 ≥ 右括号数，序列才合法。`,
      source: 'LeetCode 22'
    },
    {
      id: 'al-093', category: 'algorithm', tags: ['回溯', 'DFS'], difficulty: 2,
      question: '单词搜索：判断单词是否存在于二维网格中（相邻字母）？',
      answer: `一句话结论：回溯 DFS——从每个匹配首字母的格子出发，向四方向搜索，用标记防止重复访问。

【思路】
1. 遍历网格，找到与 word[0] 相等的格子作为起点；
2. DFS 向上下左右搜索下一个字符；
3. 访问过的格子临时标记（如置特殊字符），回溯时恢复。

【复杂度】O(m×n×3ᴸ)（L 为单词长度）。

【避坑】必须「回溯恢复」标记，否则影响其他路径；或用 visited 数组。

【面试追问】多个单词（单词搜索 II）？→ 前缀树 Trie + DFS，剪枝。`,
      source: 'LeetCode 79'
    },
    {
      id: 'al-094', category: 'algorithm', tags: ['回溯'], difficulty: 3,
      question: '解数独：如何用回溯求解数独？',
      answer: `一句话结论：回溯 + 约束剪枝——逐格尝试 1~9，用行/列/宫三个布尔数组快速判断是否合法。

【思路】
1. 用三个 9×9 布尔数组记录行、列、九宫格中数字是否已用；
2. 从第一个空格开始，尝试 1~9 中未使用的数字；
3. 递归填下一个空格，失败则回溯。

【复杂度】指数级，但约束剪枝后实际很快。

【代码要点】宫索引 = (i/3)*3 + j/3。

【面试追问】如何优化？→ 从「候选数最少」的空格开始填（最少候选优先）。`,
      source: 'LeetCode 37'
    },
    {
      id: 'al-095', category: 'algorithm', tags: ['队列', '栈'], difficulty: 1,
      question: '如何用队列实现栈？',
      answer: `一句话结论：用两个队列或单队列，push 后把队列前 n-1 个元素重新入队，使新元素在队头。

【单队列法】每次 push 后，把前面 size-1 个元素出队再入队，让新元素排到队首；pop 直接出队。

【双队列法】一个主队列 + 辅助队列，push 到辅助，把主队列全倒入辅助，交换。

【复杂度】push O(n)，pop O(1)。

【面试追问】用栈实现队列？→ 两个栈：入栈 + 出栈，出栈为空时把入栈全部倒入出栈。`,
      source: 'LeetCode 225'
    },
    {
      id: 'al-096', category: 'algorithm', tags: ['队列', '设计'], difficulty: 2,
      question: '如何设计一个循环队列？',
      answer: `一句话结论：用数组 + 头尾指针实现环形队列，用「牺牲一个位置」或 size 计数区分队空与队满。

【思路】
1. 数组 capacity+1（或维护 size）；
2. front 指向队头，rear 指向队尾下一个插入位置；
3. 入队：arr[rear]=val，rear=(rear+1)%cap；
4. 出队：front=(front+1)%cap；
5. 空：front==rear；满：(rear+1)%cap==front。

【复杂度】所有操作 O(1)。

【代码要点】取模实现环形，注意满/空判断。

【面试追问】为什么牺牲一个位置？→ 否则 front==rear 无法区分空与满。`,
      source: 'LeetCode 622'
    },
    {
      id: 'al-097', category: 'algorithm', tags: ['哈希', '设计'], difficulty: 1,
      question: '如何设计一个哈希集合（HashSet）？',
      answer: `一句话结论：用「数组 + 链表（链地址法）」实现，哈希函数定位桶，冲突用链表存储。

【思路】
1. 桶数组，哈希函数 hash(key) = key % capacity（或 hashCode & (cap-1)）；
2. 冲突时桶内用链表存储；
3. add：定位桶，遍历链表查重后插入；remove/contains 同理。

【进阶】负载因子过高时扩容，重新哈希所有元素。

【复杂度】平均 O(1)，最坏 O(n)（全冲突）。

【面试追问】HashMap 和 HashSet 区别？→ HashSet 基于 HashMap 实现，只存 key，value 是常量。`,
      source: 'LeetCode 705'
    },
    {
      id: 'al-098', category: 'algorithm', tags: ['堆', '桶排序'], difficulty: 2,
      question: '前 K 个高频元素：如何求出现频率最高的 K 个元素？',
      answer: `一句话结论：先用哈希统计频次，再用「小顶堆」维护 K 个最高频，或用「桶排序」按频次分组。

【小顶堆法】遍历频次 map，维护大小为 K 的小顶堆，超过 K 且当前频次 > 堆顶则替换；O(nlogK)。

【桶排序法】用数组下标表示频次，下标 i 存出现 i 次的元素列表，从高到低取 K 个；O(n)。

【复杂度】堆 O(nlogK)；桶 O(n)。

【面试追问】为什么用小顶堆不用大顶堆？→ 小顶堆顶是 K 个里最小的，便于淘汰；大顶堆要维护全部 n 个。`,
      source: 'LeetCode 347'
    },
    {
      id: 'al-099', category: 'algorithm', tags: ['堆', '数据流'], difficulty: 3,
      question: '数据流的中位数：如何动态维护中位数？',
      answer: `一句话结论：用「大顶堆存较小的一半 + 小顶堆存较大的一半」，两堆顶之间即中位数。

【思路】
1. 大顶堆（left）存较小半，小顶堆（right）存较大半；
2. 保证 left 大小 == right 或 left 比 right 多 1；
3. 插入时先放 left，再把 left 顶移到 right，若 right 过大再移回；
4. 中位数：总数奇 → left 顶；偶 → 两顶均值。

【复杂度】插入 O(logn)，取中位数 O(1)。

【代码要点】用优先队列实现大小顶堆，注意堆大小的平衡。`,
      source: 'LeetCode 295'
    },
    {
      id: 'al-100', category: 'algorithm', tags: ['动态规划', '数组'], difficulty: 2,
      question: '最大子数组和：如何求连续子数组的最大和？（Kadane）',
      answer: `一句话结论：Kadane 算法——dp[i] = max(nums[i], dp[i-1]+nums[i])，即以 i 结尾的最大子数组和，答案取 max。

【思路】维护当前和 cur 与最大和 max：cur = max(nums[i], cur+nums[i])；max = max(max, cur)。

【理解】若 cur 为负，则从当前元素重新开始（负数只会拖累后续）。

【复杂度】O(n) 时间，O(1) 空间。

【面试追问】返回具体子数组？→ 记录起始位置，在重置 cur 时更新。环形数组最大和？→ 总和减去最小子数组和，与普通最大和取较大。`,
      source: 'LeetCode 53'
    }
  ];

  global.App = global.App || {};
  global.App.al2Bank = AL2;
})(window);

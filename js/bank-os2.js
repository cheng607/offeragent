/**
 * OfferAgent · 扩充题库（操作系统 · 第二批：os-021 ~ os-100）
 * 挂载到 global.App.os2Bank，由 bank.js 加载时合并。
 * 答案采用「一句话结论 + 完整解答」范式。
 */
(function (global) {
  'use strict';

  var OS2 = [
    {
      id: 'os-021', category: 'os', tags: ['进程', '状态'], difficulty: 2,
      question: '进程有哪些状态？状态之间如何转换？',
      answer: `一句话结论：进程基本状态有新建、就绪、运行、阻塞、终止；核心转换是「就绪 ↔ 运行 ↔ 阻塞」，调度器在就绪和运行间切换。

【五种状态】
1. 新建：进程刚创建、资源未就绪；
2. 就绪：已就绪等待 CPU 调度；
3. 运行：正在 CPU 上执行；
4. 阻塞（等待）：等待某事件（IO、锁、信号）；
5. 终止：执行完毕或被杀死。

【转换关系】
- 就绪 → 运行：被调度器选中；
- 运行 → 就绪：时间片用完被抢占；
- 运行 → 阻塞：等待 IO/事件主动让出；
- 阻塞 → 就绪：等待的事件发生。

【面试追问】运行态能直接到阻塞吗？→ 能（主动发起 IO），阻塞态不能直接到运行，必须经过就绪。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-022', category: 'os', tags: ['线程', '同步'], difficulty: 3,
      question: '线程同步方式有哪些？互斥锁、条件变量、信号量、读写锁有什么区别？',
      answer: `一句话结论：互斥锁保证「同一时刻只有一个线程访问临界区」，条件变量用于「等待某个条件成立」，信号量控制「并发数量」，读写锁区分读多写少场景。

【互斥锁 Mutex】互斥访问，同一时刻仅一个线程持有，解决竞态。

【条件变量 Condition】配合互斥锁使用，让线程在条件不满足时释放锁并等待，条件满足时被唤醒，避免忙等。

【信号量 Semaphore】计数器，P（减）V（加），可控制同时访问资源的最大线程数（如限流、资源池）。

【读写锁 RWLock】读共享、写独占，读多写少时提升并发。

【对比】互斥锁是「二值信号量」特例；条件变量解决「等待」，信号量解决「计数」，读写锁解决「读多写少」。

【面试追问】条件变量为什么必须配合锁？→ 检查条件和等待必须原子，否则会丢失唤醒信号。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-023', category: 'os', tags: ['信号量', '并发'], difficulty: 3,
      question: '信号量（Semaphore）的原理是什么？P/V 操作如何实现？',
      answer: `一句话结论：信号量是一个整数计数器，P 操作「减一并可能阻塞」，V 操作「加一并唤醒等待者」，用来控制对共享资源的并发访问数量。

【P 操作（wait/proberen）】
- 信号量值减 1；
- 若减后小于 0，则当前线程阻塞进入等待队列。

【V 操作（signal/verhogen）】
- 信号量值加 1；
- 若加后小于等于 0，说明有等待者，唤醒一个。

【关键】P/V 操作本身必须原子（通过关中断、自旋锁或硬件指令保证）。

【分类】二值信号量（互斥）、计数信号量（控制数量）。

【面试追问】信号量和互斥锁区别？→ 信号量可计数、允许多个线程，互斥锁只能一个线程持有；信号量可由其他线程 V 释放，互斥锁通常由持有者释放。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-024', category: 'os', tags: ['并发', '经典问题'], difficulty: 3,
      question: '生产者-消费者问题如何用信号量解决？',
      answer: `一句话结论：用三个信号量——互斥量保证缓冲区互斥访问，empty 表示空位数量，full 表示产品数量，生产/消费前分别 P 相应计数信号量。

【信号量设计】
- mutex = 1：互斥访问缓冲区；
- empty = N（缓冲区大小）：空槽数；
- full = 0：已生产产品数。

【生产者】
P(empty)（有空位才生产）→ P(mutex)（进临界区）→ 放入产品 → V(mutex) → V(full)（产品+1）。

【消费者】
P(full)（有产品才消费）→ P(mutex) → 取出产品 → V(mutex) → V(empty)（空位+1）。

【要点】先 P 计数信号量再 P 互斥量，避免死锁（防止持有互斥量却因无空位/产品而阻塞）。

【面试追问】若先 P(mutex) 再 P(empty) 会怎样？→ 缓冲区满时生产者持锁阻塞，消费者无法进临界区，死锁。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-025', category: 'os', tags: ['并发', '死锁'], difficulty: 4,
      question: '哲学家就餐问题是什么？如何避免死锁？',
      answer: `一句话结论：五个哲学家围坐、每两人之间一把叉子，每人需两把叉子才能吃饭，若都同时拿起左手叉子则会互相等待形成死锁；解法是破坏「循环等待」条件。

【问题本质】多进程竞争有限资源且存在循环等待的经典死锁模型。

【解法】
1. 资源有序：给叉子编号，规定必须先拿编号小的，破坏循环等待；
2. 限制人数：最多允许 4 人同时就餐，保证至少一人能拿到两把叉；
3. 一次性拿两把：要么全拿、要么不拿（需临界区保护）；
4. 奇偶策略：奇数哲学家先拿左、偶数先拿右。

【面试追问】为什么「资源有序」能避免死锁？→ 因为不会再出现「A 等 B、B 等 C、C 等 A」的环。`,
      source: '大厂后端真题'
    },
    {
      id: 'os-026', category: 'os', tags: ['锁', '自旋锁'], difficulty: 3,
      question: '自旋锁和互斥锁有什么区别？各自适用什么场景？',
      answer: `一句话结论：自旋锁在获取不到锁时「原地忙等」不释放 CPU，互斥锁会让线程「挂起睡眠」让出 CPU；自旋锁适合临界区极短、锁竞争少的场景。

【自旋锁 Spinlock】
- 忙等：不断循环尝试获取锁，消耗 CPU；
- 无上下文切换开销；
- 适合：临界区极短（几行代码）、多核环境；
- 不适合：临界区长、单核、锁竞争激烈。

【互斥锁 Mutex】
- 获取不到则阻塞睡眠，让出 CPU；
- 有上下文切换开销（切换成本高）；
- 适合：临界区较长、竞争激烈。

【面试追问】单核下自旋锁有意义吗？→ 基本无意义，自旋会占满 CPU 使持锁线程无法运行；内核在单核下自旋锁退化为禁止抢占。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-027', category: 'os', tags: ['锁', '读写锁'], difficulty: 3,
      question: '读写锁是什么？它的实现原理是怎样的？',
      answer: `一句话结论：读写锁把「读」和「写」分开——读操作可多个并发、写操作独占，读多写少时性能远优于互斥锁；实现上用计数区分读写者。

【规则】
- 读-读：可并发；
- 读-写：互斥；
- 写-写：互斥。

【实现思路】
- 维护读者计数和写者标志；
- 读者进入：无写者则计数+1，否则等待；
- 写者进入：无读者无写者才进入，否则等待；
- 需处理写者饥饿（读者不断进入导致写者等不到）。

【优化】公平读写锁：写者等待时，后续读者排队，避免写者饿死。

【面试追问】读写锁一定比互斥锁好吗？→ 临界区极短或写多时，读写锁额外开销反而更大，需按读写比选择。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-028', category: 'os', tags: ['线程池', '并发'], difficulty: 3,
      question: '线程池的原理是什么？为什么能提升性能？',
      answer: `一句话结论：线程池预先创建一批线程复用，任务到来时直接提交给空闲线程执行，避免频繁创建/销毁线程的开销，并控制并发度、保护系统资源。

【核心组成】
1. 工作线程集合；
2. 任务队列（有界/无界）；
3. 拒绝策略（队列满时如何处理）。

【为什么高效】
- 线程创建/销毁开销大（内核资源、栈分配）；
- 复用线程避免重复创建；
- 控制最大并发数，防止线程爆炸拖垮系统；
- 任务排队削峰。

【关键参数】核心线程数、最大线程数、队列容量、拒绝策略、存活时间。

【面试追问】核心线程数如何设置？→ 参考 CPU 核数：CPU 密集约 N+1，IO 密集约 2N，需实测压调。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-029', category: 'os', tags: ['进程', '线程', '选型'], difficulty: 3,
      question: '多线程和多进程如何选型？',
      answer: `一句话结论：需要共享内存、频繁通信、轻量并发用多线程；需要隔离崩溃、利用多核、独立部署用多进程；选择看「通信成本 vs 隔离需求」。

【多线程优势】
- 共享地址空间、通信快（直接读写内存）；
- 创建/切换开销小；
- 适合 IO 密集、任务协作。

【多线程劣势】
- 一个线程崩溃可能拖垮整个进程；
- 需处理数据竞争与锁。

【多进程优势】
- 隔离性好，进程崩溃互不影响；
- 天然利用多核、多机扩展；
- 适合 CPU 密集、需强隔离（如浏览器 tab、Nginx worker）。

【多进程劣势】
- 创建/切换/通信（IPC）开销大。

【面试追问】浏览器为何用多进程？→ 每个 tab 独立进程，崩溃不影响其他页面，安全隔离。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-030', category: 'os', tags: ['调度', 'CFS'], difficulty: 4,
      question: 'Linux 的 CFS（完全公平调度）原理是什么？',
      answer: `一句话结论：CFS 以「虚拟运行时间」衡量每个进程获得的 CPU，始终选择虚拟运行时间最小的进程运行，让所有就绪进程公平分享 CPU。

【核心概念】
- vruntime（虚拟运行时间）：进程已运行时间按权重归一化，权重越高增长越慢；
- 红黑树：按 vruntime 组织就绪进程，取最左节点调度。

【调度逻辑】
- 每次调度选 vruntime 最小的进程；
- 进程运行后 vruntime 增加，重新插入红黑树；
- nice 值越低（优先级越高）权重越大，vruntime 增长越慢，获得更多 CPU。

【特点】
- O(log n) 复杂度；
- 无固定时间片概念，用目标延迟动态分配。

【面试追问】CFS 如何保证 IO 密集进程响应？→ IO 进程常睡眠，vruntime 小，唤醒后能快速被调度，天然偏向交互型任务。`,
      source: '大厂内核真题'
    },
    {
      id: 'os-031', category: 'os', tags: ['调度', '优先级反转'], difficulty: 4,
      question: '什么是优先级反转？如何解决？',
      answer: `一句话结论：低优先级任务持有高优先级任务需要的锁，导致高优先级任务被低优先级阻塞、甚至被中优先级任务抢占，造成「高优先级反而最后执行」；通过优先级继承/优先级天花板解决。

【场景】
1. 低优先级 L 持有锁；
2. 高优先级 H 需要该锁而阻塞；
3. 中优先级 M 抢占 L，导致 L 迟迟不释放锁，H 被 M 长期阻塞。

【解决】
1. 优先级继承：L 临时继承 H 的优先级，快速执行完释放锁；
2. 优先级天花板：持有锁的任务立即升到锁的「天花板」优先级。

【面试追问】火星探路者事故？→ 1997 年因优先级反转导致系统重启，靠优先级继承修复，是经典案例。`,
      source: '大厂内核真题'
    },
    {
      id: 'os-032', category: 'os', tags: ['死锁', '银行家算法'], difficulty: 4,
      question: '银行家算法是什么？如何避免死锁？',
      answer: `一句话结论：银行家算法在分配资源前先「试算」，判断分配后系统是否仍处于安全状态（能找到一种让所有进程完成的顺序），安全才分配，从而避免死锁。

【数据结构】可用资源、最大需求、已分配、还需资源矩阵。

【步骤】
1. 进程请求资源；
2. 模拟分配，检查是否安全；
3. 安全（存在安全序列）→ 真分配；不安全 → 拒绝请求。

【安全序列】存在一个进程执行顺序，使每个进程都能依次获得所需资源并完成、释放资源。

【局限】需预先知道最大需求、进程数量固定、开销大，实际系统很少用，常用于教学与死锁预防。

【面试追问】死锁避免和死锁预防区别？→ 预防是破坏必要条件（静态），避免是运行时动态判断（银行家）。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-033', category: 'os', tags: ['死锁', '检测'], difficulty: 3,
      question: '死锁如何检测与恢复？',
      answer: `一句话结论：死锁检测用「资源分配图 + 环路判断」判断是否死锁；检测到后通过「撤销进程、抢占资源、回滚」等方式恢复。

【检测方法】
1. 资源分配图：进程→资源为请求边，资源→进程为分配边；
2. 若每种资源只有单个实例，图中有环即死锁；
3. 多实例资源用银行家式「安全性算法」判断。

【恢复方式】
1. 终止进程：全部终止或逐个终止直到解除；
2. 资源抢占：从某些进程抢占资源分配给他人；
3. 进程回滚：回滚到检查点。

【实际系统做法】数据库靠超时+事务回滚；操作系统常「鸵鸟策略」——假设死锁极少发生而忽略。

【面试追问】什么是鸵鸟策略？→ 对极低概率的死锁不专门处理，代价高时才处理。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-034', category: 'os', tags: ['内存', 'malloc'], difficulty: 4,
      question: 'malloc/free 的底层原理是什么？brk 和 mmap 有什么区别？',
      answer: `一句话结论：malloc 分配内存时，小块用 brk 移动堆顶指针、大块用 mmap 映射独立内存区，并维护空闲链表/元数据来管理；free 把内存归还并合并相邻空闲块。

【分配策略】
- 小于阈值（通常 128KB）：用 brk/sbrk 扩展堆；
- 大于阈值：用 mmap 匿名映射独立区。

【内存管理】
- 每个分配块前有元数据（大小、是否空闲）；
- 空闲块用链表/bins 组织，分配时按首次/最佳适配找块；
- 释放时标记空闲并合并相邻块，减少碎片。

【brk vs mmap】
- brk：连续扩展堆顶，释放只能从顶部收缩，易产生碎片；
- mmap：独立映射，free 即真正归还，但每次有系统调用与页表开销。

【面试追问】free 为什么不需要传大小？→ 因为块头元数据记录了大小。`,
      source: '大厂后端真题'
    },
    {
      id: 'os-035', category: 'os', tags: ['内存', '堆栈'], difficulty: 2,
      question: '堆和栈有什么区别？',
      answer: `一句话结论：栈由编译器自动管理、存放局部变量与调用信息、速度快但空间小；堆由程序员手动分配释放、空间大但需管理、速度慢且易碎片化。

【栈 Stack】
- 自动分配释放，函数调用压栈、返回弹栈；
- 存放局部变量、参数、返回地址；
- 空间小（几 MB），连续、速度快；
- 栈溢出风险（递归过深、大数组）。

【堆 Heap】
- 手动 malloc/new、free/delete；
- 空间大，可动态增长；
- 分配需查找空闲块，速度慢；
- 易内存泄漏与碎片。

【面试追问】为什么栈比堆快？→ 栈仅移动栈指针、连续访问缓存友好；堆需查找与元数据维护。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-036', category: 'os', tags: ['内存', '对齐'], difficulty: 3,
      question: '什么是内存对齐？为什么需要它？',
      answer: `一句话结论：内存对齐让变量的地址是其大小的整数倍，用填充字节补齐，使 CPU 能一次访存读取数据、避免跨缓存行访问，从而提升性能。

【为什么需要】
- CPU 按字/缓存行读取内存，未对齐的数据可能跨两个存储单元，需多次访存；
- 某些架构未对齐访问直接崩溃；
- 对齐减少缓存行跨越，提升效率。

【结构体对齐规则】
- 成员按自身大小对齐；
- 结构体总大小为最大成员对齐数的整数倍；
- 合理排列成员顺序可减少填充。

【面试追问】如何减少结构体大小？→ 把大成员放前面或按对齐大小降序排列，减少 padding 空洞。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-037', category: 'os', tags: ['内存', '大页'], difficulty: 4,
      question: '大页内存（Huge Page）是什么？有什么优缺点？',
      answer: `一句话结论：大页用 2MB/1GB 的页替代默认 4KB 页，减少页表项与 TLB 未命中，提升大内存应用性能；代价是内存利用率降低、管理复杂。

【背景】默认 4KB 页，大内存应用页表巨大，TLB 覆盖范围有限导致频繁未命中。

【优点】
- 减少页表项与页表级数；
- 提高 TLB 命中率；
- 减少缺页与页表遍历开销。

【缺点】
- 内存按大页分配，小需求浪费（内部碎片）；
- 换出/迁移成本高；
- 需应用配合（mmap 指定、预分配）。

【面试追问】大页适合什么场景？→ 数据库、JVM 堆、内存数据库等大内存、性能敏感应用。`,
      source: '大厂内核真题'
    },
    {
      id: 'os-038', category: 'os', tags: ['内存', 'mmap'], difficulty: 4,
      question: 'mmap 的原理是什么？有哪些用途？',
      answer: `一句话结论：mmap 把文件或设备映射到进程虚拟地址空间，让用户像访问内存一样读写文件，减少内核与用户态的拷贝，常用于文件读写、共享内存、零拷贝。

【原理】
- 建立虚拟地址到文件页的映射；
- 访问时缺页中断，内核把文件页加载进页缓存；
- 修改后脏页回写磁盘。

【用途】
1. 文件读写：替代 read/write，减少一次拷贝；
2. 共享内存：多进程映射同一文件通信；
3. 零拷贝：配合 sendfile 减少拷贝；
4. 大文件随机访问。

【优点】减少系统调用与拷贝、支持共享。

【缺点】映射大文件占用虚拟地址、缺页有开销、文件大小需固定或扩展。

【面试追问】mmap 一定比 read 快吗？→ 顺序读小块未必，read 有预读；随机大文件 mmap 优势明显。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-039', category: 'os', tags: ['内存', '写时复制'], difficulty: 3,
      question: '写时复制（Copy-on-Write）是什么？fork 如何利用它？',
      answer: `一句话结论：写时复制让多个进程先共享同一份物理内存（只读），只有当某进程要写入时才复制一份私有副本，从而避免 fork 时全量拷贝内存。

【原理】
- fork 后父子进程共享同一物理页，标记为只读；
- 任何一方写入触发缺页异常；
- 内核为该进程复制一页，解除只读后继续执行。

【好处】
- fork 极快（无需拷贝全部内存）；
- 未修改的页一直共享，省内存；
- 只有实际写的页才复制。

【应用】fork、字符串 Copy-on-Write 优化、Docker 镜像分层等。

【面试追问】fork 后父子进程内存完全独立吗？→ 逻辑上独立，物理上写时复制，写之前共享。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-040', category: 'os', tags: ['内存', 'Swap'], difficulty: 3,
      question: 'Swap 交换分区是什么？swappiness 如何影响系统？',
      answer: `一句话结论：Swap 是磁盘上的交换空间，当物理内存不足时把不常用的内存页换出到磁盘；swappiness 控制内核「换出匿名页 vs 回收文件页」的倾向。

【Swap 作用】
- 内存不足时腾出物理内存；
- 保存冷数据，避免 OOM；
- 休眠时保存内存。

【swappiness（0-100）】
- 值越大越倾向换出匿名页（进程数据）到 swap；
- 值越小越倾向回收文件页（页缓存）；
- 默认约 60。

【性能影响】swap 换入换出走磁盘，极慢；频繁换页（抖动）会严重拖慢系统。

【面试追问】生产环境要不要开 swap？→ 一般保留少量以兜底，但避免依赖；数据库等常建议 swappiness 调低。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-041', category: 'os', tags: ['内存', 'OOM'], difficulty: 3,
      question: 'OOM Killer 的原理是什么？如何排查与避免？',
      answer: `一句话结论：OOM Killer 是内核在内存耗尽、无法回收时，按进程评分（内存占用、运行时间等）强制杀死得分最高的进程以释放内存；排查靠日志与监控，避免靠容量规划与限流。

【触发条件】内存不足、回收失败、swap 也耗尽，内核调用 OOM Killer。

【评分依据】进程占用内存越多、运行时间越短（oome_score 越大）越可能被杀；oome_score_adj 可调整。

【排查】
- dmesg 查看 OOM 日志，看被杀进程；
- 检查 free、内存趋势、是否有泄漏；
- 分析是否突发大内存分配。

【避免】
- 容量规划、预留内存；
- 限制进程内存（cgroup、容器 limit）；
- 修复内存泄漏；
- 合理配置 swap 兜底。

【面试追问】容器内存 limit 和 OOM 关系？→ 超过 cgroup 限制同样触发 OOM，杀容器内进程。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-042', category: 'os', tags: ['内存', '回收'], difficulty: 4,
      question: 'Linux 内存回收机制是怎样的？',
      answer: `一句话结论：Linux 内存回收优先回收干净文件页（直接释放），其次回收脏页（先回写）、匿名页（换出到 swap），由 kswapd 与直接回收共同完成。

【回收对象】
1. 文件页（页缓存）：干净页直接释放，脏页先回写；
2. 匿名页（进程数据）：需换出到 swap；
3. slab 缓存：回收可回收的对象。

【触发机制】
- kswapd：内存低于水位时后台异步回收；
- 直接回收：分配时内存不足，进程同步回收（阻塞）；
- 紧急时 OOM Killer。

【水位线】low/min/high 三级水位，决定何时回收。

【面试追问】为什么优先回收文件页？→ 干净文件页无需写回，成本最低。`,
      source: '大厂内核真题'
    },
    {
      id: 'os-043', category: 'os', tags: ['内存', '页表', 'TLB'], difficulty: 4,
      question: '虚拟地址到物理地址的翻译过程是怎样的？多级页表和 TLB 是什么？',
      answer: `一句话结论：虚拟地址通过多级页表逐级查找到物理页框号，再加上页内偏移得到物理地址；TLB 是页表的硬件缓存，缓存最近翻译结果加速地址转换。

【翻译过程】
1. 取虚拟地址的页目录/页表各级索引；
2. 逐级查页表得到物理页框号；
3. 物理地址 = 页框号 + 页内偏移。

【多级页表】把页表分级，只为实际使用的地址范围分配页表，节省内存（4 级页表是 Linux 常见）。

【TLB（快表）】
- 缓存虚拟页号→物理页框号的映射；
- 命中则跳过页表遍历；
- 进程切换需刷新或按 ASID 区分。

【面试追问】TLB 未命中会怎样？→ 触发页表遍历，若页不在内存则缺页中断，代价大。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-044', category: 'os', tags: ['TLB', '缓存'], difficulty: 3,
      question: 'TLB 是什么？TLB 未命中（miss）会怎样？',
      answer: `一句话结论：TLB 是 CPU 内缓存虚拟地址到物理地址映射的硬件快表；命中则快速完成翻译，未命中则需遍历页表（甚至缺页），显著增加访存延迟。

【TLB 作用】页表在内存，每次访存都查页表太慢，TLB 缓存高频映射。

【未命中处理】
1. 硬件或软件遍历页表查找映射；
2. 找到则填入 TLB（可能淘汰旧项）；
3. 页不在内存则触发缺页中断。

【优化】
- 大页提高 TLB 覆盖率；
- 减少进程切换导致的 TLB 刷新；
- 数据局部性提高命中。

【面试追问】进程切换为什么影响 TLB？→ 不同进程地址空间不同，需刷新 TLB 或使用 ASID 区分。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-045', category: 'os', tags: ['内存', '缺页中断'], difficulty: 3,
      question: '什么是缺页中断（Page Fault）？处理流程是怎样的？',
      answer: `一句话结论：缺页中断是程序访问的虚拟页不在物理内存时触发的中断，内核据此把所需页从磁盘加载到内存；分「次缺页」「主缺页」两种。

【分类】
1. 次缺页（minor）：页在内存但未映射（如写时复制、共享库），仅更新页表；
2. 主缺页（major）：页在磁盘，需真正 IO 读入。

【处理流程】
1. 访问缺页地址触发异常；
2. 内核判断地址是否合法；
3. 非法 → 段错误（segfault）；
4. 合法 → 找空闲页框或淘汰页；
5. 从磁盘读入页并更新页表；
6. 重新执行触发指令。

【面试追问】频繁主缺页说明什么？→ 内存不足或访问模式差，频繁换页，性能下降。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-046', category: 'os', tags: ['文件', 'fd'], difficulty: 3,
      question: '文件描述符（fd）是什么？fd 耗尽如何排查？',
      answer: `一句话结论：文件描述符是进程打开文件/套接字等资源的整数句柄，进程通过它读写资源；fd 耗尽会导致「Too many open files」错误，排查靠查看打开数量与泄漏点。

【本质】fd 是进程文件描述符表的索引，指向内核文件对象。

【限制】
- 进程级：ulimit -n；
- 系统级：/proc/sys/fs/file-max。

【fd 耗尽排查】
1. 报错确认 Too many open files；
2. lsof -p PID 统计打开数量；
3. ls /proc/PID/fd 查看；
4. 检查是否忘记 close（连接泄漏）；
5. 调大 ulimit 或修复泄漏。

【面试追问】fd 和 inode 区别？→ fd 是进程内的句柄，inode 是文件在磁盘的元数据，一个 inode 可被多个 fd 引用。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-047', category: 'os', tags: ['IO', 'epoll'], difficulty: 4,
      question: 'epoll 的实现原理是什么？为什么比 select/poll 高效？',
      answer: `一句话结论：epoll 用红黑树管理所有监听 fd、用就绪链表返回就绪事件，内核在事件发生时直接把 fd 挂到就绪链表，无需每次遍历全部 fd，因此高效。

【三个核心】
- epoll_create：创建 epoll 实例（红黑树+就绪链表）；
- epoll_ctl：增删改监听 fd；
- epoll_wait：等待并返回就绪事件。

【为什么高效】
1. 事件驱动：fd 就绪时内核回调挂到就绪链表，O(1) 获取；
2. 无需像 select/poll 每次拷贝并遍历全部 fd；
3. fd 集合在内核常驻，不用每次重新传入；
4. 支持大量连接（百万级）。

【对比】select 有 fd 数量上限（1024）、每次 O(n) 遍历；poll 无上限但仍是 O(n) 轮询。

【面试追问】epoll 有上限吗？→ 无固定 fd 上限，受系统资源限制。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-048', category: 'os', tags: ['IO', 'epoll'], difficulty: 4,
      question: 'epoll 的 LT（水平触发）和 ET（边缘触发）有什么区别？',
      answer: `一句话结论：LT 只要事件未处理就持续通知，ET 只在状态变化时通知一次；ET 需非阻塞 IO 且要把数据读完，但系统调用更少、性能更高。

【LT 水平触发】
- 就绪（有数据）就会一直通知；
- 不处理会反复提醒；
- 编程简单，可用阻塞 IO；
- 是 epoll 默认模式。

【ET 边缘触发】
- 只在「无数据→有数据」的边沿通知一次；
- 必须非阻塞 IO，循环读到 EAGAIN；
- 减少系统调用、性能高，但编程复杂易漏事件。

【面试追问】ET 为什么必须非阻塞？→ 需一次把数据读完，若用阻塞读，读完最后一条会阻塞住整个事件循环。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-049', category: 'os', tags: ['IO', '页缓存'], difficulty: 4,
      question: '页缓存（Page Cache）和脏页回写是什么？',
      answer: `一句话结论：页缓存是内核缓存文件内容的物理内存页，读写文件先走页缓存加速访问；脏页是已修改但未写回磁盘的页，由内核按策略异步回写。

【页缓存作用】
- 缓存文件数据，命中则避免磁盘 IO；
- read/write 先操作页缓存；
- 这也是 free 中 cache 的来源。

【脏页回写】
- 脏页：内存中被修改、与磁盘不一致的页；
- 触发回写：脏页比例超阈值、脏页超时、内存不足；
- 由内核线程（如 kworker）异步写回。

【关键参数】dirty_ratio、dirty_background_ratio、dirty_expire_centisecs。

【面试追问】为什么要有脏页机制？→ 合并多次写、延迟写回，减少磁盘随机写、提升吞吐。`,
      source: '大厂内核真题'
    },
    {
      id: 'os-050', category: 'os', tags: ['IO', 'DirectIO'], difficulty: 3,
      question: 'Direct IO 和 Buffered IO 有什么区别？',
      answer: `一句话结论：Buffered IO 走页缓存、由内核缓存与预读，适合常规读写；Direct IO 绕过页缓存直接读写磁盘，避免双重缓存，适合数据库等自管缓存场景。

【Buffered IO】
- 默认模式，经页缓存；
- 内核预读、写合并，性能好；
- 数据可能延迟落盘（脏页）；
- 存在用户态与页缓存间的拷贝。

【Direct IO】
- O_DIRECT 打开，绕过页缓存；
- 应用自管缓存（如数据库 buffer pool）；
- 需满足对齐要求（偏移、长度、缓冲区）；
- 无缓存加速，随机小 IO 反而慢。

【面试追问】数据库为什么用 Direct IO？→ 数据库有自己的缓存，避免页缓存双重缓存浪费内存与拷贝。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-051', category: 'os', tags: ['文件系统', '日志'], difficulty: 4,
      question: '文件系统的日志（Journal）机制是什么？',
      answer: `一句话结论：日志机制在真正修改文件系统前，先把操作意图写入日志区，崩溃后靠重放日志恢复一致性，避免元数据损坏；分 writeback、ordered、data 三种模式。

【背景】文件系统元数据更新多步，中途断电会导致不一致，日志保证原子性。

【流程】
1. 写日志（journal）：记录将要做的修改；
2. 提交日志；
3. 真正写数据/元数据；
4. 完成后清理日志。

【三种模式】
- writeback：只记元数据，数据可能先落盘，最快但一致性弱；
- ordered（默认）：数据先于元数据落盘，平衡；
- data：数据也记日志，最安全但最慢。

【面试追问】ext4 和 xfs 都用日志吗？→ 都支持日志，xfs 日志机制更强。`,
      source: '大厂内核真题'
    },
    {
      id: 'os-052', category: 'os', tags: ['磁盘', 'IO调度'], difficulty: 3,
      question: '磁盘 IO 调度算法有哪些？各有什么特点？',
      answer: `一句话结论：磁盘 IO 调度通过重排请求顺序减少磁头移动、提升吞吐；常见有 NOOP、Deadline、CFQ、以及现在的 mq-deadline 等。

【NOOP】FIFO，仅做简单合并，适合 SSD（无寻道时间）。

【Deadline】为读写设置截止时间，防止请求饿死，兼顾吞吐与延迟，适合数据库。

【CFQ（完全公平队列）】按进程分队列公平分配 IO，适合多任务桌面。

【现代调度】多队列 blk-mq 后常用 none（SSD）和 mq-deadline。

【选择】
- SSD：none 或 NOOP（无需寻道优化）；
- HDD：deadline 或 CFQ。

【面试追问】为什么 SSD 不需要寻道优化？→ SSD 无机械磁头，随机访问快，重排意义不大反而增加延迟。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-053', category: 'os', tags: ['磁盘', 'RAID'], difficulty: 3,
      question: 'RAID 有哪些级别？各有什么区别？',
      answer: `一句话结论：RAID 通过多磁盘组合提升性能或可靠性；RAID0 条带化提速、RAID1 镜像备份、RAID5 分布式奇偶校验（兼顾容量与容错）、RAID10 结合镜像与条带。

【RAID0】数据条带分布到多盘，读写并行快，无冗余，任一盘坏数据全丢。

【RAID1】镜像，数据完整复制到另一盘，可靠性高，容量利用率 50%。

【RAID5】数据+奇偶校验分布在多盘，允许坏 1 块盘，容量利用 (n-1)/n，写有校验开销。

【RAID6】双奇偶校验，允许坏 2 块盘。

【RAID10】先镜像再条带，性能与可靠性兼得，容量利用率 50%，常用于数据库。

【面试追问】RAID5 和 RAID10 如何选？→ 读多写少、重容量选 RAID5；写多、要求高可靠选 RAID10。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-054', category: 'os', tags: ['文件', 'inode'], difficulty: 4,
      question: '打开一个文件时，进程、文件描述符表、文件表、inode 之间是什么关系？',
      answer: `一句话结论：每个进程有文件描述符表，fd 指向内核「打开文件表」条目，条目记录文件偏移等状态并指向 inode；多个进程可共享同一 inode。

【层次关系】
1. 进程文件描述符表：fd → 打开文件表项指针；
2. 打开文件表（系统级）：记录文件偏移、访问模式、引用计数，指向 inode；
3. inode：文件元数据（大小、权限、数据块位置）。

【含义】
- 同一进程 dup 一个 fd：共享同一文件表项（共享偏移）；
- 两个进程各自 open 同一文件：各自独立文件表项（独立偏移）；
- 软硬链接、多个 fd 可指向同一 inode。

【面试追问】父子进程 fork 后 fd 共享吗？→ 共享同一文件表项，文件偏移是共享的。`,
      source: '大厂内核真题'
    },
    {
      id: 'os-055', category: 'os', tags: ['并发', 'CAS'], difficulty: 3,
      question: 'CAS 是什么？ABA 问题如何解决？',
      answer: `一句话结论：CAS（比较并交换）是硬件原子指令，比较内存值是否为预期值、是则更新，是实现无锁并发的基础；ABA 问题指值从 A 变 B 又变回 A 导致误判，靠版本号解决。

【CAS 原理】
- 原子执行：若内存值 == 预期值，则写入新值返回成功；
- 否则返回失败，由调用方重试；
- 对应 CPU 的 cmpxchg 指令。

【ABA 问题】
- 线程 1 读到 A；
- 线程 2 把 A 改成 B 又改回 A；
- 线程 1 的 CAS 仍成功，但它不知道中间发生过变化。

【解决】
1. 加版本号/时间戳（AtomicStampedReference）；
2. 每次更新版本号+1，CAS 同时比较值和版本。

【面试追问】CAS 的缺点？→ 自旋空转消耗 CPU、只能保证单个变量原子性、ABA 问题。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-056', category: 'os', tags: ['并发', '无锁'], difficulty: 4,
      question: '什么是无锁编程（Lock-Free）？如何实现？',
      answer: `一句话结论：无锁编程用 CAS 等原子操作替代传统锁，保证任意时刻至少一个线程能推进、系统不会因锁而整体停滞；实现靠 CAS 循环、原子变量与内存序。

【核心思想】
- 不阻塞线程，用原子指令完成数据更新；
- CAS 循环重试直到成功。

【实现手段】
1. CAS 原子操作；
2. 原子变量（AtomicInteger 等）；
3. 内存屏障保证可见性与顺序；
4. 无锁队列（如 Michael-Scott 队列）。

【优点】避免上下文切换、无死锁、高并发下扩展性好。

【缺点】实现复杂、CAS 自旋消耗 CPU、ABA 问题、难调试。

【面试追问】无锁一定比有锁快吗？→ 低竞争时可能相当或略慢（CAS 有开销），高竞争时无锁通常更好。`,
      source: '大厂后端真题'
    },
    {
      id: 'os-057', category: 'os', tags: ['并发', '内存序'], difficulty: 4,
      question: '内存序（Memory Order）是什么？为什么需要它？',
      answer: `一句话结论：内存序规定多线程环境下内存读写操作的可见顺序，防止编译器和 CPU 的乱序优化破坏并发正确性；通过内存屏障保证「先于」关系。

【背景】编译器指令重排 + CPU 乱序执行会导致多线程下操作顺序与代码不一致。

【内存序级别（C++ 为例）】
- relaxed：仅保证原子性，不保证顺序；
- acquire：读操作，后续操作不能重排到它之前；
- release：写操作，之前操作不能重排到它之后；
- acq_rel：两者兼有；
- seq_cst：全序一致，最强但最慢。

【作用】保证跨线程的可见性与 happens-before 关系。

【面试追问】volatile 能保证内存序吗？→ Java volatile 保证可见性与有序性，但不保证原子性；C++ volatile 不等于内存序。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-058', category: 'os', tags: ['并发', 'RCU'], difficulty: 4,
      question: 'RCU（Read-Copy-Update）是什么？适用什么场景？',
      answer: `一句话结论：RCU 是一种「读多写少」的同步机制，读者无锁无开销地访问旧数据，写者复制一份新数据修改后原子替换指针，旧数据延迟回收。

【原理】
1. 写者复制数据副本；
2. 在副本上修改；
3. 原子更新指针指向新副本；
4. 等所有读者退出临界区后回收旧数据。

【特点】
- 读者完全无锁、零开销，可并发读；
- 写者较慢（复制+延迟回收）；
- 旧数据在宽限期（grace period）后回收。

【适用场景】读极多、写极少，如内核路由表、配置、读多写少的数据结构。

【面试追问】RCU 为什么读者不需要锁？→ 因为读者始终访问一致的旧数据，写者通过替换指针而非原地修改。`,
      source: '大厂内核真题'
    },
    {
      id: 'os-059', category: 'os', tags: ['内核', '同步'], difficulty: 4,
      question: 'Linux 内核同步机制有哪些？自旋锁、信号量、互斥体有什么区别？',
      answer: `一句话结论：内核同步有原子操作、自旋锁、读写锁、信号量、互斥体、RCU、完成量等；自旋锁用于原子上下文/短临界区，信号量和互斥体用于可睡眠的长临界区。

【自旋锁】忙等、不可睡眠、适合中断上下文与极短临界区。

【信号量 semaphore】可睡眠、可计数、适合长临界区。

【互斥体 mutex】可睡眠、二值、有优先级继承与调试支持，适合进程上下文。

【对比】
- 自旋锁：中断/原子上下文可用，信号量/互斥体不能睡眠；
- 互斥体比信号量语义更清晰、推荐用于互斥。

【面试追问】为什么中断上下文不能用信号量？→ 中断上下文不能睡眠，否则会调度导致系统状态错乱。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-060', category: 'os', tags: ['线程', '模型'], difficulty: 4,
      question: '用户态线程和内核态线程的区别？1:1、N:1、M:N 模型是什么？',
      answer: `一句话结论：内核线程由内核调度、每个线程都是独立调度单位；用户线程由用户态库管理、内核不感知；对应 1:1、N:1、M:N 三种映射模型。

【内核线程】内核创建调度、开销大、可并行利用多核。

【用户线程】用户库创建切换、开销小、但一个线程阻塞会阻塞同组（N:1 时）。

【映射模型】
- 1:1：一个用户线程对应一个内核线程，可并行，但创建开销大（Linux pthread）；
- N:1：多用户线程映射一个内核线程，轻量但无法并行、一阻塞全阻塞（协程早期）；
- M:N：M 个用户线程映射 N 个内核线程，兼顾并行与轻量（Go 的 GMP 思想）。

【面试追问】Go 协程属于哪种？→ M:N 模型，用户态 goroutine 由 runtime 调度到内核线程。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-061', category: 'os', tags: ['排查', 'CPU'], difficulty: 3,
      question: '线上服务器 CPU 飙到 100%，如何排查？',
      answer: `一句话结论：先用 top 定位占用 CPU 的进程，再用 top -H 或 pidstat 定位到具体线程，最后用 jstack/gdb/perf 分析线程在做什么代码。

【排查步骤】
1. top：找 CPU 最高的进程 PID；
2. top -Hp PID 或 pidstat -t：找最高的线程 TID；
3. 把 TID 转十六进制；
4. jstack PID（Java）或 gdb -p PID：查线程栈，定位代码；
5. 分析是死循环、频繁 GC、还是正常高负载。

【可能原因】死循环、频繁 GC、正则回溯、锁竞争、大量计算、异常流量。

【面试追问】非 Java 进程如何看？→ perf top 看热点函数、gdb attach、pstack 打栈。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-062', category: 'os', tags: ['排查', '内存'], difficulty: 3,
      question: '线上内存占用持续升高（疑似内存泄漏），如何排查？',
      answer: `一句话结论：先用 free/top 观察内存趋势，再按进程用 ps/pidstat 定位，接着看进程内存分布（堆、栈、代码），Java 用 heap dump、C++ 用 valgrind/asan 分析泄漏点。

【步骤】
1. free -m：看内存总量、used、cache 变化；
2. top 按内存排序找进程；
3. 观察是否持续增长（泄漏 vs 正常缓存）；
4. 确认是堆、栈还是 mmap 增长（/proc/PID/maps、smaps）；
5. 按语言分析：Java 用 jmap/jprofiler，C++ 用 valgrind/AddressSanitizer。

【常见原因】未释放对象、集合持续增长、缓存无上限、连接/线程泄漏。

【面试追问】如何区分是缓存还是泄漏？→ 触发回收或压测后看内存是否回落，缓存可回收、泄漏不可。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-063', category: 'os', tags: ['性能', 'load'], difficulty: 3,
      question: 'load average 是什么？如何解读它？',
      answer: `一句话结论：load average 是系统 1/5/15 分钟平均的「可运行 + 不可中断睡眠」进程数，反映系统忙碌程度；接近或超过 CPU 核数通常说明负载过重。

【含义】统计处于 R（运行/就绪）和 D（不可中断 IO）状态的进程数。

【解读】
- 值 < 核数：负载正常；
- 值 ≈ 核数：满负荷；
- 值 > 核数：过载，有进程排队。

【注意】
- 高 load 不一定 CPU 高，可能是大量进程在等待 IO（D 状态）；
- 需结合 CPU 使用率、IO 一起看。

【面试追问】load 很高但 CPU 空闲，可能是什么？→ 大量进程卡在不可中断 IO（如磁盘/网络阻塞）。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-064', category: 'os', tags: ['内存', 'free'], difficulty: 3,
      question: 'free 命令各字段的含义？buffer 和 cache 有什么区别？',
      answer: `一句话结论：free 展示 total（总量）、used（已用）、free（空闲）、shared、buff/cache、available（可用）；buffer 缓存块设备元数据，cache 缓存文件页。

【字段】
- total：物理内存总量；
- used：已使用（不含 buff/cache）；
- free：完全空闲；
- buff/cache：内核缓存，可回收；
- available：真正可分配给进程的内存（比 free 更准确）。

【buffer vs cache】
- buffer：块设备读写缓冲（如元数据、文件系统结构）；
- cache：页缓存，缓存文件内容。

【面试追问】为什么 used 高但 available 也高？→ 大部分被 buff/cache 占用，可回收，available 才代表真实可用。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-065', category: 'os', tags: ['性能', 'vmstat'], difficulty: 3,
      question: 'vmstat 和 iostat 如何用于性能排查？',
      answer: `一句话结论：vmstat 看 CPU、内存、进程、swap、IO 的整体情况，iostat 深入看磁盘 IO 的吞吐、延迟与利用率，两者结合定位性能瓶颈。

【vmstat 关注】
- r：运行队列，持续大于核数说明 CPU 瓶颈；
- b：阻塞进程；
- si/so：swap 换入换出，非零说明内存不足；
- us/sy/id/wa：用户/系统/空闲/IO 等待 CPU 占比。

【iostat 关注】
- %util：磁盘忙比例，接近 100% 是 IO 瓶颈；
- r/s、w/s：读写 IOPS；
- await：平均 IO 等待时间；
- rkB/s、wkB/s：吞吐。

【排查思路】wa 高 → 看 iostat 是否磁盘饱和；si/so 高 → 内存不足；r 高 → CPU 不足。

【面试追问】%util 100% 一定有问题吗？→ SSD 或 RAID 并发下 util 高不一定瓶颈，结合 await 和吞吐判断。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-066', category: 'os', tags: ['排查', 'strace', 'perf'], difficulty: 4,
      question: 'strace 和 perf 分别用于什么场景？',
      answer: `一句话结论：strace 跟踪进程的系统调用与信号，用于排查 IO、文件、网络等交互问题；perf 是性能剖析工具，采样热点函数与事件，用于定位 CPU 性能瓶颈。

【strace】
- 显示每次系统调用、参数、返回值、耗时；
- 场景：卡顿、打开文件失败、网络调用慢、权限问题；
- 例：strace -p PID 或 strace -c 统计调用耗时。

【perf】
- 基于硬件计数器采样；
- perf top 实时看热点函数；
- perf record + perf report 分析；
- 场景：CPU 高、缓存未命中、函数级瓶颈。

【区别】strace 看「与内核交互」，perf 看「CPU 时间花在哪」。

【面试追问】strace 会拖慢程序吗？→ 会，每次系统调用都拦截，生产慎用，可短时间抓取。`,
      source: '大厂运维真题'
    },
    {
      id: 'os-067', category: 'os', tags: ['进程', '守护进程'], difficulty: 3,
      question: '什么是守护进程（Daemon）？如何创建？',
      answer: `一句话结论：守护进程是脱离终端、在后台长期运行的进程（如 sshd、nginx）；创建需 fork、setsid 脱离会话、改变工作目录、重定向标准 IO 等步骤。

【创建步骤】
1. fork 出子进程，父进程退出（子进程不是组长）；
2. 子进程 setsid() 创建新会话、脱离终端；
3. 再次 fork 防止重新获取终端；
4. chdir 到根目录；
5. 重定向标准输入输出错误到 /dev/null 或日志；
6. 设置 umask。

【特点】
- 无控制终端；
- 常驻后台；
- 由 init/systemd 管理。

【面试追问】为什么 setsid？→ 让进程成为新会话首进程、脱离原终端，避免受终端关闭影响。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-068', category: 'os', tags: ['Linux', 'systemd'], difficulty: 2,
      question: 'systemd 和 systemctl 是什么？',
      answer: `一句话结论：systemd 是 Linux 的初始化系统与服务管理器（PID 1），负责启动并管理服务、依赖、日志等；systemctl 是管理 systemd 的命令行工具。

【systemd 功能】
- 系统启动、服务并行启动；
- 服务依赖与顺序；
- 统一管理（service、socket、timer 等 unit）；
- 日志（journalctl）。

【systemctl 常用】
- systemctl start/stop/restart/status 服务；
- systemctl enable/disable 服务（开机自启）；
- systemctl list-units 查看。

【面试追问】systemd 相比传统 init 优势？→ 并行启动更快、依赖管理、统一配置、进程监控与自愈。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-069', category: 'os', tags: ['Linux', '环境变量'], difficulty: 2,
      question: '环境变量 PATH 的作用是什么？',
      answer: `一句话结论：PATH 是一组目录列表，shell 在用户输入命令时按顺序在这些目录中查找可执行文件；找不到则报 command not found。

【作用】让用户无需输入绝对路径即可执行命令。

【特点】
- 用冒号分隔多个目录；
- 从左到右依次查找，先找到的优先；
- 通过 export PATH=... 修改。

【面试追问】为什么装了程序却 command not found？→ 程序不在 PATH 目录，需把安装目录加入 PATH。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-070', category: 'os', tags: ['Linux', 'crontab'], difficulty: 2,
      question: 'crontab 定时任务如何配置？',
      answer: `一句话结论：crontab 用于设置周期性执行的定时任务，用「分 时 日 月 周」五段表达式指定时间；编辑用 crontab -e，查看用 crontab -l。

【格式】分(0-59) 时(0-23) 日(1-31) 月(1-12) 周(0-7) 命令。

【示例】
- 每分钟：星号 星号 星号 星号 星号（每段都是星号）；
- 每天凌晨 3 点：0 3 星号 星号 星号；
- 每 5 分钟：星号/5 星号 星号 星号 星号。

【注意】
- 需确认 cron 服务运行；
- 命令需绝对路径或设 PATH；
- 输出可重定向到日志。

【面试追问】crontab 和 systemd timer 区别？→ timer 更强大，支持依赖、日志、秒级，现代推荐 timer。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-071', category: 'os', tags: ['Linux', '权限'], difficulty: 2,
      question: 'Linux 文件权限和 chmod 如何理解？',
      answer: `一句话结论：Linux 权限分属主、属组、其他三类，每类有读(r)、写(w)、执行(x)三种；chmod 用于修改权限，可用数字或符号方式。

【权限表示】如 -rwxr-xr--，首字符类型，后 9 位分三组（属主/属组/其他）。

【数字】r=4、w=2、x=1，相加组合，如 755 = rwxr-xr-x，644 = rw-r--r--。

【chmod 用法】
- chmod 755 file：数字方式；
- chmod u+x file：给属主加执行权限；
- chmod -R 递归。

【特殊位】suid、sgid、sticky bit。

【面试追问】目录的执行权限含义？→ 对目录，x 表示可进入（cd），r 表示可列出内容。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-072', category: 'os', tags: ['IPC', '通信'], difficulty: 3,
      question: 'IPC 方式对比：管道、消息队列、共享内存、信号、套接字各有什么特点？',
      answer: `一句话结论：管道是单向字节流、消息队列是有边界的消息、共享内存是速度最快的零拷贝共享、信号是异步通知、套接字支持跨主机；共享内存最快但需配合同步。

【管道 pipe】单向、父子/兄弟进程间，字节流无边界；命名管道可跨进程。

【消息队列】有边界的消息，支持类型选择，异步解耦，但消息有大小限制。

【共享内存】多进程映射同一内存，速度最快（无内核拷贝），但需信号量/锁同步，最复杂。

【信号 signal】异步通知，携带信息少，如 SIGINT/SIGKILL。

【套接字 socket】可跨主机、双向，通用但开销相对大。

【面试追问】为什么共享内存最快？→ 数据直接读写内存，无需内核拷贝与系统调用开销。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-073', category: 'os', tags: ['容器', '虚拟化'], difficulty: 3,
      question: '容器和虚拟机有什么区别？',
      answer: `一句话结论：虚拟机在硬件层虚拟出完整操作系统，隔离强但开销大；容器共享宿主机内核、隔离用户空间，轻量启动快但隔离性弱。

【虚拟机】
- Hypervisor 虚拟 CPU/内存等硬件；
- 每台有独立完整 OS；
- 隔离强、启动慢、资源开销大。

【容器】
- 共享宿主机内核；
- 用 namespace 隔离进程/网络/挂载，cgroup 限制资源；
- 启动快（秒级）、开销小、密度高。

【对比】隔离性 VM 强于容器；轻量与效率容器强于 VM。

【面试追问】容器共享内核有什么风险？→ 内核漏洞或内核 panic 可能影响所有容器。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-074', category: 'os', tags: ['容器', 'namespace', 'cgroup'], difficulty: 4,
      question: 'namespace 和 cgroup 的原理是什么？',
      answer: `一句话结论：namespace 提供「资源隔离」，让不同进程看到独立的 PID、网络、挂载等视图；cgroup 提供「资源限制」，控制 CPU、内存等使用量，两者是容器技术的基石。

【namespace 类型】
- PID：隔离进程号；
- NET：隔离网络栈（独立 IP、端口）；
- MNT：隔离挂载点；
- UTS：隔离主机名；
- IPC/USER：隔离通信与用户。

【cgroup 功能】
- 限制 CPU、内存、IO、网络等资源；
- 按组统计与控制；
- 层级化组织。

【容器 = namespace（隔离）+ cgroup（限制）+ 联合文件系统（镜像层）。

【面试追问】docker 如何限制内存？→ 通过 cgroup 设置 memory.limit，超限触发 OOM。`,
      source: '大厂云原生真题'
    },
    {
      id: 'os-075', category: 'os', tags: ['容器', '镜像'], difficulty: 3,
      question: 'Docker 镜像分层与写时复制是什么？',
      answer: `一句话结论：镜像由多层只读文件系统叠加构成，基于 UnionFS 实现；容器在镜像层之上加一层可写层，修改时用写时复制，共享不变的基础层以节省空间。

【分层结构】
- 每层对应一条 Dockerfile 指令；
- 层之间共享、复用，相同层只存一份；
- 不同镜像可共享基础层。

【写时复制】
- 容器启动加一个可写层；
- 读：从上层往下找；
- 写：复制到可写层再修改，底层只读不变。

【好处】镜像构建快、存储省、分发高效。

【面试追问】为什么分层能加快构建？→ 未变的层直接用缓存，只重建变化层。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-076', category: 'os', tags: ['系统调用', '内核'], difficulty: 3,
      question: '系统调用的过程是怎样的？如何从用户态切换到内核态？',
      answer: `一句话结论：用户程序通过系统调用指令（如 int 0x80、syscall）触发软中断/异常进入内核态，内核根据系统调用号执行对应内核函数后返回用户态。

【切换途径】
1. 系统调用（主动）：用户程序请求内核服务；
2. 中断（被动）：硬件事件；
3. 异常：如缺页、除零。

【系统调用流程】
1. 用户态把系统调用号与参数放入寄存器；
2. 执行 syscall/int 指令，陷入内核；
3. CPU 切换到内核栈、特权级提升；
4. 内核按调用号查系统调用表执行；
5. 返回用户态。

【为什么区分】保护内核不被用户程序破坏，安全隔离。

【面试追问】系统调用开销大在哪？→ 特权级切换、保存/恢复上下文、可能缓存失效。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-077', category: 'os', tags: ['信号', '进程'], difficulty: 3,
      question: '什么是信号（Signal）？常见信号有哪些？',
      answer: `一句话结论：信号是内核或进程发给进程的异步通知，用于告知某事件发生；进程可捕获处理、忽略或按默认动作响应。

【常见信号】
- SIGINT（2）：Ctrl+C 中断；
- SIGKILL（9）：强制杀死，不可捕获；
- SIGTERM（15）：终止，可捕获优雅退出；
- SIGSEGV（11）：段错误；
- SIGHUP（1）：终端挂断/重载配置；
- SIGCHLD（17）：子进程状态变化。

【处理方式】默认动作、忽略、自定义 handler。

【面试追问】SIGKILL 和 SIGTERM 区别？→ SIGTERM 可被捕获做清理后退出，SIGKILL 立即强杀无法捕获。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-078', category: 'os', tags: ['进程', 'fork'], difficulty: 3,
      question: 'fork、exec、wait 分别做什么？它们如何配合？',
      answer: `一句话结论：fork 创建子进程（复制父进程），exec 用新程序替换当前进程映像，wait 让父进程等待子进程结束并回收；三者配合实现「创建进程运行新程序」。

【fork】返回两次（父得子 PID、子得 0），子进程是父进程副本（写时复制）。

【exec】不创建新进程，用新程序代码/数据替换当前进程，PID 不变。

【wait】父进程阻塞等待子进程终止，回收其资源（避免僵尸进程）。

【配合】父 fork 出子进程 → 子进程 exec 运行新程序 → 父 wait 回收。

【面试追问】为什么 fork 后要 exec？→ fork 复制父进程，exec 换成真正要跑的程序，两者结合创建不同进程。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-079', category: 'os', tags: ['进程', '会话'], difficulty: 3,
      question: '什么是进程组、会话？它们和终端有什么关系？',
      answer: `一句话结论：进程组是一组相关进程的集合（用于作业控制和信号广播），会话是一个或多个进程组的集合（关联一个控制终端）；两者用于终端的进程管理。

【进程组】
- 一组相关进程，有组 ID（PGID）；
- 用于向整组发送信号（如 Ctrl+C 发 SIGINT 给前台进程组）；
- 组长进程的 PID 即 PGID。

【会话】
- 多个进程组的集合，有会话首进程；
- 关联一个控制终端；
- 一个终端最多一个前台进程组。

【关系】终端 → 会话 → 前台/后台进程组 → 进程。

【面试追问】setsid 的作用？→ 创建新会话，使进程脱离原控制终端，守护进程常用。`,
      source: '大厂内核真题'
    },
    {
      id: 'os-080', category: 'os', tags: ['性能', '缓存行'], difficulty: 4,
      question: '缓存行（Cache Line）是什么？什么是缓存友好编程？',
      answer: `一句话结论：缓存行是 CPU 缓存与内存交换数据的最小单位（通常 64 字节），一次加载整个缓存行；缓存友好编程通过连续访问、避免伪共享来提升缓存命中。

【缓存行】CPU 按行缓存，访问某地址会把同行相邻数据一起加载。

【缓存友好编程】
1. 顺序访问数组（空间局部性）；
2. 复用近期数据（时间局部性）；
3. 结构体字段按访问热度排列；
4. 避免伪共享（不同线程频繁写的变量放同一缓存行）。

【伪共享】多核下两个线程写同一缓存行不同变量，导致缓存行反复失效、性能骤降。

【面试追问】如何避免伪共享？→ 变量填充对齐到独立缓存行（如 C++ alignas(64)、Java @Contended）。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-081', category: 'os', tags: ['性能', 'CPU亲和性'], difficulty: 3,
      question: 'CPU 亲和性（Affinity）是什么？有什么用？',
      answer: `一句话结论：CPU 亲和性把进程/线程绑定到指定的 CPU 核上运行，减少跨核调度带来的缓存失效与迁移开销，提升性能稳定。

【原理】通过 sched_setaffinity 或 taskset 指定进程可运行的 CPU 集合。

【好处】
- 提高缓存命中（数据留在固定核缓存）；
- 减少任务迁移开销；
- 性能更稳定可预测。

【风险】
- 绑定不当导致负载不均；
- 降低调度灵活性。

【场景】性能敏感服务、实时任务、NUMA 优化。

【面试追问】NUMA 下亲和性要注意什么？→ 绑定到同 NUMA 节点的核，避免跨节点访存延迟高。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-082', category: 'os', tags: ['NUMA', '性能'], difficulty: 4,
      question: '什么是 NUMA？它对性能有什么影响？',
      answer: `一句话结论：NUMA（非一致性内存访问）把内存划分为多个节点、每个节点绑定部分 CPU，访问本地内存快、访问远端内存慢；影响是跨节点访存延迟高，需优化亲和性。

【背景】多核系统所有 CPU 共享总线成为瓶颈，NUMA 让每个 CPU 有本地内存。

【特点】
- 本地内存访问快；
- 远端内存访问慢（需经互联总线）；
- 有 NUMA 节点概念。

【影响】进程的内存与 CPU 不在同一节点时性能下降。

【优化】
- CPU 亲和性绑定同节点；
- 内存就近分配（numactl）；
- 避免跨节点频繁访问共享数据。

【面试追问】如何查看 NUMA 拓扑？→ numactl --hardware 或 lscpu。`,
      source: '大厂内核真题'
    },
    {
      id: 'os-083', category: 'os', tags: ['字节序', '大小端'], difficulty: 2,
      question: '大端（Big-endian）和小端（Little-endian）是什么？',
      answer: `一句话结论：字节序指多字节数据在内存中的存放顺序；大端「高位字节存低地址」，小端「低位字节存低地址」，网络协议用大端、x86 用小端。

【大端】数据的高位字节放在内存低地址，符合人类阅读习惯。

【小端】数据的低位字节放在内存低地址，x86/ARM 主流用小端。

【举例】0x12345678 在内存：大端 12 34 56 78，小端 78 56 34 12。

【影响】
- 网络传输统一用大端（网络字节序）；
- 跨平台数据交换需转换（htonl/ntohl）。

【面试追问】如何判断机器大小端？→ 用 union 或指针读一个 int 的低字节判断。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-084', category: 'os', tags: ['内存', '抖动'], difficulty: 3,
      question: '什么是系统抖动（Thrashing）？如何避免？',
      answer: `一句话结论：抖动是系统内存不足、频繁在内存与磁盘间换页，导致 CPU 大量时间花在换页上、吞吐骤降；通过增加内存、限制并发进程数、优化局部性避免。

【表现】换页活动频繁、CPU 利用率低但磁盘 IO 高、系统响应极慢。

【原因】并发进程过多、每个进程工作集超过可用内存。

【避免】
1. 增加物理内存；
2. 降低多道程序度（限制并发）；
3. 优化程序局部性；
4. 工作集模型分配内存。

【面试追问】如何判断是否抖动？→ 观察 swap in/out 频繁（vmstat si/so 高）且 CPU 利用率低。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-085', category: 'os', tags: ['调度', '多级反馈'], difficulty: 4,
      question: '时间片轮转和多级反馈队列调度分别是什么？',
      answer: `一句话结论：时间片轮转让就绪进程轮流运行固定时间片，适合分时系统；多级反馈队列设多个优先级队列、时间片逐级增大，兼顾短作业响应与长作业公平。

【时间片轮转 RR】
- 就绪队列循环，每个进程运行一个时间片；
- 时间片过长退化 FCFS、过短切换开销大。

【多级反馈队列 MLFQ】
- 多个优先级队列，高优先级时间片短；
- 新进程进最高队列，用完后降级；
- 低优先级队列时间片长；
- 有进程等待则抢占，兼顾交互与吞吐。

【优点】无需预知进程时长，自适应，是现代调度基础。

【面试追问】MLFQ 如何防饥饿？→ 周期性把所有进程提升到最高优先级（优先级提升）。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-086', category: 'os', tags: ['调度', '实时'], difficulty: 3,
      question: '实时操作系统和分时操作系统有什么区别？',
      answer: `一句话结论：实时系统强调「在规定时间内完成」，保证截止时间与确定性；分时系统强调「公平分享 CPU」，追求交互响应与吞吐。

【实时系统】
- 硬实时：必须在截止时间内完成，否则严重错误（工控、航空）；
- 软实时：尽量满足截止时间，偶尔超时可容忍（音视频）；
- 调度基于优先级与截止时间（EDF、RMS）。

【分时系统】
- 时间片轮转，公平分配；
- 追求整体吞吐与平均响应；
- 无严格时间保证。

【面试追问】Linux 是实时系统吗？→ 普通 Linux 是分时系统，有 PREEMPT_RT 补丁增强实时性。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-087', category: 'os', tags: ['fork', '写时复制'], difficulty: 3,
      question: 'fork 的写时复制如何避免全量内存拷贝？',
      answer: `一句话结论：fork 后父子进程共享同一份物理内存，页表项标记为只读，只有某方写入时才触发缺页异常、复制该页，未写的页始终共享，从而避免全量拷贝。

【流程】
1. fork 不复制物理内存，只复制页表，所有页标记只读；
2. 父子任一方写入触发写保护缺页；
3. 内核分配新物理页、复制内容、更新该方页表为可写；
4. 只读页继续共享。

【收益】fork 从「拷贝全部内存」变为「拷贝页表」，速度极快，内存按需复制。

【面试追问】exec 后还需要写时复制吗？→ exec 替换地址空间，之前的共享页直接丢弃，写时复制主要为 fork 不 exec 的场景服务。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-088', category: 'os', tags: ['内存', 'valgrind'], difficulty: 3,
      question: 'valgrind 排查内存泄漏的原理是什么？',
      answer: `一句话结论：valgrind 通过动态二进制插桩，在程序运行时跟踪每次内存分配与释放，记录哪些内存未释放、是否越界，从而检测内存泄漏与非法访问。

【原理】
- 不重新编译，在机器码层面插桩；
- 维护内存分配/释放的影子状态；
- 检测泄漏（分配未释放）、非法读写、未初始化使用等。

【工具】memcheck 最常用，检测泄漏与越界。

【缺点】程序运行慢 10-50 倍，适合测试环境。

【面试追问】生产环境如何替代 valgrind？→ 用 AddressSanitizer（编译期插桩）或 perf/jemalloc 的统计。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-089', category: 'os', tags: ['内存', '隔离'], difficulty: 3,
      question: '用户态内存和内核态内存是如何隔离的？',
      answer: `一句话结论：通过 CPU 特权级与页表权限位隔离——内核空间页仅内核态可访问，用户态访问会触发保护异常，防止用户程序破坏内核。

【机制】
1. CPU 特权级（ring0 内核、ring3 用户）；
2. 虚拟地址空间划分：高地址为内核空间、低地址为用户空间；
3. 页表项有 U/S 位，内核页标记仅特权级访问；
4. 用户态访问内核地址触发缺页/保护异常。

【作用】安全隔离、保护内核数据与代码。

【面试追问】系统调用如何跨越隔离？→ 通过陷入内核态，内核代用户执行特权操作。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-090', category: 'os', tags: ['内核', '抢占'], difficulty: 4,
      question: '内核抢占（Preemption）是什么？',
      answer: `一句话结论：内核抢占指正在内核态运行的进程可被更高优先级任务打断并让出 CPU；它提升实时响应，但增加同步复杂度。

【非抢占内核】进程在内核态运行必须主动返回用户态或阻塞才切换，响应慢。

【抢占内核】满足条件（如不在临界区）时可被抢占，响应快。

【条件】Linux 2.6 起支持抢占，通过 preempt_count 判断是否可抢占（持锁、关中断时不可）。

【面试追问】为什么持自旋锁时不能抢占？→ 抢占后新进程可能也申请该锁导致死锁，自旋锁会关闭抢占。`,
      source: '大厂内核真题'
    },
    {
      id: 'os-091', category: 'os', tags: ['内核', '中断上下文'], difficulty: 4,
      question: '什么是中断上下文？为什么它不能睡眠？',
      answer: `一句话结论：中断上下文是内核处理中断时的一种特殊执行环境，没有独立的进程上下文可调度；睡眠会导致当前 CPU 无法继续处理中断、系统状态错乱，因此禁止睡眠。

【中断上下文特点】
- 无关联进程、无独立任务结构；
- 不能睡眠、不能调用可能阻塞的函数；
- 需快速完成，耗时工作交给下半部。

【为什么不能睡眠】
- 睡眠意味着让出 CPU、等待调度，但中断上下文没有可恢复的任务；
- 可能死锁或延误关键处理。

【面试追问】中断处理耗时怎么办？→ 把耗时工作放到下半部（软中断、工作队列）执行。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-092', category: 'os', tags: ['内核', '下半部'], difficulty: 4,
      question: '中断下半部：softirq、tasklet、工作队列有什么区别？',
      answer: `一句话结论：三者都是把中断的耗时工作延后处理；softirq 最快但需静态定义、tasklet 基于 softirq 更易用、工作队列运行在进程上下文可睡眠但最慢。

【softirq】
- 静态定义、内核编译期确定；
- 可并发运行在不同 CPU、性能最高；
- 需自处理并发，编写复杂。

【tasklet】
- 基于 softirq 的动态封装；
- 同一 tasklet 不会并发、更简单；
- 不能睡眠。

【工作队列 workqueue】
- 运行在进程上下文、可睡眠；
- 延迟高但灵活，适合耗时操作。

【面试追问】如何选择？→ 极高频、性能敏感用 softirq；一般用 tasklet；需睡眠/耗时长用工作队列。`,
      source: '大厂内核真题'
    },
    {
      id: 'os-093', category: 'os', tags: ['内存', '页表项'], difficulty: 4,
      question: '页表项（PTE）的 dirty 位和 accessed 位有什么用？',
      answer: `一句话结论：accessed 位记录页是否被访问过（用于 LRU 页面置换），dirty 位记录页是否被写过（用于判断写回磁盘），由硬件自动置位。

【accessed 位】
- 页被访问时硬件置 1；
- 内核用于近似 LRU：定期清零，看是否再次被访问判断活跃度。

【dirty 位】
- 页被写时硬件置 1；
- 表示内存与磁盘不一致；
- 换出时若 dirty 需写回磁盘，否则直接丢弃。

【作用】支撑页面置换与页缓存回写。

【面试追问】为什么需要近似 LRU？→ 精确 LRU 开销大，用 accessed 位实现二次机会等近似算法。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-094', category: 'os', tags: ['并发', '内存屏障'], difficulty: 4,
      question: '内存屏障的 acquire/release 语义是什么？',
      answer: `一句话结论：acquire 屏障保证「其后的操作不能重排到它之前」，用于读共享变量；release 屏障保证「其前的操作不能重排到它之后」，用于写共享变量。

【acquire】
- 读操作前/后屏障：acquire 之后的读写不能提前到 acquire 之前；
- 常配合 load，保证读到最新值后，后续操作能看到其副作用。

【release】
- 写操作屏障：release 之前的读写不能推迟到 release 之后；
- 常配合 store，保证之前操作的结果对后续读者可见。

【配对】线程 A release 写，线程 B acquire 读，构成 happens-before，保证可见性。

【面试追问】full barrier 是什么？→ 双向屏障，前后都不可跨越，最强也最慢。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-095', category: 'os', tags: ['线程', 'pthread'], difficulty: 3,
      question: 'pthread 和 NPTL 是什么？',
      answer: `一句话结论：pthread 是 POSIX 线程库的标准 API，用于创建和管理线程；NPTL 是 Linux 上 pthread 的实现，基于 1:1 模型、每个用户线程对应一个内核线程。

【pthread API】
- pthread_create 创建线程；
- pthread_join 等待线程；
- pthread_mutex_lock 加锁等。

【NPTL（Native POSIX Thread Library）】
- Linux 线程库实现；
- 1:1 模型，用户线程即内核线程（轻量进程）；
- 支持大规模线程、与内核同步机制高效对接。

【面试追问】NPTL 相比旧 LinuxThreads 优势？→ 真正的 1:1 模型、更好性能与 POSIX 兼容性、支持信号与取消语义。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-096', category: 'os', tags: ['调度', 'nice'], difficulty: 3,
      question: 'Linux 的 nice 值和优先级是什么关系？',
      answer: `一句话结论：nice 值（-20 到 19）表示进程的友好程度，值越小优先级越高、获得 CPU 越多；它通过影响 CFS 的权重来调节调度。

【nice 范围】
- -20 最高优先级，19 最低；
- 默认 0；
- 普通用户只能调高（降低优先级），root 可调低。

【与权重关系】nice 每变化一档，CFS 权重按约 1.25 倍增减，影响 vruntime 增长速度。

【命令】nice 启动时设置，renice 修改运行中进程。

【面试追问】nice 和实时优先级区别？→ nice 是普通进程的相对优先级，实时进程用独立的实时优先级（1-99），始终高于普通进程。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-097', category: 'os', tags: ['容器', 'cgroup'], difficulty: 4,
      question: 'cgroup v1 和 v2 有什么区别？',
      answer: `一句话结论：cgroup v1 每个子系统独立层级、管理分散；cgroup v2 统一单一层级，一个进程只属于一个层级，更简洁一致，是当前主流与未来方向。

【v1 特点】
- 各子系统（cpu、memory 等）独立挂载；
- 可分别建立不同层级；
- 灵活但复杂、规则不一致。

【v2 特点】
- 统一单一层级，所有控制器挂一起；
- 一个进程只在一个层级；
- 支持更细粒度资源控制（如 memory 下的 swap、PSI 压力统计）。

【趋势】Kubernetes、systemd 逐渐默认 v2。

【面试追问】为什么迁移 v2？→ 简化管理、避免 v1 的多层级不一致与规则混乱。`,
      source: '大厂云原生真题'
    },
    {
      id: 'os-098', category: 'os', tags: ['文件系统', 'overlay'], difficulty: 4,
      question: 'overlay 文件系统是什么？容器如何用它？',
      answer: `一句话结论：overlay 文件系统把多个目录（层）叠加成一个合并视图，下层只读、上层可写，是 Docker 镜像分层存储的底层实现。

【组成】
- lowerdir：只读底层（镜像层）；
- upperdir：可写上层（容器层）；
- merged：合并视图；
- 写时复制：修改 lower 文件时复制到 upper。

【容器应用】
- 镜像各层作为 lowerdir 叠加；
- 容器可写层为 upperdir；
- 多个容器共享只读镜像层。

【优点】共享基础层省空间、构建快、分发高效。

【面试追问】overlay 和 aufs 区别？→ 都是联合文件系统，overlay 更简洁且已进主线内核，是主流。`,
      source: '面试鸭高频'
    },
    {
      id: 'os-099', category: 'os', tags: ['Linux', '启动'], difficulty: 3,
      question: 'Linux 系统的启动过程是怎样的？',
      answer: `一句话结论：Linux 启动从 BIOS/UEFI → 引导加载器（GRUB）→ 内核加载 → init/systemd 启动服务，逐步完成硬件初始化到用户空间服务启动。

【流程】
1. BIOS/UEFI：上电自检、加载引导程序；
2. GRUB：加载内核与 initramfs 到内存；
3. 内核启动：初始化硬件、挂载根文件系统（借助 initramfs 加载驱动）；
4. 启动 init（systemd，PID 1）；
5. systemd 启动各服务与登录。

【initramfs 作用】提供根文件系统挂载前所需的驱动与工具。

【面试追问】为什么需要 initramfs？→ 根文件系统可能在磁盘/网络/加密设备上，需先加载驱动才能挂载。`,
      source: '牛客面经高频'
    },
    {
      id: 'os-100', category: 'os', tags: ['内核', 'panic'], difficulty: 3,
      question: '什么是内核 panic？如何排查？',
      answer: `一句话结论：内核 panic 是内核遇到无法恢复的严重错误而主动停止系统，打印崩溃信息（调用栈、寄存器）后停机；排查靠分析崩溃日志、kdump 与现场。

【常见原因】
- 空指针/非法内存访问；
- 内核 bug 或驱动错误；
- 硬件故障（内存、CPU）；
- 资源耗尽导致内核无法运行。

【排查】
1. 查看 panic 时的调用栈定位出错模块；
2. 配置 kdump 保存崩溃转储（vmcore）；
3. 用 crash 工具分析 vmcore；
4. 结合内核日志、升级内核/驱动。

【面试追问】panic 和 oops 区别？→ oops 是局部错误（如某模块非法访问），系统可能继续运行；panic 是致命错误，系统停机。`,
      source: '大厂内核真题'
    }
  ];

  global.App = global.App || {};
  global.App.os2Bank = OS2;
})(window);

/**
 * OfferAgent · 扩充题库（操作系统 · 第三批：内存管理 / IO 模型 / 进程线程进阶 / Linux）
 * 挂载到 global.App.os3Bank，由 bank.js 加载时合并。
 */
(function (global) {
  'use strict';

  var OS3 = [
    {
      id: 'os-101', category: 'os', tags: ['进程', '上下文切换', '性能'], difficulty: 3,
      question: '进程上下文切换具体切换了什么？为什么开销大？如何减少？',
      answer: `一句话结论：上下文切换要保存/恢复 CPU 寄存器、程序计数器、栈指针、页表等执行现场；因为涉及内核态切换、缓存失效、TLB 刷新，所以开销大。

【切换内容】
1. CPU 寄存器（通用寄存器、程序计数器 PC、栈指针 SP、状态寄存器）；
2. 进程的地址空间信息（页表基址寄存器，如 CR3）；
3. 内核栈、调度信息等。

【为什么开销大】
1. 保存与恢复现场本身耗时；
2. 用户态 ↔ 内核态切换；
3. 切换后 CPU 缓存（L1/L2）大量失效；
4. 切换页表导致 TLB 失效（TLB 刷新），后续访存变慢。

【如何减少】
1. 减少进程/线程数量，避免频繁切换；
2. 用协程（用户态切换，不经过内核）替代线程；
3. 绑定 CPU 亲和性（taskset），减少跨核迁移；
4. 合并小任务，减少并发度。

【追问】线程切换比进程切换便宜在哪？→ 线程共享地址空间，切换时不需要切换页表，但仍要切寄存器/栈。

【避坑】「上下文切换」在不同语境（进程、线程、中断）含义有差异，但核心都是「保存现场 + 恢复现场」。`,
      source: '操作系统原理 / 大厂八股'
    },
    {
      id: 'os-102', category: 'os', tags: ['线程', '同步', '锁'], difficulty: 3,
      question: '线程同步的常用方式有哪些？互斥锁、自旋锁、读写锁、条件变量、信号量有何区别？',
      answer: `一句话结论：这些都是并发同步原语——互斥锁互斥访问临界区，自旋锁忙等不睡眠，读写锁区分读多写少，条件变量等待条件，信号量控制资源数量。

【各类对比】
1. 互斥锁（Mutex）：同一时刻只有一个线程持有，其他线程阻塞睡眠；适合临界区较长的场景；
2. 自旋锁（Spinlock）：忙等（while 循环）不释放 CPU，适合临界区极短、多核场景；不适合单核（浪费）；
3. 读写锁（RWLock）：读可并发、写互斥，适合读多写少；
4. 条件变量（Condition Variable）：配合互斥锁，让线程等待某个条件成立，适合「生产者-消费者」；
5. 信号量（Semaphore）：计数器，控制同时访问资源的线程数，可做互斥或限流。

【选择建议】
· 临界区短、多核 → 自旋锁；
· 读多写少 → 读写锁；
· 等待条件 → 条件变量；
· 限制并发数量 → 信号量。

【追问】自旋锁和互斥锁什么时候切换？→ 自旋一段时间后若仍拿不到锁，会退化为睡眠（如 Java 的锁升级）。

【避坑】条件变量必须配合互斥锁使用，且要用 while 而非 if 判断条件（防止虚假唤醒）。`,
      source: '操作系统原理 / 大厂八股'
    },
    {
      id: 'os-103', category: 'os', tags: ['并发', 'CAS', '锁'], difficulty: 3,
      question: 'CAS（Compare-And-Swap）的原理是什么？它有什么问题和应用？',
      answer: `一句话结论：CAS 是一条原子指令，比较内存值与期望值，相等则写入新值；它是无锁编程和乐观锁的基础。

【原理】
1. CAS(V, expected, newValue)：若 V 的当前值 == expected，则把 V 更新为 newValue，返回成功；否则失败；
2. 由 CPU 原子指令（如 cmpxchg）保证，无需加锁。

【典型应用】
1. Java 的 AtomicInteger、无锁队列；
2. 乐观锁：数据库的版本号/字段 CAS 更新；
3. 自旋锁的实现；
4. ConcurrentHashMap 等并发容器的核心。

【三大问题】
1. ABA 问题：值从 A 变 B 又变回 A，CAS 误以为没变；解决：加版本号（AtomicStampedReference）；
2. 自旋开销：CAS 失败时忙等，循环会耗 CPU；
3. 只能保证单个变量的原子性：多个变量需用锁或封装成对象。

【追问】CAS 和悲观锁的区别？→ CAS 是乐观的（先试，失败再重试/自旋），悲观锁直接阻塞，适合竞争不激烈的场景。

【避坑】高竞争下 CAS 自旋会退化，性能反而不如锁；要结合实际竞争程度选型。`,
      source: 'Java 并发 / CPU 原子指令'
    },
    {
      id: 'os-104', category: 'os', tags: ['虚拟内存', 'MMU', 'TLB'], difficulty: 3,
      question: '虚拟地址是如何翻译成物理地址的？（MMU、TLB、多级页表）',
      answer: `一句话结论：CPU 发出虚拟地址，MMU 通过页表把虚拟页号翻译成物理页号，加上页内偏移得到物理地址；TLB 缓存页表项加速翻译。

【翻译过程】
1. 虚拟地址 = 虚拟页号 VPN + 页内偏移 offset；
2. MMU 拿 VPN 查页表（多级页表），得到物理页号 PFN；
3. 物理地址 = PFN + offset；
4. 访问物理内存。

【多级页表】
1. 为了节省页表占用的内存，用多级页表（如 x86 的 4 级：PGD→PUD→PMD→PTE）；
2. 每级取 VPN 的一部分作为索引，逐级查找；
3. 只分配用到的页表，未映射的页表不占空间。

【TLB（快表）】
1. 缓存最近的「虚拟页 → 物理页」映射，加速翻译；
2. 命中则跳过页表查询，快几个数量级；
3. 进程切换时 TLB 需要刷新（或打标签）。

【缺页中断】若页表项不存在或没在内存，触发缺页中断，从磁盘调入页面。

【追问】为什么需要多级页表？→ 单级页表在 64 位地址空间下会占用巨量内存，多级按需分配可节省。

【避坑】TLB 命中率对性能影响巨大，访问局部性好（如顺序访问）能显著提高命中率。`,
      source: '操作系统原理 / 计算机体系结构'
    },
    {
      id: 'os-105', category: 'os', tags: ['内存', '缺页中断', '页表'], difficulty: 3,
      question: '什么是缺页中断？它的处理流程是怎样的？',
      answer: `一句话结论：当访问的页面不在物理内存时，触发缺页中断，操作系统从磁盘（或交换区）调入页面，再重新执行指令。

【触发条件】
1. 访问的虚拟页没有对应的物理页（未分配）；
2. 页面被换出到磁盘（swap）；
3. 访问权限不符（写只读页，触发保护异常）。

【处理流程】
1. CPU 发现缺页，触发缺页中断，陷入内核；
2. 保存现场，检查地址是否合法；
3. 若地址非法 → 段错误（segfault）终止进程；
4. 若合法：在物理内存找空闲页（无则触发页面置换）；
5. 从磁盘读入页面到空闲页；
6. 更新页表项，标记有效；
7. 恢复现场，重新执行引发缺页的指令。

【缺页类型】
1. 软缺页（minor fault）：页面在内存但未映射，无需磁盘 IO；
2. 硬缺页（major fault）：需从磁盘读入，开销大。

【追问】缺页中断和普通中断有何不同？→ 缺页中断是在指令执行中发生的，处理后要重新执行当前指令（不是下一条）。

【避坑】频繁缺页会导致「抖动」，性能骤降，需合理设置内存/swap 或优化程序内存使用。`,
      source: '操作系统原理 / Linux 内核'
    },
    {
      id: 'os-106', category: 'os', tags: ['IO', 'IO模型', '网络'], difficulty: 3,
      question: 'Linux 的五大 IO 模型分别是什么？（阻塞/非阻塞/多路复用/信号驱动/异步）',
      answer: `一句话结论：按「是否阻塞」和「谁负责拷贝」区分，五大模型是阻塞 IO、非阻塞 IO、IO 多路复用、信号驱动 IO、异步 IO。

【五大模型】
1. 阻塞 IO：进程调用 recv 后阻塞，直到数据到达并拷贝完成；
2. 非阻塞 IO：recv 立即返回（无数据返回 EWOULDBLOCK），需轮询，不阻塞但浪费 CPU；
3. IO 多路复用：用 select/poll/epoll 同时监听多个 fd，有就绪再处理，一个线程管理多个连接；
4. 信号驱动 IO：数据就绪时内核发 SIGIO 信号通知，然后进程再 recv；
5. 异步 IO（AIO）：进程发起 aio_read 后直接返回，内核完成数据拷贝后再通知进程，全程不阻塞。

【关键区别】
· 前四种在「数据拷贝」阶段都阻塞，只有异步 IO 拷贝也不阻塞；
· 多路复用是「一个线程等多个 fd 就绪」，不是真正的异步。

【应用】
· Nginx、Redis 用 IO 多路复用（epoll）；
· 高性能场景选 epoll + 非阻塞；
· AIO 适合磁盘 IO 密集（如数据库）。

【追问】为什么 epoll 比 select/poll 好？→ epoll 基于事件驱动，fd 多时不用遍历全部，且用红黑树 + 就绪链表，O(1) 获取就绪事件。

【避坑】「非阻塞」不等于「异步」；异步 IO 是内核完成拷贝后通知，多数场景「多路复用 + 非阻塞」已足够。`,
      source: 'UNIX 网络编程 / 大厂八股'
    },
    {
      id: 'os-107', category: 'os', tags: ['缓存', '伪共享', '性能'], difficulty: 3,
      question: '什么是缓存行和伪共享（False Sharing）？如何用填充解决？',
      answer: `一句话结论：缓存行是 CPU 缓存与内存交换数据的最小单位（通常 64 字节）；伪共享是多个线程修改同一缓存行内的不同变量，导致缓存一致性协议频繁失效。

【缓存行 Cache Line】
1. CPU 以缓存行为单位加载数据，通常 64 字节；
2. 即使只访问一个变量，也会加载整行。

【伪共享】
1. 两个线程分别修改「同一缓存行内的两个变量」；
2. 虽然变量独立，但修改会让整行失效，触发 MESI 协议来回同步；
3. 导致性能严重下降（缓存一致性风暴）。

【解决：缓存行填充（Padding）】
1. 在变量周围填充无意义字节，让不同线程修改的变量落在不同缓存行；
2. Java 用 @Contended 注解或手动填充 long[7]；
3. 例：两个 long 变量之间填充 56 字节，使其各占一个 64 字节缓存行。

【相关机制】MESI 协议维护缓存一致性，写操作会使其他核的该缓存行失效。

【追问】如何验证伪共享？→ 用两个线程分别累加不同变量，对比有/无填充的耗时，填充后明显更快。

【避坑】填充要针对「频繁写且被多线程访问」的变量；过度填充浪费内存，需权衡。`,
      source: '计算机体系结构 / 高性能并发'
    },
    {
      id: 'os-108', category: 'os', tags: ['内存', 'mmap', '文件'], difficulty: 3,
      question: 'mmap（内存映射）的原理是什么？有哪些使用场景？',
      answer: `一句话结论：mmap 把文件映射到进程的虚拟地址空间，访问内存即访问文件，避免用户态与内核态的数据拷贝。

【原理】
1. 调用 mmap 后，建立「虚拟内存区域 ↔ 文件」的映射（页表项指向文件）；
2. 首次访问映射区域时触发缺页，从文件读入对应页到物理内存；
3. 之后读写内存页，内核负责与文件同步（msync 刷盘）；
4. 省去 read/write 的「内核缓冲 → 用户缓冲」拷贝。

【使用场景】
1. 大文件读写：减少拷贝，提高效率；
2. 进程间共享内存：MAP_SHARED 实现多进程共享；
3. 零拷贝：配合 sendfile 优化网络传输；
4. 高效文件 IO（如 RocketMQ、数据库用 mmap 存日志）。

【优缺点】
· 优点：减少拷贝、懒加载、共享内存；
· 缺点：小文件收益不明显、映射大文件占用虚拟地址空间、缺页开销。

【追问】mmap 和普通 read 的区别？→ read 要拷贝到用户缓冲，mmap 直接映射，少了拷贝。

【避坑】mmap 后文件被截断或修改可能引发 SIGBUS 错误；写入后要 msync 或 munmap 保证落盘。`,
      source: 'Linux 内存管理 / 高性能 IO'
    },
    {
      id: 'os-109', category: 'os', tags: ['调度', 'CFS', 'Linux'], difficulty: 3,
      question: 'Linux 的 CFS（完全公平调度）算法是怎样的？',
      answer: `一句话结论：CFS 以「虚拟运行时间」为度量，让每个进程公平地分配 CPU 时间；每次选 vruntime 最小的进程运行。

【核心思想】
1. 每个进程维护一个 vruntime（虚拟运行时间）；
2. vruntime = 实际运行时间 × 权重系数（nice 值决定权重）；
3. 调度器每次选择 vruntime 最小的进程（最「吃亏」的优先）；
4. 用红黑树维护就绪队列，找最小值 O(log n)。

【公平性】
1. nice 值越低（优先级越高），权重越大，vruntime 增长越慢，实际分到的 CPU 越多；
2. 目标是「所有进程的 vruntime 趋近相等」，即公平分配。

【关键概念】
1. 调度周期：一段时间内让所有就绪进程至少运行一次；
2. 时间片动态分配：按权重分配时间片，而非固定值；
3. 红黑树：按 vruntime 排序，高效取最小。

【追问】CFS 和传统 O(1) 调度器有何区别？→ CFS 不再维护固定优先级队列，而是用 vruntime 动态公平调度，更平滑。

【避坑】CFS 主要调度普通进程（SCHED_OTHER）；实时进程（SCHED_FIFO/RR）优先级更高，独立调度。`,
      source: 'Linux 内核 / CFS 调度'
    },
    {
      id: 'os-110', category: 'os', tags: ['内核', '抢占', '实时性'], difficulty: 3,
      question: '什么是内核抢占和用户抢占？它们与实时性有什么关系？',
      answer: `一句话结论：用户抢占是用户态进程被更高优先级进程打断；内核抢占是内核态代码执行中也能被抢占；内核抢占能力越强，系统实时性越好。

【用户抢占】
1. 用户态进程在两种时机被抢占：时间片用完、有更高优先级进程就绪；
2. 从内核返回用户态时检查是否需要抢占。

【内核抢占】
1. 传统非抢占内核：内核态代码执行完才返回用户态，不能被抢占，延迟高；
2. 可抢占内核（Linux 2.6+）：内核态中在「安全点」（非临界区）可被抢占；
3. 通过 preempt_count 判断是否处于不可抢占的临界区（如持自旋锁时不可抢占）。

【与实时性的关系】
1. 实时系统要求「响应延迟可预测、有上限」；
2. 可抢占内核能更快响应高优先级任务，降低调度延迟；
3. Linux 的 PREEMPT_RT 补丁进一步把自旋锁等改造成可睡眠锁，实现硬实时。

【追问】为什么持自旋锁时不能抢占？→ 自旋锁是忙等，若被抢占，其他核可能长时间等这把锁，甚至死锁。

【避坑】抢占和自愿调度（进程主动 sleep/yield）不同；抢占是强制性的，由调度器触发。`,
      source: 'Linux 内核 / 实时系统'
    },
    {
      id: 'os-111', category: 'os', tags: ['内存', '大页', '性能'], difficulty: 2,
      question: '什么是大页内存（Huge Pages）？它有什么优缺点？',
      answer: `一句话结论：大页内存用更大的页（如 2MB/1GB 代替 4KB）减少页表项数量和 TLB 未命中，提升大内存应用的性能。

【原理】
1. 默认页大小 4KB，大页可为 2MB（或 1GB）；
2. 页更大 → 同样内存所需的页表项更少 → TLB 能覆盖更多内存 → 命中率更高；
3. 减少缺页和页表遍历开销。

【优点】
1. 提升 TLB 命中率，减少地址翻译开销；
2. 减少页表项，节省页表内存；
3. 适合大内存、访问模式规整的应用（数据库、JVM 堆、DPDK）。

【缺点】
1. 内存碎片：大页要求连续物理内存，可能分配失败；
2. 浪费：小应用用大页会浪费内存；
3. 换页不灵活：大页不能随意换出，增加管理复杂度。

【应用场景】
· 数据库（Oracle/MySQL）、JVM（-XX:+UseLargePages）、网络数据面（DPDK）常用大页。

【追问】大页为什么能提高性能？→ 核心是减少 TLB miss：TLB 条目数有限，页越大，同样条目覆盖的内存越多。

【避坑】大页需操作系统预先配置（hugetlbfs / 预留），分配失败时应用可能起不来。`,
      source: 'Linux 内存管理 / 性能调优'
    },
    {
      id: 'os-112', category: 'os', tags: ['Linux', '启动', 'boot'], difficulty: 2,
      question: 'Linux 系统的启动过程是怎样的？（从开机到登录）',
      answer: `一句话结论：开机后依次经历 BIOS/UEFI → 引导加载程序（GRUB）→ 加载内核 → init 进程 → 启动各服务 → 进入登录界面。

【启动流程】
1. BIOS/UEFI：硬件自检（POST），找到并加载引导设备；
2. 引导加载程序（GRUB2）：加载内核镜像和 initramfs 到内存；
3. 内核初始化：解压内核，初始化硬件、内存、调度器，挂载根文件系统（借助 initramfs 加载驱动）；
4. 启动 init 进程（PID 1）：systemd（现代发行版）或 SysV init；
5. init 启动服务：按依赖启动各系统服务（网络、ssh 等），进入目标运行级别；
6. 登录：显示登录界面（图形界面或终端）。

【关键组件】
· initramfs：临时根文件系统，包含必要驱动，帮助内核挂载真正的根文件系统；
· systemd：并行启动服务、管理依赖、接管进程。

【追问】为什么需要 initramfs？→ 内核要挂载根文件系统，但所需驱动（如磁盘控制器、文件系统模块）在根文件系统里，需要 initramfs 先加载驱动。

【避坑】不同发行版细节略有差异，但「固件 → 引导器 → 内核 → init → 服务」的主线一致。`,
      source: 'Linux 系统启动 / 大厂八股'
    },
    {
      id: 'os-113', category: 'os', tags: ['内存', 'slab', '内核'], difficulty: 3,
      question: 'slab/slub 分配器是什么？为什么内核需要它？',
      answer: `一句话结论：伙伴系统按 2 的幂次页分配内存，会产生内部碎片且频繁分配效率低；slab 分配器在其上缓存「固定大小对象」，减少碎片、提高小对象分配效率。

【背景：伙伴系统的问题】
1. 伙伴系统只按页（2^n 页）分配，适合大块内存；
2. 内核大量分配小对象（如 inode、task_struct），按页分配会浪费（内部碎片）；
3. 频繁创建/销毁小对象，直接走伙伴系统效率低。

【slab 原理】
1. 从伙伴系统申请大块内存（slab），切分成固定大小的对象；
2. 用「对象缓存（kmem_cache）」管理同类对象，如 inode_cache、dentry_cache；
3. 释放的对象放入空闲链表，下次分配直接复用，避免重复初始化；
4. 减少内部碎片、提高分配/释放速度。

【slub 改进】
· slab 的简化变体，减少元数据开销，Linux 默认用 slub。

【应用】内核中 inode、dentry、进程描述符等都通过 slab 分配。

【追问】slab 和 malloc 的关系？→ 用户态 malloc 是 glibc 的实现，内核用 slab；两者思路类似（内存池）。

【避坑】slab 解决的是「小对象高频分配」，大块内存仍走伙伴系统，两者是分层关系。`,
      source: 'Linux 内核 / 内存管理'
    },
    {
      id: 'os-114', category: 'os', tags: ['进程', '线程', '协程'], difficulty: 2,
      question: '进程、线程、协程在内存占用和调度开销上有什么区别？',
      answer: `一句话结论：进程开销最大（独立地址空间、切换要切页表），线程次之（共享地址空间、切换切寄存器栈），协程最小（用户态切换，不经过内核）。

【内存占用】
1. 进程：独立地址空间（页表、堆栈、代码段等），内存开销大；
2. 线程：共享进程地址空间，各自有独立栈（默认约 1MB/8MB），开销中等；
3. 协程：栈很小（可动态增长，几 KB），开销最小。

【调度开销】
1. 进程：内核调度，切换要切页表、刷新 TLB，开销最大；
2. 线程：内核调度，切换寄存器/栈，但共享页表，开销较小；
3. 协程：用户态调度（不陷入内核），切换只保存少量寄存器，开销极小。

【并发能力】
1. 进程：隔离性强，可跨核；
2. 线程：共享内存，需加锁；
3. 协程：单线程内高并发（如 Go goroutine 数十万级），适合 IO 密集。

【追问】为什么协程能支持海量并发？→ 内存占用极小 + 用户态切换快，无需内核参与。

【避坑】协程是「协作式」调度，一个协程阻塞（如 CPU 密集或同步 IO）会卡住同线程其他协程；CPU 密集任务应交给线程/进程。`,
      source: '操作系统原理 / 大厂八股'
    },
    {
      id: 'os-115', category: 'os', tags: ['进程', 'COW', 'fork'], difficulty: 3,
      question: '什么是写时复制（Copy-on-Write）？fork 如何利用 COW？',
      answer: `一句话结论：写时复制是「读时共享、写时才复制」：父子进程先共享同一物理页，任一方写入时才复制一份，避免不必要的拷贝。

【fork 与 COW】
1. 传统 fork：把父进程内存全部复制给子进程，开销巨大；
2. 现代 fork：父子的页表都指向同一物理页，标记为只读（COW）；
3. 若只读，共享物理页，零拷贝；
4. 若任一进程写入，触发写保护异常，内核复制该页给写入方，再标记可写。

【优点】
1. 节省内存：父子共享只读数据；
2. 加快 fork 速度：不必立即复制全部内存；
3. 配合 exec：fork 后 exec 加载新程序，很多页根本不用复制。

【追问】COW 在哪些场景用到？→ fork、虚拟机快照、Redis 的 BGSAVE（fork 子进程 RDB 持久化）、std::string 的 COW（已弃用）。

【避坑】COW 下 fork 的「内存占用」看似翻倍，实际只有真正写入的部分才复制；但大量写入时仍会复制。`,
      source: '操作系统原理 / Linux fork'
    },
    {
      id: 'os-116', category: 'os', tags: ['锁', '自旋锁', '并发'], difficulty: 2,
      question: '什么是自旋锁？什么时候该用自旋锁而不是互斥锁？',
      answer: `一句话结论：自旋锁在拿不到锁时「忙等」而不让出 CPU，适合临界区极短且多核的场景；互斥锁拿不到就睡眠，适合临界区长或单核场景。

【自旋锁特点】
1. 忙等：while 循环反复尝试，不睡眠；
2. 优点：避免上下文切换开销，临界区短时效率高；
3. 缺点：忙等浪费 CPU，单核下会死锁（占用 CPU 的线程无法让出，别的线程拿不到锁）。

【选择依据】
1. 临界区很短（几个指令/微秒级）→ 自旋锁；
2. 多核 CPU，且期望等待时间短 → 自旋锁；
3. 临界区长、可能阻塞/睡眠 → 互斥锁；
4. 单核或不确定 → 互斥锁。

【进阶】自适应自旋锁（如 Java synchronized 锁升级）：先自旋，自旋超时仍拿不到就升级为重量级锁（睡眠）。

【追问】自旋锁能用在中断上下文吗？→ 可以，但要禁止本地中断，防止「持有锁的进程被中断且中断处理程序也抢这把锁」导致死锁。

【避坑】自旋锁内不能睡眠、不能调用可能阻塞的函数；持有自旋锁期间应尽量短。`,
      source: '操作系统原理 / Linux 内核锁'
    },
    {
      id: 'os-117', category: 'os', tags: ['信号', '进程', 'Linux'], difficulty: 2,
      question: 'Linux 的信号（Signal）机制是什么？常见信号有哪些？如何处理？',
      answer: `一句话结论：信号是进程间「异步通知」的机制，用于告知进程发生了某事件；进程可自定义处理函数、忽略或按默认行为处理。

【常见信号】
1. SIGINT（Ctrl+C，2）：中断进程；
2. SIGKILL（9）：强制终止，不可捕获/忽略；
3. SIGTERM（15）：请求终止，可捕获做优雅退出；
4. SIGSEGV（11）：段错误（访问非法内存）；
5. SIGCHLD（17）：子进程状态变化（退出/停止）；
6. SIGPIPE（13）：向已关闭的管道/连接写数据；
7. SIGHUP（1）：终端挂断，常用于重新加载配置。

【处理方式】
1. 默认行为：终止、忽略、或停止进程；
2. 自定义处理函数：signal() 或 sigaction() 注册；
3. 忽略：signal(SIGPIPE, SIG_IGN)。

【典型应用】
· 优雅退出：捕获 SIGTERM 做清理再退出（如 Nginx、K8s 容器停止）；
· 僵尸进程回收：父进程处理 SIGCHLD。

【追问】SIGKILL 和 SIGTERM 的区别？→ SIGTERM 可被捕获做优雅退出，SIGKILL 不可捕获直接杀，是最强硬的终止。

【避坑】信号处理函数里要避免调用非异步安全的函数（如 printf、malloc），否则可能死锁；网络编程常忽略 SIGPIPE 防止进程被杀。`,
      source: 'Linux 信号 / 大厂八股'
    },
    {
      id: 'os-118', category: 'os', tags: ['守护进程', 'daemon', 'Linux'], difficulty: 2,
      question: '什么是守护进程（daemon）？如何创建一个守护进程？',
      answer: `一句话结论：守护进程是后台长期运行、脱离终端、独立于会话的进程，常用于提供服务；创建需「fork + setsid + 二次 fork + 重定向标准 IO」。

【守护进程特点】
1. 后台运行，不关联任何终端；
2. 常驻内存，随系统启动或按需启动；
3. 例：sshd、nginx、systemd 管理的服务。

【创建步骤】
1. 第一次 fork，父进程退出：让子进程脱离原会话，避免获得终端控制权；
2. setsid()：子进程创建新会话，成为会话组长，脱离控制终端；
3. 第二次 fork，父进程退出：确保进程不是会话组长，无法再打开终端；
4. chdir('/')：改变工作目录到根，避免占用挂载点；
5. umask(0)：重设文件权限掩码；
6. 关闭/重定向标准输入输出错误（0/1/2）到 /dev/null 或日志。

【现代实践】
· 现在多用 systemd 等进程管理器托管服务，无需手动写这些步骤，但原理是面试考点。

【追问】为什么要两次 fork？→ 第二次 fork 让进程不再是会话组长，彻底失去重新关联控制终端的能力。

【避坑】守护进程要处理日志输出（重定向到文件），否则关闭终端后输出会异常。`,
      source: 'UNIX 环境高级编程 / Linux'
    },
    {
      id: 'os-119', category: 'os', tags: ['内存', 'malloc', '分配'], difficulty: 3,
      question: 'malloc 的实现原理是什么？（brk、mmap、内存池）',
      answer: `一句话结论：malloc 是用户态内存分配器，通过 brk/mmap 向内核申请内存，再用空闲链表/内存池管理，按大小分级优化分配。

【向内核申请内存的两种方式】
1. brk：调整堆顶指针，分配连续小内存（<128KB），适合小对象；
2. mmap：直接映射一块内存（>=128KB），释放时立即归还，适合大对象。

【malloc 内部机制】
1. 维护空闲块链表（free list），分配时找合适块（first-fit/best-fit）；
2. 小对象用内存池（如 tcache、fastbins、small bins），大对象用 mmap；
3. 空闲块合并：相邻空闲块合并减少碎片；
4. 对齐：按 8/16 字节对齐。

【glibc 的 malloc】
· 用 tcache（线程缓存）加速小对象分配，多线程无锁分配；
· 分级 bins 管理不同大小的空闲块。

【追问】为什么频繁 malloc/free 会有性能问题？→ 锁竞争、碎片化、系统调用；解决用内存池/对象池复用。

【避坑】malloc 返回的是虚拟地址，实际物理内存可能「惰性分配」——首次写入时才真正分配物理页。`,
      source: 'glibc malloc / 内存分配器'
    },
    {
      id: 'os-120', category: 'os', tags: ['NUMA', '架构', '性能'], difficulty: 3,
      question: '什么是 NUMA 架构？它对性能有什么影响？',
      answer: `一句话结论：NUMA（非统一内存访问）架构下，CPU 访问「本地内存」快、访问「远端内存」慢；对性能敏感的应用要考虑内存亲和性。

【背景】
1. SMP（对称多处理）：所有 CPU 共享一条总线访问统一内存，扩展性差（总线成为瓶颈）；
2. NUMA：多个 CPU 组成多个「节点」，每个节点有自己的本地内存，节点间通过互联总线访问远端内存。

【特点】
1. 访问本地内存快，访问远端内存慢（有额外延迟）；
2. 扩展性好，适合多核服务器；
3. Linux 用 numactl 查看节点拓扑（numactl --hardware）。

【性能影响】
1. 进程的内存分配应尽量「就近」绑定到运行它的 CPU 节点；
2. 跨节点访问内存会导致延迟上升、带宽下降；
3. 多线程应用若线程跨节点且内存远端访问，性能会退化。

【优化】
1. CPU 亲和性绑定（taskset/numactl）；
2. 内存策略：--membind 绑定本地内存；
3. 数据库/大数据应用关注 NUMA 绑定。

【追问】为什么数据库（如 MySQL）在 NUMA 下要关 NUMA 或用 numactl？→ 避免内存分配集中在某节点导致不平衡，或远端访问拖慢性能。

【避坑】NUMA 的「内存不平衡」可能让单节点内存耗尽而其他节点空闲，需要 interleave 策略分散。`,
      source: '计算机体系结构 / NUMA'
    }
  ];

  global.App = global.App || {};
  global.App.os3Bank = OS3;
})(window);

# OfferAgent 开发日志

> 记录项目实现过程中的关键决策、问题与解决方案。

---

## 2026-09-01

### 今日完成

1. **需求与产品文档**
   - 完成 `docs/需求分析.md`，明确痛点、目标用户、核心功能与备考闭环。
   - 完成 `docs/产品设计.md`，定义首页、刷题、题库、统计四个视图与交互流程。

2. **项目初始化**
   - 初始化 Git 仓库，关联远程 `https://github.com/cheng607/offeragent.git`。
   - 创建目录结构：`css/`、`js/`、`docs/`。
   - 编写 `README.md`、`.gitignore`、`LICENSE`。

3. **核心模块实现**
   - `js/config.js`：分类、难度、评分、艾宾浩斯间隔参数配置。
   - `js/bank.js`：31 道结构化面试题，覆盖前端、后端、算法、数据库、计算机网络、操作系统六大分类。
   - `js/storage.js`：`localStorage` 状态持久化、导入导出、版本迁移。
   - `js/scheduler.js`：基于艾宾浩斯遗忘曲线的记忆调度算法。
   - `js/stats.js`：掌握度分布、每日趋势、分类进度、连续打卡等统计。
   - `js/charts.js`：自绘 SVG 图表（折线图、环形图、水平柱状图）。
   - `js/app.js`：单页应用控制器，实现视图切换、卡片翻转、评分、设置。
   - `index.html` + `css/style.css`：移动端优先的响应式 UI。

4. **测试与验证**
   - 使用 `node --check` 检查全部 JS 文件语法。
   - 编写并运行 Node 单元测试，覆盖题库、存储、调度、统计四大模块，15 项断言全部通过。
   - 启动本地静态服务器，使用无头 Chrome 截取首页、刷题、题库、统计四个视图的移动端渲染图。

### 关键问题与解决

#### 问题 1：底部导航在 390px 视口下只显示 3 个 tab

**现象**：无头 Chrome 截图中底部只显示「首页 / 刷题 / 题库」，缺少「统计」。

**排查**：
- 检查 DOM，确认 4 个 `<button class="tab">` 均存在。
- 通过注入临时脚本打印 `window.innerWidth` 与 `getBoundingClientRect()`，发现无头 Chrome 实际视口为 500px，而截图文件宽度为 390px。tabbar 按 `max-width: 480px` 渲染，因此截图只能看到左侧 390px 内容，造成「缺少第 4 个 tab」的假象。
- 在 480px 宽度截图下，4 个 tab 完整显示。

**解决**：
- 将 tabbar 从 `display: flex` 改为 `display: grid; grid-template-columns: repeat(4, minmax(0, 1fr))`，确保无论内容宽度如何，4 个 tab 始终等分容器。
- 为 `.tab` 增加 `min-width: 0` 与 `overflow: hidden`，防止内容撑开网格列。
- 将 SVG 图标与文字适当缩小（20px / 10px），提升极小屏幕下的容纳能力。

**结论**：UI 布局本身正确；无头截图的视口不一致是测试环境 artifact。真实移动设备上 4 个 tab 会正常显示。

#### 问题 2：图表 SVG 路径拼接错误

**现象**：`js/charts.js` 中折线图的 `<path>` 标签嵌套错误。

**解决**：重构 `renderRetentionCurve`，使用独立的 `path()` 函数生成 `d` 属性，再拼接到 `<path d="...">` 中，避免标签嵌套。

### 今日新增迭代（晚间）

5. **题库扩充**
   - 在 `js/bank.js` 中为六大分类各补充 4–5 题，总题量从 31 题提升至 **60 题**，每分类 10 题，覆盖原型链、深拷贝、缓存三穿、分布式锁、HTTP/2/3、CORS、页面置换等高频考点。
   - 通过 Node 脚本验证：总题数 60、id 唯一、字段完整、分类分布正确。

6. **PWA 支持**
   - 生成应用图标：`icon-512.png`、`icon-192.png`、`apple-touch-icon.png`。
   - 新增 `manifest.json`：`standalone`、`portrait`、主题色/背景色、图标声明。
   - 新增 `sw.js`：安装时缓存核心静态资源，Cache-First 策略，支持离线访问；版本号 `offeragent-v2`，后续发布只需递增版本。
   - `index.html` 中增加 `theme-color`、`apple-mobile-web-app-capable`、`apple-touch-icon`、`manifest` 引用，并在 `load` 事件注册 Service Worker。
   - 清理 `index.html` 中残留的无用 `data-page-node-id` 属性。

7. **部署**
   - 将新增代码提交并推送到 `cheng607/offeragent` 的 `main` 分支。
   - GitHub Pages 自动重新部署，在线地址：https://cheng607.github.io/offeragent/

8. **题库重写与来源校准（对标公开题库）**
   - 联网核实六大分类的权威题源，将 `js/bank.js` 从 60 题重写扩充为 **78 题**（六大分类各 13 题）。
   - 全部题目的 `source` 由占位「高频面试题」改为**具体出处**，覆盖：牛客面经（含美团/字节/网易/滴滴等大厂标注）、大厂八股文（字节/腾讯/阿里/美团）、LeetCode 热题 HOT100、LeetCode 经典（146 LRU、206 反转链表、232 用栈实现队列、141/142 环形链表、1/15 两数/三数之和）、剑指 Offer、MySQL/Redis 八股文等。
   - 答案全部重写为**面试结构化回答范式**：「一句话结论 → 分点论证 → 面试官追问/避坑/加分点」，78/78 题均含「一句话结论」。
   - 扩充高难度题（难度 3 困难 21 题、中等 44 题、简单 13 题）：如手写 Promise、LRU 缓存、MVCC、redo/undo/binlog、epoll、零拷贝、TCP 粘包、HTTPS 中间人攻击、进程线程协程、死锁等。
   - 修复重写过程中 answer 字符串内英文单引号未转义导致的 `SyntaxError`（用脚本批量转义后 `node --check` 通过）。

9. **题库验证与部署（含网络降级方案）**
   - 验证脚本确认：78 题、六大分类各 13 题、无重复 id、无字段缺失、无占位 source、78/78 题含「一句话结论」。
   - 15 项核心逻辑单元测试全部通过。
   - 部署时遇到 `github.com` HTTPS 推送被代理拦截（502 CONNECT tunnel），而 `api.github.com` 可达。采用 **GitHub Git Data API** 走 `api.github.com` 完成推送：创建 blob → 重建 tree（tree SHA 与本地逐字节一致）→ 创建 commit → 更新 `main` 分支引用。
   - GitHub Pages 已重新构建（`status: built`，commit 指向新题库），线上 `js/bank.js` 已更新为 78 题。
   - **注意**：API 创建的远端 commit（`199643b`）与本地 commit（`ce6f47d`）内容完全一致、仅提交时间时区被 GitHub 归一化为 UTC 导致 SHA 不同。后续网络恢复后用 `git fetch origin && git reset --hard origin/main` 即可对齐（无内容丢失）。

10. **题库扩充至 120 题 + 答案深化**
   - 新增 `js/bank-extra.js`（第二批扩充题库），在 `bank.js` 加载时通过 `App.extraBank` 合并进主题库，`index.html` 在 `bank.js` 前加载该文件。
   - 题库从 78 题扩充到 **120 题**（六大分类各 20 题），新增 42 题：前端 this 指向/虚拟 DOM/模块化、后端微服务/分布式事务/熔断降级限流/RPC/线程安全/分布式 ID、算法堆与 TopK/回溯/贪心/BFS·DFS/KMP/快速选择/位运算、数据库连接池/主键设计/SQL 执行流程/SQL 注入/间隙锁/死锁/数据倾斜、网络 WebSocket/重定向/HTTP 报文/CDN/证书链/HTTPDNS/ARP、操作系统内存布局/协程/MESI/中断/inode/内存屏障/伙伴系统。
   - 全部答案均按「一句话结论 → 分点 → 面试官追问 → 避坑」深化，120/120 题含「一句话结论」。
   - 难度分布：简单 15 · 中等 70 · 困难 35。
   - 更新 `sw.js`：缓存列表加入 `bank-extra.js`，版本号 `v2` → `v3`（保证已安装 PWA 用户拉取新资源）。
   - 验证：`node --check` 全部通过；120 题完整校验（无重复 id/无字段缺失/无占位 source）通过；15 项单元测试通过；无头 Chrome 渲染验证通过（tabbar 正常、题库总数 120 已渲染）。

11. **题库扩至 310 题 + 新增「人工智能」分类 + 答案完善**
   - 新增「人工智能」分类：`config.js` 的 `CATEGORIES` 增加 `ai`（颜色 `#14B8A6`）。由于统计/图表/筛选全部数据驱动，新增分类无需改动其他代码。
   - 新增 6 个题库文件（各文件挂载到 `global.App` 的不同键，由 `bank.js` 末尾合并）：
     - `js/bank-fe2.js`：前端 JS 深入 / 手写 / 浏览器 / 安全，40 题（`fe-021`~`fe-060`）。
     - `js/bank-fe3.js`：前端 React / Vue / 工程化 / TS / 性能，45 题（`fe-061`~`fe-105`）。
     - `js/bank-ai1.js`：机器学习基础，28 题（`ai-001`~`ai-028`）。
     - `js/bank-ai2.js`：深度学习，28 题（`ai-029`~`ai-056`）。
     - `js/bank-ai3.js`：大模型 / LLM，28 题（`ai-057`~`ai-084`）。
     - `js/bank-ai4.js`：NLP / CV / 工程化，21 题（`ai-085`~`ai-105`）。
   - 题库从 120 题扩充到 **310 题**：前端 20→105 题、AI 新增 105 题，后端/算法/数据库/网络/操作系统各 20 题。
   - `bank.js` 合并逻辑由单一 `concat(extraBank)` 改造为遍历 `EXTRA_BANKS` 数组，未来新增题库文件只需在列表追加键名并挂载到 `global.App`。
   - 答案统一完善为「一句话结论 + 完整解答（核心概念 / 关键要点 / 代码示例 / 面试追问 / 避坑）」范式，并全部改用反引号模板字符串编写，从根源规避英文单引号转义导致的 `SyntaxError`。
   - `sw.js` 缓存列表新增 6 个题库文件，版本号 `v3` → `v4`。
   - 验证：`node --check` 全部通过；310 题完整校验（前端 105 / AI 105、无重复 id、无字段缺失、无占位 source、310/310 一句话结论）通过；15 项单元测试通过；无头 Chrome 渲染验证（「人工智能」及全部 7 分类正常显示、题数 310 正确渲染）。

### 待办 / 下一步

- [x] 使用 GitHub Token 将代码推送到 `cheng607/offeragent`。
- [x] 开启 GitHub Pages，获取在线预览地址。
- [x] 补充更多题目至 60 题。
- [x] 添加 PWA 支持（添加到主屏幕、离线访问）。
- [x] 题库重写扩充至 78 题，答案结构化 + 真实来源标注。
- [x] 题库扩充至 120 题（六大分类各 20 题），答案进一步深化。
- [x] 题库扩充至 310 题（前端 105 / AI 105），答案完善为「一句话结论 + 完整解答」。
- [ ] 支持按知识点/公司标签筛选。
- [ ] 评估数据云端备份（解决换设备丢进度问题）。

---

## 2026-09-02

### 今日完成

1. **修复卡片翻转 bug**
   - 现象：第一次点击卡片翻到答案后，再次点击无法翻回题目面。
   - 原因：`showPracticeCard` 中原逻辑用 `classList.contains('flipped')` 提前 `return`，阻止了第二次点击。
   - 修复：改为 `flipCard.classList.toggle('flipped')`，每次点击都切换正反面；引入 `revealed` 状态，保证「首次揭晓答案后评分按钮保持可用」。
   - 提示文案更新为「点击卡片查看答案 · 再次点击返回题目」。

2. **五大分类题库各扩充至 100 题（总题量 310 → 710）**
   - 新增 5 个扩充题库文件（各挂载到 `global.App` 的不同键，由 `bank.js` 末尾合并）：
     - `js/bank-be2.js`：后端 80 题（`be-021`~`be-100`），覆盖 JVM/GC/并发、Spring/MyBatis、消息队列、分布式事务/一致性、Redis、微服务、系统设计、线上排查、容器/K8s 等。
     - `js/bank-al2.js`：算法 80 题（`al-021`~`al-100`），覆盖排序/二分/链表/树/图/动态规划/贪心/滑动窗口/回溯/单调栈/位运算等。
     - `js/bank-db2.js`：数据库 80 题（`db-021`~`db-100`），覆盖 InnoDB/索引/锁/MVCC/事务/主从复制/分库分表/explain/备份高可用/NoSQL/缓存一致性等。
     - `js/bank-net2.js`：网络 80 题（`net-021`~`net-100`），覆盖 TCP 可靠性/HTTP 缓存/TLS/HTTP2·3/Web 安全/DNS·CDN/负载均衡/RPC/容器网络等。
     - `js/bank-os2.js`：操作系统 80 题（`os-021`~`os-100`），覆盖进程线程/调度/内存管理/文件系统/IO/并发/内核机制/Linux 实战/容器虚拟化等。
   - 题量分布：前端 105 / AI 105 / 后端 100 / 算法 100 / 数据库 100 / 网络 100 / 操作系统 100，共 **710 题**。

3. **集成与配置**
   - `js/bank.js`：`EXTRA_BANKS` 追加 `be2Bank`、`al2Bank`、`db2Bank`、`net2Bank`、`os2Bank`。
   - `index.html`：在 `bank.js` 前加载 5 个新题库脚本。
   - `js/config.js`：`DIFFICULTY` 增加难度 4「专家」（后端/网络/操作系统部分高难度题使用）。
   - `sw.js`：缓存列表加入 5 个新题库文件，版本号 `v4` → `v5`。

4. **验证**
   - `node --check` 全部 JS 文件语法通过。
   - 完整校验脚本通过：总题数 710、分类分布正确、无重复 id、无字段缺失、无占位 source、710/710 含「一句话结论」、平均答案长度 377 字、id 前缀与分类一致。

### 关键问题与解决

#### 问题 1：新增高难度题使用了 `difficulty: 4`，但 `config.DIFFICULTY` 仅定义 1~3

**现象**：后端/网络/操作系统部分「专家级」题目标记为 `difficulty: 4`，而 `difficultyLabel` 未覆盖 4，会回落到「简单」标签。

**解决**：在 `config.DIFFICULTY` 中增加 `{ value: 4, label: '专家' }`，使难度徽章正确显示。

---

## 2026-09-02（续）· 对标面试鸭扩充题库（总题量 710 → 940）

### 背景

用户反馈题库「答案和题目不够完整全面」，提出直接爬取牛客/面试鸭转换入库。经实测：

- **牛客**：SPA + 反爬 + 登录墙，无法直接抓取；
- **面试鸭**：公开目录可读，但大量题目标 VIP 付费、深度答案需会员。

结论：**直接爬取不可行**，改为「用公开题库目录做选题对标 + 联网采集公开真题 + 撰写深度答案」的合规替代方案（用户已确认「现有分类继续扩 + 新增热门方向」「答案进一步深化」）。

### 本轮完成

1. **新增「系统设计」独立分类**
   - `js/config.js` 增加 `system-design` 分类（`C05C8A`）；
   - `js/bank-sd1.js`：50 题（`sd-001`~`sd-050`），覆盖容量估算、可用性、缓存/CDN、微服务/DDD、RPC/消息队列/线程池设计、分布式 ID/限流/锁、Feed 流/IM/热搜 TopK、秒杀防超卖、幂等/熔断/可观测性等。

2. **AI 分类扩充 AI Agent + 大模型应用**
   - `js/bank-ai5.js`：50 题（`ai-106`~`ai-155`），覆盖 Agent 组件/ReAct/Reflexion/Tool Calling/记忆/多 Agent、MCP/A2A 协议、Agent 安全与评估、RAG 全链路（分块/混合检索/Rerank/GraphRAG/RAGAS）、PagedAttention/投机采样、语义缓存/成本优化/流式与结构化输出等。

3. **后端扩充（工具链 + 场景）**
   - `js/bank-be3.js`：30 题（`be-101`~`be-130`），覆盖 Git 三区/merge·rebase/reset、SOLID/工厂/单例/代理/观察者等设计模式、Netty（Reactor/粘包拆包/零拷贝）、JWT/RBAC、对账/大表归档/高并发计数、灰度路由/敏感词 DFA/防盗链、测试金字塔等。

4. **五分类各补 20 题**
   - `js/bank-fe4.js`（`fe-106`~`fe-125`）：并发控制 Promise、大数相加、memoize/compose、axios·fetch 区别、SSE、渲染流水线、Observer、AbortController、sourcemap、点击劫持、SWR、useState 闭包陷阱、微前端沙箱、动态高度虚拟滚动等。
   - `js/bank-al3.js`（`al-101`~`al-120`）：滑动窗口/中心扩展/合并 K 链表/股票系列/LIS/编辑距离/最小路径和/接雨水/LCA/序列化/最大路径和/旋转数组搜索/拓扑排序/单词拆分/Trie/并查集/单调栈/手写最小堆等。
   - `js/bank-db3.js`（`db-101`~`db-120`）：UPDATE 执行流程/Buffer Pool 冷热分离/自增主键/count 区别/幻读与间隙锁/唯一索引 change buffer/锁体系/隔离级别/深分页/最左前缀/前缀索引/大字段/主从延迟/高可用架构/跨库查询/在线 DDL/8.0 特性/Redis 快与底层结构/快照读当前读等。
   - `js/bank-net3.js`（`net-101`~`net-120`）：TIME_WAIT·CLOSE_WAIT/滑动窗口/Nagle·延迟确认/四次挥手/UDP 可靠传输·QUIC/HTTP3/TLS 握手/队头阻塞/URL 全流程/keep-alive/mTLS/NAT·内网穿透/四层七层负载/DDoS/IPv6/MTU·MSS/BBR/正反向代理/502·504·499/tcpdump 排查等。
   - `js/bank-os3.js`（`os-101`~`os-120`）：上下文切换/线程同步/CAS/虚拟地址翻译/缺页中断/五大 IO 模型/伪共享/mmap/CFS/内核抢占/大页内存/Linux 启动/slab/进程线程协程/写时复制/自旋锁/信号/守护进程/malloc/NUMA 等。

5. **集成**
   - `js/bank.js`：`EXTRA_BANKS` 追加 `fe4Bank`、`ai5Bank`、`be3Bank`、`al3Bank`、`db3Bank`、`net3Bank`、`os3Bank`、`sd1Bank`。
   - `index.html`：在 `bank.js` 前加载 8 个新题库脚本。
   - `sw.js`：缓存列表加入 8 个新题库文件，版本号 `v5` → `v6`。

6. **验证**
   - 8 个新题库文件全部 `node --check` 通过；
   - 全量校验通过：总题数 **940**、分类分布正确（前端 125 / 后端 130 / 算法 120 / 数据库 120 / 网络 120 / 操作系统 120 / 系统设计 50 / AI 155）、无重复 id、无字段缺失、无占位 source、940/940 含「一句话结论」、平均答案长度 427 字、id 前缀与分类一致。

### 关键问题与解决

#### 问题 1：直接爬取牛客/面试鸭不可行

**现象**：牛客为 SPA + 反爬 + 登录墙；面试鸭答案大量 VIP 付费。

**解决**：改为「公开目录选题对标 + 公开真题采集 + 深度答案撰写」，既合规又能对标权威题源，已向用户说明。

---

## 后续迭代建议

1. **题库管理**：将题库拆分为 JSON 文件，便于运营人员批量导入。
2. **数据同步**：增加云端备份能力，换设备不丢进度。
3. **社交打卡**：分享每日学习卡片到朋友圈/微信状态。
4. **智能提醒**：基于复习到期时间，推送微信服务通知或浏览器通知。
5. **暗色模式**：通过 `prefers-color-scheme` 自动切换。

# OfferAgent 开发日志

> 记录项目实现过程中的关键决策、问题与解决方案。

---

## 2026-09-01

### 今日完成

1. **需求与产品文档**
   - 完成 `docs/需求分析.md`，明确痛点、目标用户、核心功能与备考闭环。
   - 完成 `docs/产品设计.md`，定义首页、刷题、题库、统计四个视图与交互流程。

2. **项目初始化**
   - 初始化 Git 仓库，关联远程 `https://github.com/cheng607/offeragent.git`。
   - 创建目录结构：`css/`、`js/`、`docs/`。
   - 编写 `README.md`、`.gitignore`、`LICENSE`。

3. **核心模块实现**
   - `js/config.js`：分类、难度、评分、艾宾浩斯间隔参数配置。
   - `js/bank.js`：31 道结构化面试题，覆盖前端、后端、算法、数据库、计算机网络、操作系统六大分类。
   - `js/storage.js`：`localStorage` 状态持久化、导入导出、版本迁移。
   - `js/scheduler.js`：基于艾宾浩斯遗忘曲线的记忆调度算法。
   - `js/stats.js`：掌握度分布、每日趋势、分类进度、连续打卡等统计。
   - `js/charts.js`：自绘 SVG 图表（折线图、环形图、水平柱状图）。
   - `js/app.js`：单页应用控制器，实现视图切换、卡片翻转、评分、设置。
   - `index.html` + `css/style.css`：移动端优先的响应式 UI。

4. **测试与验证**
   - 使用 `node --check` 检查全部 JS 文件语法。
   - 编写并运行 Node 单元测试，覆盖题库、存储、调度、统计四大模块，15 项断言全部通过。
   - 启动本地静态服务器，使用无头 Chrome 截取首页、刷题、题库、统计四个视图的移动端渲染图。

### 关键问题与解决

#### 问题 1：底部导航在 390px 视口下只显示 3 个 tab

**现象**：无头 Chrome 截图中底部只显示「首页 / 刷题 / 题库」，缺少「统计」。

**排查**：
- 检查 DOM，确认 4 个 `<button class="tab">` 均存在。
- 通过注入临时脚本打印 `window.innerWidth` 与 `getBoundingClientRect()`，发现无头 Chrome 实际视口为 500px，而截图文件宽度为 390px。tabbar 按 `max-width: 480px` 渲染，因此截图只能看到左侧 390px 内容，造成「缺少第 4 个 tab」的假象。
- 在 480px 宽度截图下，4 个 tab 完整显示。

**解决**：
- 将 tabbar 从 `display: flex` 改为 `display: grid; grid-template-columns: repeat(4, minmax(0, 1fr))`，确保无论内容宽度如何，4 个 tab 始终等分容器。
- 为 `.tab` 增加 `min-width: 0` 与 `overflow: hidden`，防止内容撑开网格列。
- 将 SVG 图标与文字适当缩小（20px / 10px），提升极小屏幕下的容纳能力。

**结论**：UI 布局本身正确；无头截图的视口不一致是测试环境 artifact。真实移动设备上 4 个 tab 会正常显示。

#### 问题 2：图表 SVG 路径拼接错误

**现象**：`js/charts.js` 中折线图的 `<path>` 标签嵌套错误。

**解决**：重构 `renderRetentionCurve`，使用独立的 `path()` 函数生成 `d` 属性，再拼接到 `<path d="...">` 中，避免标签嵌套。

### 待办 / 下一步

- [ ] 使用 GitHub Token 将代码推送到 `cheng607/offeragent`。
- [ ] 开启 GitHub Pages，获取在线预览地址。
- [ ] 收集用户反馈后补充更多题目（目标 100+）。
- [ ] 评估是否需要封装为微信小程序或 PWA。

---

## 后续迭代建议

1. **题库管理**：将题库拆分为 JSON 文件，便于运营人员批量导入。
2. **数据同步**：增加云端备份能力，换设备不丢进度。
3. **社交打卡**：分享每日学习卡片到朋友圈/微信状态。
4. **智能提醒**：基于复习到期时间，推送微信服务通知或浏览器通知。
5. **暗色模式**：通过 `prefers-color-scheme` 自动切换。
